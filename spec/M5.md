---

title: AI: scent/fear diffusion, horde, pathing
milestone: M5
name: ai   # M5 — ai
ext_blocks: 89
clusters: 3
cross_file_deps: 20
self_contained: true
index: see llms.txt or the <details> block index below
layer: engine
build_phase: P2-world
build_order: 7
---


## M5 — NPCs / zombie hordes (base)


> **M5 quick-index (16 EXT blocks).** Read ONLY the block(s) you need — each is self-contained.

<details><summary>M5 block index (click to expand)</summary>

- `M5-EXT-01` Asynchronous Tile-Voxelized NavMesh Baker
- `M5-EXT-02` Dual-Quaternion GPU Compute Skinning
- `M5-EXT-03` Dynamic Logistic Spawn Pacing
- `M5-EXT-04` Perimeter Illumination Repulsion
- `M5-EXT-05` Hydrostatic Velocity Penalties
- `M5-EXT-06` AI Behavior / Utility System
- `M5-EXT-07` Navigation / Pathfinding
- `M5-EXT-08` Procedural Animation (physics/IK)
- `M5-EXT-09` AI Perception System
- `M5-EXT-10` AI Director (horde pacing)
- `M5-EXT-11` Horde Emergence / Spawn Waves
- `M5-EXT-12` Zombie Facing / Emerge Animation
- `M5-EXT-13` Zombie FSM States (Dormant→Idle→Wander→Alert→Chase→Attack→Flee)
- `M5-EXT-14` Hearing Perception (directional cone + freq)
- `M5-EXT-15` Short-Term Memory & Investigation
- `M5-EXT-16` Horde Merge & Split Behavior
- `M5-EXT-64` Sleeper Zombie Spawn & Wake System
- `M5-EXT-65` Zombie Scent / Pheromone Trail
- `M5-EXT-66` Stealth Approach System

</details>

##### Implementation Steps

* ECS Native Spawning. Navigation Grid A* Pathfinding.
* Dual Quaternion Compute Skinning.
* Dynamic Logistic Spawn Pacing (`dN/dt = rN(1−N/K)`) reading AI Director threat.
* Perimeter Illumination Repulsion (light sources add nav-grid weights). Hydrostatic Velocity Penalties.

##### Decisions to flag

Horde count target (~200). CPU vs GPU skinning target.

##### Exit Criteria

* ~200 zombies pathfind efficiently. GPU skinning confirmed.
* Zombies respond to destroyed barricades immediately.

### Extended Systems Library — engine-side additions for M5

*Systems proposed in later design-iteration sessions, folded in here at their correct pipeline destination. IDs are stable — reference them (e.g. `M9-EXT-03`) when discussing this doc with the coding agent so everyone means the same system.*

### Cluster A — bootstrap core


#### [M5-EXT-01] Asynchronous Tile-Voxelized NavMesh Baker

> **tags** · NavMesh
> **tl;dr** · Replaces static grids in M5 (Navigation Grid A* Pathfinding) and reacts to M3 macro-destruction structural collapses.
> **ctx** · Asynchronous Tile-Voxelized NavMesh Baker -- Replaces static grids in M5 (Navigation Grid A* Pathfinding) and reacts to M3 macro-destruction structural collapses.
> **meta** · depends-on: - · depended-by: M5-EXT-04

##### Systems Touched
Replaces static grids in M5 (Navigation Grid A* Pathfinding) and reacts to M3 macro-destruction structural collapses.

##### Math
Voxel generation checks surface orientation by evaluating the dot product between the Jolt geometry surface normal (n) and the world up vector (u), filtering out non-traversable slope boundaries:

`n · u ≥ cos(θ_max)`

##### Algorithm
A simple, flat navigation grid breaks completely when structural components collapse under horde load (M3) or players erect free-form fortifications across choke points. This feature converts the navigation framework into a collection of asynchronous coordinate tiles matching your world chunk bounds. When a structural graph link severs or a barricade component is emplaced, the engine flags the local navigation tile as dirty. An enkiTS background thread task triggers a 3D Jolt box-cast sweeping pass across the local tile area. The sweep samples surface height coordinates, filters out non-traversable angles using the slope dot-product boundary, and bakes a dense, local navigation connectivity grid. The new navigation tile is dynamically re-stitched to adjacent nodes, updating AI routing live without causing main-thread frame drops.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombie pathfinding adapts intelligently to dynamic changes in the environment. Kicking a rotting locked door open or blasting a hole through a residential wall instantly causes trailing hordes to alter their pursuit trajectories. They stream through the new structural opening or navigate around debris piles left by collapsed building roofs, instead of walking into walls or pathing along broken nodes.

#### [M5-EXT-02] Dual-Quaternion GPU Compute Skinning

> **tags** · GPU
> **tl;dr** · Declares the actual "Dual Quaternion Compute Skinning" bullet from M5's Implementation Steps above. Note: [M5-EXT-19] refers to this system as "M5.2's existi...
> **ctx** · Dual-Quaternion GPU Compute Skinning -- Declares the actual "Dual Quaternion Compute Skinning" bullet from M5's Implementation Steps above. Note: [M5-EXT-19] refers to this system as "M5.2's existing Dual-Quaternion compute skinning" — read that as "M5's"; this base-M5 bullet is the canonical declaration. Consumes bone bind weights from [M5-EXT-19]'s Master Skeleton and per-bone pose data from M5.2's active ragdolls / IK-driven locomotion targets, and writes finished vertex positions into M1's buffer-device-address skinned-mesh path.
> **meta** · depends-on: M5-EXT-19 · depended-by: -

##### Systems Touched
Declares the actual "Dual Quaternion Compute Skinning" bullet from M5's Implementation Steps above. Note: `[M5-EXT-19]` refers to this system as "M5.2's existing Dual-Quaternion compute skinning" — read that as "M5's"; this base-M5 bullet is the canonical declaration. Consumes bone bind weights from `[M5-EXT-19]`'s Master Skeleton and per-bone pose data from M5.2's active ragdolls / IK-driven locomotion targets, and writes finished vertex positions into M1's buffer-device-address skinned-mesh path.

##### Math
Per-vertex skinning blends unit dual quaternions (screw-motion interpolation) rather than bone matrices, using the weights `[M5-EXT-19]` already computes and normalizes:

`q̂_blend = Σ w_i · q̂_i`, then `q̂_final = q̂_blend / ‖q_blend‖` — normalizing by the real-part magnitude renormalizes both parts of the dual quaternion together.

##### Algorithm
Linear blend skinning interpolates bone matrices directly, which collapses volume at extreme twists — the classic "candy-wrapper" pinch at elbows, knees, and hips. Dual quaternion skinning avoids this by interpolating rigid screw motions instead of matrices, so twisted joints sweep smoothly instead of folding. A compute pass dispatched per horde entity reads each bone's current-frame transform (from ragdoll or procedural IK, whichever M5.2 has active that tick), converts it to a unit dual quaternion, blends per-vertex using `[M5-EXT-19]`'s baked weights, and writes the skinned position straight into the buffer-device-address vertex buffer M1 already declared for skinned meshes — no CPU-side skinning path exists at all.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies crawling through low-clearance gaps, mid-lunge attacks, and compact ragdoll pile-ups no longer show pinched or self-intersecting mesh at the joints. Because skinning runs entirely on the GPU, ~200 simultaneous horde entities skin cleanly every frame without a CPU cost.

#### [M5-EXT-03] Dynamic Logistic Spawn Pacing

> **tags** · general
> **tl;dr** · Declares M5's "Dynamic Logistic Spawn Pacing" bullet, wiring its carrying-capacity term to M5.4's TensionSignal and its output into M1's EntityFactory JSON-d...
> **ctx** · Dynamic Logistic Spawn Pacing -- Declares M5's "Dynamic Logistic Spawn Pacing" bullet, wiring its carrying-capacity term to M5.4's TensionSignal and its output into M1's EntityFactory JSON-driven spawn requests (ECS Native Spawning).
> **meta** · depends-on: M5-EXT-49 · depended-by: -

##### Systems Touched
Declares M5's "Dynamic Logistic Spawn Pacing" bullet, wiring its carrying-capacity term to M5.4's `TensionSignal` and its output into M1's `EntityFactory` JSON-driven spawn requests (ECS Native Spawning).

##### Math
`K(t) = K_base · (1 + κ·TensionSignal)` — carrying capacity rises and falls with the Director's smoothed tension scalar rather than staying fixed.

`N_{t+Δt} = N_t + r·N_t·(1 − N_t/K(t))·Δt`

##### Algorithm
Each Director tick, the spawn system re-evaluates `K(t)` off M5.4's already-computed `TensionSignal` before stepping the logistic growth equation. Low tension caps horde population well below the ~200 target so a quiet sector doesn't stay saturated; a spike in tension (gunfire, a screamer alarm, regional SEIR severity feeding through `[M5-EXT-49]`) raises the ceiling and lets population race toward it. The resulting spawn count is dispatched through M1's `EntityFactory`, which parses each zombie's initial component set from JSON exactly as it does for any other entity.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Horde density visibly tracks how loud and dangerous you've been rather than sitting at a flat number — a sector you've kept quiet stays thinned out, while one you've been firing guns in all night keeps refilling toward a much higher ceiling.

#### [M5-EXT-04] Perimeter Illumination Repulsion

> **tags** · general
> **tl;dr** · Declares M5's "Perimeter Illumination Repulsion" bullet: a lightweight LightSource registry (position, radius, intensity) queried through M1's SpatialHash, f...
> **ctx** · Perimeter Illumination Repulsion -- Declares M5's "Perimeter Illumination Repulsion" bullet: a lightweight LightSource registry (position, radius, intensity) queried through M1's SpatialHash, feeding an additive cost weight into [M5-EXT-01]'s async navmesh baker so lit tiles read as less desirable to path through.
> **meta** · depends-on: M5-EXT-01 · depended-by: -

##### Systems Touched
Declares M5's "Perimeter Illumination Repulsion" bullet: a lightweight `LightSource` registry (position, radius, intensity) queried through M1's `SpatialHash`, feeding an additive cost weight into `[M5-EXT-01]`'s async navmesh baker so lit tiles read as less desirable to path through.

##### Math
`W_light(tile) = Σ_{s ∈ Lights(tile)} β · (I_s / (1 + d_s²))` — summed, inverse-square-falloff repulsion from every registered light source within query radius of the tile.

##### Algorithm
Every placed or portable light source (torches, floodlights, vehicle headlights) registers into a small entity set queried the same way any other `SpatialHash` consumer does. When `[M5-EXT-01]`'s tile baker runs (on structural change or, for lights, whenever a source is added/removed/toggled), it sums the inverse-falloff weight from nearby lights into that tile's traversal cost before re-stitching it to neighbors. Zombies still path through if that's the only route, but the cost bias makes them favor darker approach lines when one exists — no separate zombie-AI-side light query is needed, since the weight is baked straight into the nav data they already read.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A well-lit barricade perimeter genuinely discourages wandering hordes from drifting through it, rewarding deliberate light discipline at a camp. Shoot out a floodlight or let a generator die, and the repulsion drops out live through the same dirty-tile re-bake `[M5-EXT-01]` already runs for structural changes — the perimeter goes dark and the paths shift immediately.

#### [M5-EXT-05] Hydrostatic Velocity Penalties

> **tags** · general
> **tl;dr** · Declares M5's "Hydrostatic Velocity Penalties" bullet by reusing the hydrostatic-pressure term from M9's vehicle wading model (P_hydrostatic = ρ_water · g · ...
> **ctx** · Hydrostatic Velocity Penalties -- Declares M5's "Hydrostatic Velocity Penalties" bullet by reusing the hydrostatic-pressure term from M9's vehicle wading model (P_hydrostatic = ρ_water · g · h_water), sampling M10's Saint-Venant flood depth field, and applying it to zombie locomotion speed instead of engine torque.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Declares M5's "Hydrostatic Velocity Penalties" bullet by reusing the hydrostatic-pressure term from M9's vehicle wading model (`P_hydrostatic = ρ_water · g · h_water`), sampling M10's Saint-Venant flood depth field, and applying it to zombie locomotion speed instead of engine torque.

##### Math
`P_hydrostatic = ρ_water · g · h_water` *(same term M9 already computes for vehicle wading — sample it rather than re-deriving)*

`v_effective = v_locomotion · Clamp(1.0 − P_hydrostatic/P_ref, 0.2, 1.0)`

##### Algorithm
M9 already converts local flood depth into a hydrostatic pressure term each tick to throttle vehicle engine torque. Zombie locomotion reads that same computed pressure value — it does not stand up a second water-physics system — and applies it as a velocity clamp instead of a torque efficiency curve, floored so a horde wading through knee-deep runoff slows down but never fully stalls the way a stalling vehicle engine can.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Hordes wading through flooded streets after a heavy Saint-Venant flood event visibly bog down, giving you a genuine tactical option to funnel or outrun pursuers through standing water — without the engine needing a zombie-specific water system, since it's riding the same hydrostatic term M9 already pays for.

#### [M5-EXT-06] AI Behavior / Utility System

> **tags** · AI
> **tl;dr** · M5.1. No AI decision system; zombies have no behavior.
> **ctx** · AI Behavior / Utility System -- M5.1. No AI decision system; zombies have no behavior.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.1. No AI decision system; zombies have no behavior.

##### Math
-

##### Algorithm
Data-driven utility/behavior tree; SLM generates weight tables (M13).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies act believably.

#### [M5-EXT-07] Navigation / Pathfinding

> **tags** · general
> **tl;dr** · M5. grep Recast|Detour|navmesh → 0 in code. Zombies cannot move toward player.
> **ctx** · Navigation / Pathfinding -- M5. grep Recast|Detour|navmesh → 0 in code. Zombies cannot move toward player.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5. `grep Recast|Detour|navmesh` → 0 in code. Zombies cannot move toward player.

##### Math
-

##### Algorithm
Recast builds navmesh; Detour queries paths; tiled for streaming.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies path to the player.

#### [M5-EXT-08] Procedural Animation (physics/IK)

> **tags** · IK
> **tl;dr** · M5.2. grep Animation|IK → 0. Zombies static without it.
> **ctx** · Procedural Animation (physics/IK) -- M5.2. grep Animation|IK → 0. Zombies static without it.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.2. `grep Animation|IK` → 0. Zombies static without it.

##### Math
-

##### Algorithm
Physics-driven limb IK; SLM generates blend weights (M13).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Organic zombie movement.

#### [M5-EXT-09] AI Perception System

> **tags** · AI, perception, senses
> **tl;dr** · Zombies perceive via directional sight cone + directional hearing cone + blood scent gradient; alertness drives state transitions.
> **ctx** · AI Perception System -- M5.3. Replaces the prior stub ("No sight/hearing/visibility model") with a real multi-modal perception model. Queries `RayBatchQuery` (M1-002) + spatial hash (M1-001) for stimulus, emits a unified `PerceptionStimulus` to the FSM (M5-EXT-13).
> **meta** · depends-on: [M1-002], [M1-001], [M5-EXT-13], [M5-EXT-14], [M5-EXT-18], [M2-EXT-80], [M10-EXT-23] · depended-by: [M5-EXT-13], [M5-EXT-15]

##### Systems Touched
M5.3 perception pipeline: sight (cone + LOS), hearing (directional cone + freq attenuation), smell (blood scent field, M5-EXT-18), alertness state machine. Feeds FSM (M5-EXT-13) + short-term memory (M5-EXT-15).

##### Math
`detect = max(sightScore, hearScore, smellScore)`
- `sightScore = (dist < R_sight && angleToPlayer < FOV/2 && LOSclear) ? (1 - dist/R_sight) * nightVisMult : 0`
- `hearScore  = (dist < hearR && |angleToSound| < hearCone/2) ? (1 - dist/hearR) * freqWeight : 0`
- `smellScore = scentFieldSample(pos) * windAlignment` (M5-EXT-18)
- `nightVisMult = lerp(0.4, 1.0, daylight)` (M10-EXT-23 day/night)

##### Algorithm
Per tick, each zombie polls the three senses. Sight uses a forward FOV cone (~110°) with `RayBatchQuery` LOS validation. Hearing is a forward-biased cone (~160°) — sounds behind the zombie incur a 0.5× range penalty. Smell samples the blood-scent diffusion field (M5-EXT-18) weighted by wind alignment. The max of the three scores vs the zombie's `alertThreshold` sets its alertness: Unaware → Suspicious (investigate last stimulus) → Alert (confirmed, commit to chase). Alertness decays over time when no stimulus is present.

##### Examples
- Player sprints past a zombie's front at 6 m → sightScore high → Alert → Chase.
- Player crouch-walks behind a zombie at 4 m on grass → hearScore low (back-facing + quiet) → stays Unaware.
- Player with open wound stands downwind of a pack → smellScore ramps → pack converges up-gradient.

##### Failure Modes
- LOS false-negative through thin foliage → zombie fails to see player (acceptable, adds stealth depth).
- Scent field stale after rain wash → smellScore drops to 0 (intended counterplay).
- Alertness decay too slow → zombies "remember" too long → tune `alertDecay` per archetype.

##### Player-Facing Impact
Zombies are not omniscient. They see, hear, and smell you — and you can exploit each. Darkness, quiet movement, and wind direction are survival tools.

#### [M5-EXT-10] AI Director (horde pacing)

> **tags** · AI
> **tl;dr** · Config-driven density curve; SLM telemetry-Director tunes it (M13 + M7 telemetry).
> **ctx** · AI Director (horde pacing) -- M5.4. No spawn/difficulty director.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.4. No spawn/difficulty director.

##### Math
-

##### Algorithm
Config-driven density curve; SLM telemetry-Director tunes it (M13 + M7 telemetry).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Pacing feels intentional, not random.

#### [M5-EXT-11] Horde Emergence / Spawn Waves

> **tags** · general
> **tl;dr** · Director emits spawn waves from pressure + telemetry.
> **ctx** · Horde Emergence / Spawn Waves -- M5.4. No emergent wave/spawn logic.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.4. No emergent wave/spawn logic.

##### Math
-

##### Algorithm
Director emits spawn waves from pressure + telemetry.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies arrive in waves.

#### [M5-EXT-12] Zombie Facing / Emerge Animation

> **tags** · general
> **tl;dr** · M2/M5.2. Zombies must face player + emerge.
> **ctx** · Zombie Facing / Emerge Animation -- M2/M5.2. Zombies must face player + emerge.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M2/M5.2. Zombies must face player + emerge.

##### Math
-

##### Algorithm
Orient to player; play emerge anim on spawn.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies face + climb out.

#### [M5-EXT-13] Zombie FSM States

> **tags** · FSM, AI
> **tl;dr** · Seven-state zombie behavior machine: Dormant → Idle → Wander → Alert → Chase → Attack → Flee, driven by perception (M5-EXT-09).
> **ctx** · Zombie FSM States -- M5. Replaces the prior stub ("States idle/wander/chase/attack not defined") with a full state machine. Dormant state (M5-EXT-17) added so buildings are unpredictable.
> **meta** · depends-on: [M5-EXT-09], [M5-EXT-14], [M5-EXT-15], [M5-EXT-17] · depended-by: [M5-EXT-10], [M5-EXT-11]

##### Systems Touched
M5 zombie behavior core. State transitions gate animation (M5-EXT-12), pathing (M5-EXT-07), and attack resolution (M2.9). Director (M5-EXT-10) reads aggregate state counts for pacing.

##### Math
`transition = f(perceptionScore, memoryTimer, hp, stimulusAge)`
- `Dormant → Idle` on wake trigger (sound > wakeThresh, proximity < 2 m, light on face, damage)
- `Idle → Wander` after idleTimer expires
- `Wander/Idle → Alert` on perceptionScore > suspiciousThresh
- `Alert → Chase` on perceptionScore > alertThresh (confirmed)
- `Chase → Attack` on `dist(player) < meleeRange`
- `Any → Flee` on `hp < fleeHp` or fire/severe-stimulus (rare)

##### Algorithm
Explicit FSM: `switch(state)`. Each state has entry/exit hooks. Dormant zombies (M5-EXT-17) are inactive until a wake trigger fires. Alert state routes the zombie to last-known-player-pos (M5-EXT-15) then escalates to Chase if re-confirmed. Attack uses M2.9 damage event. Flee is rare (panic on fire, near-death) and returns to Wander after the stimulus clears.

##### Examples
- Zombie dormant in a house; player kicks the door → wakeThresh exceeded → Dormant → Idle → Alert (door sound) → Chase.
- Zombie Wanders; player crouches behind at 5 m → perceptionScore < suspiciousThresh → stays Wander.
- Player shoots zombie from 20 m → hp < fleeHp → Flee (staggers away, then Wander).

##### Failure Modes
- State thrash (Alert↔Wander) when perceptionScore sits on threshold → add hysteresis band.
- Dormant zombie wakes mid-stealth-takedown → takedown interrupted → require approach from behind + hold-breath window (M5-EXT-19).
- Flee pathing into a wall → reuse M5-EXT-07 navmesh fallback.

##### Player-Facing Impact
Predictable, debuggable AI with real menace. A "dead" building can wake up. A cornered zombie can run. The world reacts, not just responds.


### Cluster B — input / determinism / save / platform

#### [M5-EXT-14] Hearing Perception (directional cone + freq)

> **tags** · AI, perception, audio
> **tl;dr** · Sound events propagate as attenuated spheres; zombies hear via a forward-biased hearing cone with frequency-weighted range. Back-facing sounds penalized.
> **ctx** · Hearing Perception (directional cone + freq) -- M5.3. Replaces the prior stub ("No hearing model") with a directional, frequency-aware model. Consumed by M5-EXT-09 perception score; produced by M6 audio events.
> **meta** · depends-on: [M5-EXT-09], [M6-EXT-01] · depended-by: [M5-EXT-09], [M5-EXT-13]

##### Systems Touched
M5.3 hearing model. Sound events originate from M6 (gunshots, footsteps, doors, collapses) as `SoundEvent{pos, freq, amplitude}`. Zombie queries its hearing cone + range vs the event.

##### Math
`hearScore = (dist < hearR(freq) && |angleToSound| < hearCone/2) ? (1 - dist/hearR) * freqWeight : 0`
- `hearR(freq) = baseR * freqWeight`, `freqWeight`: whisper 0.3, footsteps 0.7, gunshot 2.5, explosion 5.0 (map-wide)
- Back-facing penalty: if `|angle| > 90°`, `hearR *= 0.5`
- Per-archetype `baseR`: shambler 12 m, runner 18 m, sleeper (M5-EXT-17) 6 m until woken

##### Algorithm
On `SoundEvent` emit, every zombie within `hearR(freq)` of the source computes `hearScore`. Front-facing events score higher; rear events are halved. Score feeds M5-EXT-09's `detect`. Explosions set a map-wide alert that elevates nearby zombies to Alert state. Door opens / building collapses produce structural-event sounds that wake dormant zombies (M5-EXT-17).

##### Examples
- Player fires a pistol 15 m in front of a shambler (baseR 12, freqWeight 2.5 → hearR 30) → heard → Alert.
- Player whispers 3 m behind a runner (baseR 18, freqWeight 0.3 → hearR 5.4, back-facing ×0.5 = 2.7) → not heard.
- Building collapse 40 m away → structural event → wakes all dormant zombies in radius.

##### Failure Modes
- Sound spam (every footstep) → perf hit with 200 zombies → throttle footstep events to 4 Hz per source.
- Cone math drift at zombie spawn rotation → normalize angle to [-180,180] before compare.
- Explosion alert too broad → cap alert radius, falloff with distance.

##### Player-Facing Impact
Sound is a tactical resource AND a liability. A slammed door wakes the house. A suppressed shot at range goes unheard. Movement discipline keeps you alive.

#### [M5-EXT-15] Short-Term Memory & Investigation

> **tags** · general
> **tl;dr** · Zombie stores last-known-player-pos for N seconds; searches there then gives up
> **ctx** · Short-Term Memory & Investigation -- M5.3. No memory of last-known position.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.3. No memory of last-known position.

##### Math
-

##### Algorithm
Zombie stores last-known-player-pos for N seconds; searches there then gives up. `memory.timer -= dt;`

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Less robotic, more believable.

#### [M5-EXT-16] Horde Merge & Split Behavior

> **tags** · general
> **tl;dr** · Nearby zombies form a horde (shared target); horde splits when targets diverge
> **ctx** · Horde Merge & Split Behavior -- M5.4. No horde cohesion logic.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.4. No horde cohesion logic.

##### Math
-

##### Algorithm
Nearby zombies form a horde (shared target); horde splits when targets diverge. `if (Dist(a,b)<mergeR) shareTarget(a,b);`

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Emergent swarm behavior.

#### [M5-EXT-17] Aerodynamic Wake Slipstream Horde Slingshot

> **tags** · general
> **tl;dr** · Links M9 vehicle chassis velocity tracking fields straight to M5.1 Reynolds boid steering arrays.
> **ctx** · Aerodynamic Wake Slipstream Horde Slingshot -- Links M9 vehicle chassis velocity tracking fields straight to M5.1 Reynolds boid steering arrays.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Links M9 vehicle chassis velocity tracking fields straight to M5.1 Reynolds boid steering arrays.

##### Math
A speeding vehicle chassis shears the atmosphere, creating a low-pressure wake zone behind its path. Aggressive zombie phenotypes entering this tracking cone encounter virtually zero aerodynamic resistance, dropping their drag coefficients (C_d ) exponentially:

`C_d(x) = C_d0 · (1.0 − e^(−x/λ_wake))`

##### Algorithm
Each tick, boids inside M9's tracked wake cone have their drag coefficient queried against distance into the cone via the exponential falloff, letting fast zombies inside the cone match the vehicle's closing speed.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Speeding down highways doesn't let you effortlessly outrun threats. High-tier running zombies can actively catch your vehicle's slipstream, entering the low-pressure pocket behind your rear bumper. They gain a high-velocity drafting boost that lets them latch onto your tailgate or swarm your open truck beds like drafting racing cars.

#### [M5-EXT-18] Player Character Generation (Uninfected-Mode Phenotype Pipeline)

> **tags** · general
> **tl;dr** · Runs the existing M5.1 zombie phenotype/bio-weight generator with the infection-mutation pass disabled — one canonical procedural humanoid generator for play...
> **ctx** · Player Character Generation (Uninfected-Mode Phenotype Pipeline) -- Runs the existing M5.1 zombie phenotype/bio-weight generator with the infection-mutation pass disabled — one canonical procedural humanoid generator for players, survivors, and zombies, not a separate hand-authored player model.
> **meta** · depends-on: - · depended-by: M5-EXT-21

##### Systems Touched
Runs the existing M5.1 zombie phenotype/bio-weight generator with the infection-mutation pass disabled — one canonical procedural humanoid generator for players, survivors, and zombies, not a separate hand-authored player model.

##### Math
No new math — identical `ZombieTraits` deterministic trait distribution already used for zombies, evaluated with `InfectionState = Uninfected` so the mutation terms zero out.

##### Algorithm
The same trait-distribution pipeline already generating every zombie in the world runs once at character-creation time with the mutation/decay passes gated off by an `InfectionState` flag — character creation is choosing which slice of an existing procedural space to sample from, not a second content pipeline.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Player character appearance is procedurally generated with the same richness and variation as the zombies and survivors around them — genuinely unique-looking characters with zero hand-authored art, consistent with everything else in this engine.

#### [M5-EXT-19] Topology-Invariant Master Rig with Deformation-Transfer Binding

> **tags** · general
> **tl;dr** · Upgrades M5.1 zombie variation routines and feeds bind data into M5.2 procedural animation and M5.2's existing Dual-Quaternion compute skinning.
> **ctx** · Topology-Invariant Master Rig with Deformation-Transfer Binding -- Upgrades M5.1 zombie variation routines and feeds bind data into M5.2 procedural animation and M5.2's existing Dual-Quaternion compute skinning.
> **meta** · depends-on: - · depended-by: M5-EXT-02, M5-EXT-20, M5-EXT-21

##### Systems Touched
Upgrades M5.1 zombie variation routines and feeds bind data into M5.2 procedural animation and M5.2's existing Dual-Quaternion compute skinning.

##### Math
Vertex skinning weights fall off with distance from a bone's influence line, but are now sampled once against the fixed master topology rather than recomputed per instance:

`Weight = max(0, 1 − Distance_to_bone / Radius_of_influence)²`

Per-vertex bone weights are NOT computed at runtime via raw line-proximity against each phenotype's bone placement — at extreme joint configurations (deep lunges, low-clearance crawling, compact ragdoll pile-ups) that produces "paper-folding" volume collapse and self-intersecting mesh, since a line-distance-only solve has no knowledge of surrounding topology. Bone placement is still generated per-phenotype/seed-driven at runtime, but weights are not recomputed from scratch each time — see the heat-diffusion approach below.

##### Algorithm
A single, structurally perfect Master Skeleton is authored once with hand-painted, high-quality base skinning weights covering every joint fold case (deep bends, compact poses, etc.). When a phenotype seed generates a specific mutant or zombie scale, the system does not re-derive skinning weights — it programmatically adjusts the *lengths and offsets* of the Master Skeleton's bone nodes to match the generated SDF volume, then transfers the resulting mesh deformation via Skeletal Morph Targets / Deformation Transfer matrices computed against the master's known-good weight painting. Because the topology and weight painting never change — only bone length/offset — extreme poses fold cleanly across every generated body variation instead of collapsing.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Hordes display rich anatomical variety — distorted mutations, heavily built behemoths, and lanky, emaciated infected all move, crawl, and ragdoll with accurate joint behavior and zero texture stretching or paper-folding, even in extreme compact poses.

#### [M5-EXT-20] Anatomical Hit-Volume SDF Gradients

> **tags** · SDF
> **tl;dr** · Declares the base "Anatomical Hit-Volume SDF Gradients" bullet. Consumes [M5-EXT-19]'s Master Skeleton pose data (skinned vertex positions feed the SDF's zer...
> **ctx** · Anatomical Hit-Volume SDF Gradients -- Declares the base "Anatomical Hit-Volume SDF Gradients" bullet. Consumes [M5-EXT-19]'s Master Skeleton pose data (skinned vertex positions feed the SDF's zero-crossing surface) and feeds M2.9's ballistic damage resolution — hit location and penetration depth are read off the gradient rather than a raw mesh raycast.
> **meta** · depends-on: M5-EXT-19 · depended-by: -

##### Systems Touched
Declares the base "Anatomical Hit-Volume SDF Gradients" bullet. Consumes `[M5-EXT-19]`'s Master Skeleton pose data (skinned vertex positions feed the SDF's zero-crossing surface) and feeds M2.9's ballistic damage resolution — hit location and penetration depth are read off the gradient rather than a raw mesh raycast.

##### Math
Each anatomical region (skull, torso, limb) is represented as a signed distance field; a hit's damage multiplier and exit-wound depth come from the gradient at the impact point: `Depth = |Φ(p)| / |∇Φ(p)|`, where `Φ` is the SDF value and `∇Φ` its normalized gradient direction.

##### Algorithm
Rather than raycasting the full skinned mesh per shot (expensive at horde density), each zombie carries a small set of primitive SDF volumes (capsules/ellipsoids) rigged to the Master Skeleton's bones, updated once per animation tick alongside `[M5-EXT-19]`'s pose. A hit tests against this lightweight SDF stack instead of triangle geometry: the gradient at the query point gives both the surface normal (for spatter direction) and penetration depth (for through-and-through vs. stopping-power resolution) in one evaluation.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Headshots and limb hits register consistently even mid-animation (staggering, lunging, ragdolling) without expensive per-triangle raycasts, and bullet penetration depth reads correctly against actual body mass rather than a flat hitbox.

#### [M5-EXT-21] Heat-Diffusion Automatic Rigging & PSOLA Phenotype Vocalization

> **tags** · PSOLA
> **tl;dr** · Declares the base "Heat-Diffusion Automatic Rigging. Phenotype Vocalization Shift via PSOLA" bullet. Rigging half feeds [M5-EXT-19]'s Master Skeleton binding...
> **ctx** · Heat-Diffusion Automatic Rigging & PSOLA Phenotype Vocalization -- Declares the base "Heat-Diffusion Automatic Rigging. Phenotype Vocalization Shift via PSOLA" bullet. Rigging half feeds [M5-EXT-19]'s Master Skeleton binding; vocalization half feeds M6's audio propagation as the per-zombie emitter source, sharing PSOLA machinery with M6's [M6-EXT] voice pipeline rather than duplicating a pitch-shift implementation.
> **meta** · depends-on: M5-EXT-19, M5-EXT-18 · depended-by: -

##### Systems Touched
Declares the base "Heat-Diffusion Automatic Rigging. Phenotype Vocalization Shift via PSOLA" bullet. Rigging half feeds `[M5-EXT-19]`'s Master Skeleton binding; vocalization half feeds M6's audio propagation as the per-zombie emitter source, sharing PSOLA machinery with M6's `[M6-EXT]` voice pipeline rather than duplicating a pitch-shift implementation.

##### Math
Skin weight at each mesh vertex is the steady-state solution of the heat equation seeded at each bone's joint position, solved to convergence rather than approximated by inverse distance: `∂h/∂t = κ∇²h`, with each joint held as a fixed-temperature boundary condition (`h = 1` at its own seed point, `h = 0` at all other seeds) and the converged `h` per joint, per vertex, normalized across joints to give that vertex's bone weights. Vocalization pitch/formant shift is a PSOLA resampling of the base clip's pitch marks by a ratio derived from the phenotype's vocal-tract-length scalar `L`: `PitchRatio = L_reference / L_phenotype`.

##### Algorithm
Automatic rigging: bone influence weights for each generated phenotype are solved by heat diffusion across the mesh surface from seed points at each joint — heat "flows" from a bone's seed point outward, and the resulting steady-state temperature at each vertex becomes its skin weight, which handles irregular/mutated topology (extra mass, missing limbs) more robustly than distance-based weighting. Vocalization: each phenotype's base groan/scream audio clip has its pitch and formants shifted via Pitch-Synchronous Overlap-Add (PSOLA) according to the phenotype's mass and vocal-tract-length parameters from `[M5-EXT-18]`'s generation pipeline, so a heavy zombie sounds bass-shifted and a child-scaled infected sounds pitched up from the same source clips instead of needing per-phenotype voice recordings.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Every generated phenotype — however unusual its proportions — gets correctly weighted deformation and a vocalization that matches its size, without hand-rigging or recording a unique voice per body type.

#### [M5-EXT-22] Screamer Convergence & Holling Type II Cannibalism Feeding

> **tags** · II
> **tl;dr** · Screamer Convergence & Holling Type II Cannibalism Feeding
> **ctx** · Screamer Convergence & Holling Type II Cannibalism Feeding
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Horde feeding on survivors ([M8] SEIR infected), scent/fear grid ([M5] diffusion), animation. Depends on [M1] ECS.

##### Math
Holling Type II functional response: `f(N)=a·N/(1+a·h·N)` — feeding rate saturates as prey density N rises (handling time h). Cannibalism convergence: screamers flock to max scent-gradient cell `g=∇S`; convergence `dx/dt = α·g - β·(x - x_flock)`.

##### Algorithm
1. Sample local prey density N. 2. Feeding rate = Holling f(N) (saturation, not linear). 3. Screamers steer along scent gradient ∇S. 4. On kill, emit scent spike → more converge (cannibalism cascade).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
The swarm FEELS intelligent: it surges to a kill, then saturates — never infinitely fast. Avoids the 'zombies teleport to you' failure; feeding has real cost and rhythm.

#### [M5-EXT-23] Optimal Reciprocal Collision Avoidance (ORCA) Solver

> **tags** · ORCA
> **tl;dr** · M5.1/M5.3 + M5.4. Local collision-avoidance velocity for agents (companions, animals, crowds).
> **ctx** · Optimal Reciprocal Collision Avoidance (ORCA) Solver -- M5.1/M5.3 + M5.4. Local collision-avoidance velocity for agents (companions, animals, crowds).
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.1/M5.3 + M5.4. Local collision-avoidance velocity for agents (companions, animals, crowds).

##### Math
v = ORCA(agent, neighbors, goal); each agent picks velocity in its free half-plane, reciprocal.

##### Algorithm
Each dynamic agent computes a velocity avoiding collision with neighbors while moving to its goal, using the ORCA reciprocal formulation (neighbors do the same, so avoidance is shared). Drives companion/animal/crowd navigation.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Allies/animals/crowds navigate around each other smoothly - no overlap or jitter.

#### [M5-EXT-24] Atmosphere Volumetric Scattering Pass

> **tags** · general
> **tl;dr** · Extends M5.1 atmosphere; scatters light through zone-tagged (M4-EXT-63) air; M13-EXT-53 lore ambiance.
> **ctx** · Atmosphere Volumetric Scattering Pass -- Extends M5.1 atmosphere; scatters light through zone-tagged (M4-EXT-63) air; M13-EXT-53 lore ambiance.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Extends M5.1 atmosphere; scatters light through zone-tagged (M4-EXT-63) air; M13-EXT-53 lore ambiance.

##### Math
inscatter = ∫_0^D σ_s · phase(θ) · transmittance(s) ds (perceptual approximation).

##### Algorithm
A low-cost volumetric pass integrates in-scatter along view rays using the current sky (M5.1) and zone pollution (M4-EXT-63) to tint haze; dust/fog zones read denser.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Cities haze differently than forests — the air itself tells you where you are.

#### [M5-EXT-25] Procedural Joint Compliance Degradation (Dynamic Muscle Crippling)

> **tags** · general
> **tl;dr** · Expands M5.2 (Procedural Animation / Active Ragdolls) and interfaces with M2.9 Ballistics.
> **ctx** · Procedural Joint Compliance Degradation (Dynamic Muscle Crippling) -- Expands M5.2 (Procedural Animation / Active Ragdolls) and interfaces with M2.9 Ballistics.
> **meta** · depends-on: - · depended-by: M5-EXT-38

##### Systems Touched
Expands M5.2 (Procedural Animation / Active Ragdolls) and interfaces with M2.9 Ballistics.

##### Math
The Proportional-Derivative (PD) motor torque equations governing your active ragdoll skeleton joints are modified at runtime based on localized bone kinetic energy dissipation tensors:

`T_motor = K_p·(θ_target − θ_current) + K_d·(ω_target − ω_current)`

When a specific limb segment sustains an entry from a non-penetrating ballistic hit, instead of applying a static lurch animation, the engine scales down the joint tracking gains exponentially over accumulated tissue damage metrics:

`K_p ←K_p ⋅e^(−α⋅∑D_limb)`

`,K_d ←K_d ⋅e^(−α⋅∑D_limb)`

Explicit PD (evaluating force from the current frame's error only) is unstable on a heavy phenotype combined with a large `Δt` (frame hitch) or stiff `K_p` — it can overshoot and oscillate without bound. Reference Implementation below uses **Stable PD** (implicit) instead: it solves for next-step velocity before applying force, damping high-gain resonance regardless of frame-time — the technique from Tan, Liu & Turk's "Stable Proportional-Derivative Controllers" (2011), standard for ragdoll/character joint control.

##### Algorithm
This cleanly replaces hardcoded injury animations with genuine physics-driven impairment. Your active ragdolls continually try to match their procedural locomotion target poses. By dropping the motor stiffness (K_p ) and damping (K_d ) on a heavily damaged joint, the limb physically gives out under the zombie's own phenotype mass scale.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies don't just have a generic "limp" toggle. If you sweep a low-caliber weapon or a blunt melee object into a heavy zombie's right knee joint, that specific Jolt constraint goes soft. The zombie's leg will physically buck under its weight, lowering its hip center-of-mass, causing it to drag the limp leg realistically across dynamic rubble fields, or tumble forward completely if it tries to turn a sharp corner at high velocity.

#### [M5-EXT-26] Zombie Tissue Rigor Mortis Lifecycle Decay

> **tags** · general
> **tl;dr** · Intersects M5.1 zombie bio phenotypes with M5.2 Active Ragdoll joint tracking loops.
> **ctx** · Zombie Tissue Rigor Mortis Lifecycle Decay -- Intersects M5.1 zombie bio phenotypes with M5.2 Active Ragdoll joint tracking loops.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Intersects M5.1 zombie bio phenotypes with M5.2 Active Ragdoll joint tracking loops.

##### Math
The muscle flexibility index (χ) of a zombie transitions over biological timeline states (t_age ). Fresh infections display standard flexibility; recently turned corpses enter a high-stiffness phase, which eventually degrades into a flaccid, loose decay state:

`χ(t_age) = Clamp(e^(−α·t) + 2.5·(1.0−e^(−β·t))·e^(−γ·t), 0.05, 1.5)`

This flexibility coefficient scales the base motor stiffness variables (K_p) across all skeletal joint elements inside the active ragdoll controllers:

`K_p = K_p0 · χ(t_age)`

##### Algorithm
Your zombie entities hold timestamp values inside their EnTT component sheets tracking their generation time. During your active ragdoll physics loop updates, the joint tracking motor gains sample this age-dependent modifier, shifting constraint limits dynamically without introducing specialized procedural animation code branches.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies generated from freshly killed survivors move with fluid dexterity. Hordes that have spent hours wandering in cold sectors enter a stiff, robotic rigor mortis phase, causing their walks to appear mechanical and making them clumsy over dynamic debris. Deeply decayed zombies display loose, hyper-extended joint reactions that cause their bodies to buck wildly under bullet impacts.

#### [M5-EXT-27] Biomechanical Joint Luxation Limits

> **tags** · general
> **tl;dr** · Deepens M5.2 (Active Ragdolls) and interfaces with Jolt constraint friction callbacks.
> **ctx** · Biomechanical Joint Luxation Limits -- Deepens M5.2 (Active Ragdolls) and interfaces with Jolt constraint friction callbacks.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Deepens M5.2 (Active Ragdolls) and interfaces with Jolt constraint friction callbacks.

##### Math
Instead of skeletal constraints possessing binary breaks, joint limits experience structural luxation (dislocation) when the torque loads (τ_joint) surpass a dynamic tendon structural yield limit (T_yield):

`If ∥τ_joint∥ > T_yield ⟹ θ_limit_max = θ_limit_max + γ·(∥τ_joint∥ − T_yield)`

This structural displacement warps your target physics limits permanently, inducing a persistent mechanical structural offset in the ragdoll assembly.

##### Algorithm
Your active ragdoll limits are driven by Jolt's native joint constraint systems. This field listens to the impulse manifold data returned by the physics step. If an impact load crosses the structural boundary, the script uses SetLimits directly on the Jolt constraint handles to warp the joint bounds inline, bypassing the need to destroy or detach the entire joint assembly.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Dropping a heavy zombie off a multi-story building ledge onto concrete doesn't always break the leg off entirely. If the forces hit a specific threshold, the hip or knee joint experiences realistic dislocation. The active ragdoll constraint warps permanently, causing the limb to drag backward at an unnatural angle while the entity struggles to balance its weight during locomotion loops.

#### [M5-EXT-28] Centrifugal Kinetic Limb Flail

> **tags** · general
> **tl;dr** · Expands M5.2 active ragdoll constraints using Jolt contact impulse manifolds.
> **ctx** · Centrifugal Kinetic Limb Flail -- Expands M5.2 active ragdoll constraints using Jolt contact impulse manifolds.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Expands M5.2 active ragdoll constraints using Jolt contact impulse manifolds.

##### Math
When a zombie's joint compliance drops or experiences partial structural failure, the limb is not lazily deleted. It acts as an unconstrained chaotic double pendulum. The rotational asymmetry generates a dynamic centrifugal angular torque vector (τ_flail ) during locomotion swings:

`τ_flail =m_limb ⋅(ω_torso ×(ω_torso ×r_limb ))`

##### Algorithm
When a limb's joint compliance is loosened/broken, it stops following the animated/ragdoll blend and free-swings as an unconstrained double-pendulum segment. Torso rotation drives a centrifugal torque that whips the limb outward; contact impulses apply through the existing Jolt manifold like any rigid-body hit.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Damaging a heavy zombie's arm or shoulder joint causes the limb to swing wildly like a heavy canvas sack as it charges forward. This chaotic kinetic momentum turns the limb into a physical flail that accidentally smashes into and trips neighboring zombies in the crowd, initiating chain-reaction stumble cascades across the herd.

#### [M5-EXT-29] Control-Rig-Style Procedural Pose Override Layer

> **tags** · general
> **tl;dr** · Sits downstream of caps.annMotionMatching (M5.2 above) in the per-frame pose pipeline. Intended chain: Motion Matching → Upper-Body Override ([K-EXT-01], App...
> **ctx** · Control-Rig-Style Procedural Pose Override Layer -- Sits downstream of caps.annMotionMatching (M5.2 above) in the per-frame pose pipeline. Intended chain: Motion Matching → Upper-Body Override ([K-EXT-01], Appendix K) → Procedural Rig (this layer) → Physics Post-Process → Final Pose. This layer consumes Motion Matching's output pose directly, further overridden by [K-EXT-01]'s spine-root blend before it reaches this rig layer.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Sits downstream of `caps.annMotionMatching` (M5.2 above) in the per-frame pose pipeline. Intended chain: Motion Matching → Upper-Body Override (`[K-EXT-01]`, Appendix K) → Procedural Rig (this layer) → Physics Post-Process → Final Pose. This layer consumes Motion Matching's output pose directly, further overridden by `[K-EXT-01]`'s spine-root blend before it reaches this rig layer.

##### Math
A per-joint override is a direct pose substitution: `pose_joint = Lerp(pose_MM, pose_procedural, weight)`, weight supplied per-joint by the Constraint-Weight Blending layer (M5.2-EXT-06).

##### Algorithm
A new post-solve stage that runs after Motion Matching produces its base pose and before the existing Active Ragdoll / PD-motor layer (M5.2 above) takes over for physically-driven joints. Bone transforms coming out of Motion Matching can be overridden per-joint by procedural solvers (aim-offsets, look-at, foot placement corrections) rather than requiring a new baked clip for every situational variant.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies and companions can aim, look, or reach at targets that Motion Matching's clip library was never authored for, without a visible pop back to a baked animation.

#### [M5-EXT-30] Constraint-Weight Blending (Animated ↔ Procedural)

> **tags** · general
> **tl;dr** · Per-joint blend weight consumed by the procedural override layer (M5.2-EXT-05) above.
> **ctx** · Constraint-Weight Blending (Animated ↔ Procedural) -- Per-joint blend weight consumed by the procedural override layer (M5.2-EXT-05) above.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Per-joint blend weight consumed by the procedural override layer (M5.2-EXT-05) above.

##### Math
A single scalar per constrained joint drives the blend between the Motion Matching pose and the procedural solve:

`pose_final = pose_animated · (1 − w) + pose_procedural · w`, `w ∈ [0, 1]`

`w = 0` is pure Motion Matching output; `w = 1` is fully procedural (e.g. a hard IK foot-lock or aim solve). Intermediate values let a solver fade in/out over several frames instead of snapping.

##### Algorithm
Every procedural solver in M5.2-EXT-05 (foot IK, aim IK, look-at, etc.) writes its result as a candidate pose plus a target blend weight rather than overwriting the joint transform outright. Each tick, the weight is stepped toward its target (a simple rate-limited lerp, not an instant snap) and `pose_final` is recomputed from the current animated pose and the procedural candidate. This keeps every procedural override going through one shared blend point instead of each solver having its own ad hoc fade logic.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Procedural corrections (foot placement on stairs, reaching for a door handle) fade in smoothly rather than snapping the limb into place, avoiding the "robotic pop" look of hard IK switches.

#### [M5-EXT-31] Ragdoll-to-Animation Recovery Blend

> **tags** · general
> **tl;dr** · Bridges M5.2's Active Ragdoll Dynamics (existing bullet above) back to Motion Matching once a knocked-down entity is ready to stand.
> **ctx** · Ragdoll-to-Animation Recovery Blend -- Bridges M5.2's Active Ragdoll Dynamics (existing bullet above) back to Motion Matching once a knocked-down entity is ready to stand.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Bridges M5.2's Active Ragdoll Dynamics (existing bullet above) back to Motion Matching once a knocked-down entity is ready to stand.

##### Math
A 10–15 frame crossfade blends the ragdoll's final settled pose into an orientation-matched get-up clip:

`pose_blend(t) = pose_ragdoll · (1 − α(t)) + pose_getup_clip · α(t)`, `α(t) = t / N`, `t ∈ [0, N]`, `N ∈ [10, 15]` frames

The get-up clip is selected/oriented to match the ragdoll's final yaw and prone/supine state so the crossfade doesn't visibly rotate the body mid-blend.

##### Algorithm
Once a ragdolled entity's Jolt bodies settle (velocity below threshold for N consecutive frames), the system snapshots the ragdoll's final per-bone world pose and picks a get-up animation clip matching that orientation (prone-face-down, prone-face-up, or on-side, each with its own clip). The crossfade then runs for a fixed 10–15 frame window, blending bone-by-bone from the snapshotted ragdoll pose toward the clip's pose at each frame. Once `alpha` reaches 1, control hands off fully to Motion Matching and the ragdoll bodies are released from the physics world.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A zombie or companion knocked down by an explosion or vehicle impact doesn't teleport back into a standing idle pose — it settles where physics put it, then rises through a matched get-up animation from that exact position and orientation.

#### [M5-EXT-32] IK Rig Asset (Per-Skeleton Bone-Chain / End-Effector Definition)

> **tags** · IK
> **tl;dr** · Consumed by the procedural override layer (M5.2-EXT-05) and motion-warping (M5.2-EXT-09) below, for any feature that needs to resolve an end-effector target ...
> **ctx** · IK Rig Asset (Per-Skeleton Bone-Chain / End-Effector Definition) -- Consumed by the procedural override layer (M5.2-EXT-05) and motion-warping (M5.2-EXT-09) below, for any feature that needs to resolve an end-effector target (foot, hand, weapon muzzle) back through a bone chain.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Consumed by the procedural override layer (M5.2-EXT-05) and motion-warping (M5.2-EXT-09) below, for any feature that needs to resolve an end-effector target (foot, hand, weapon muzzle) back through a bone chain.

##### Math
No formula — a declarative bone-chain graph per skeleton: `chain = [root_bone, ..., end_effector_bone]` plus a pole-vector hint per chain.

##### Algorithm
A one-time, per-skeleton data asset declaring bone chains (root→end-effector) and pole-vector hints, authored once per unique skeleton (base zombie, companion, phenotype variants sharing a skeleton) rather than re-declared per-feature.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
None directly — this is the authoring/data layer other procedural-animation features above depend on to know which bones form "the left arm" or "the right foot" without hardcoding bone-name strings per skeleton.

#### [M5-EXT-33] Motion-Warping for Target Alignment

> **tags** · general
> **tl;dr** · Consumes the IK Rig asset (M5.2-EXT-08, flagged above) and blends through the procedural override layer (M5.2-EXT-05) for melee-hit alignment, vault-landing ...
> **ctx** · Motion-Warping for Target Alignment -- Consumes the IK Rig asset (M5.2-EXT-08, flagged above) and blends through the procedural override layer (M5.2-EXT-05) for melee-hit alignment, vault-landing alignment (M2.7's vault fan-cast), and door-interaction alignment.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Consumes the IK Rig asset (M5.2-EXT-08, flagged above) and blends through the procedural override layer (M5.2-EXT-05) for melee-hit alignment, vault-landing alignment (M2.7's vault fan-cast), and door-interaction alignment.

##### Math
The root motion offset for a clip is warped toward a target transform over the clip's duration using the same normalized-time blend shape as the recovery crossfade above:

`offset(t) = offset_clip(t) + (target − offset_clip(1)) · β(t)`, `β(t)` a normalized ease-in-ease-out curve over `t ∈ [0, 1]` (clip-relative time), so the warp is zero at the clip start and fully resolved at the clip's designed contact frame.

##### Algorithm
During a vault, melee hit, or door interaction, the resolved contact point becomes the warp target. The root-motion offset blends from its authored path toward that target using an ease curve reaching full alignment at the clip's designed contact frame, so hands/feet land precisely regardless of approach angle.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Melee hits actually connect with the zombie's hitbox instead of swinging through empty air a foot off-target, and vault/door clips land hands and feet precisely on the ledge or handle regardless of the player's exact approach angle.

#### [M5-EXT-34] Physics-Based Secondary Motion Post-Process (Spring-Damper Attachments)

> **tags** · general
> **tl;dr** · Runs as the final stage of the pose pipeline — after Motion Matching, the procedural override layer (M5.2-EXT-05), and ragdoll blending — for non-simulated a...
> **ctx** · Physics-Based Secondary Motion Post-Process (Spring-Damper Attachments) -- Runs as the final stage of the pose pipeline — after Motion Matching, the procedural override layer (M5.2-EXT-05), and ragdoll blending — for non-simulated attachment points (capes, straps, loose gear). Explicitly separate from the existing XPBD Cloth Simulation (M5.2 above), which owns actual cloth meshes; this is a cheap per-point spring for rigid/semi-rigid dangling attachments that don't warrant a full cloth solve.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Runs as the final stage of the pose pipeline — after Motion Matching, the procedural override layer (M5.2-EXT-05), and ragdoll blending — for non-simulated attachment points (capes, straps, loose gear). Explicitly separate from the existing XPBD Cloth Simulation (M5.2 above), which owns actual cloth meshes; this is a cheap per-point spring for rigid/semi-rigid dangling attachments that don't warrant a full cloth solve.

##### Math
A critically-damped spring-damper per attachment point, driven by its parent bone's motion:

`x'' = −k(x − x_target)/m − c·x'`, critically damped at `c = 2√(km)`

where `x` is the attachment's current offset from its rest position, `x_target` is the rest position driven by the parent bone, `k` the spring stiffness, `m` the point mass, and `c` the damping coefficient. Critical damping is chosen specifically so the attachment settles to rest without oscillating past its target — appropriate for gear that should sway and stop, not bounce indefinitely.

##### Algorithm
Each tracked attachment point runs its own critically-damped spring each tick, pulled toward a rest position driven by its parent bone. Critical damping (`c = 2√(km)`) means it settles without overshoot or jitter.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Capes, ammo straps, and loose gear sway and settle naturally with a zombie's or companion's movement instead of rigidly following the parent bone or requiring a full cloth-sim pass for something that small.

#### [M5-EXT-35] Asynchronous Fixed-Block Humanoid Rig Matrix Pool Allocator

> **tags** · general
> **tl;dr** · Backs skeletal bone-transform storage for every humanoid entity (zombies, companions, players) across M5.2's animation pipeline — eliminates heap allocation ...
> **ctx** · Asynchronous Fixed-Block Humanoid Rig Matrix Pool Allocator -- Backs skeletal bone-transform storage for every humanoid entity (zombies, companions, players) across M5.2's animation pipeline — eliminates heap allocation thrashing when hundreds of entities spawn/evict across chunk borders.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Backs skeletal bone-transform storage for every humanoid entity (zombies, companions, players) across M5.2's animation pipeline — eliminates heap allocation thrashing when hundreds of entities spawn/evict across chunk borders.

##### Math
`Stride_aligned = (BoneCount·64 + 63) & ~63` — cache-line-aligned stride sizing.

##### Algorithm
A flat memory arena of fixed-size, cache-aligned rig blocks is constructed once at boot, each holding up to 32 bone matrices. A simple free-list index chain hands out and reclaims blocks in O(1) with zero runtime heap traffic, matching the same alignment idiom `[M0-EXT-01]` and `[M1-EXT-07]` already use elsewhere in the doc for cache-line safety under enkiTS worker contention.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Invisible directly — this is what keeps horde spawn/despawn waves at chunk borders from causing allocator-driven frame hitches.

#### [M5-EXT-36] Skeletal Joint Yield Torque Bone Fracturing Filter

> **tags** · general
> **tl;dr** · Skeletal Joint Yield Torque Bone Fracturing Filter
> **ctx** · Skeletal Joint Yield Torque Bone Fracturing Filter
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Skeleton ragdoll ([M2] Jolt), damage system, animation. Depends on [M2].

##### Math
Yield torque `τ_y = k_y·(θ - θ_0)` (bone stiffness k_y, rest θ_0). Fracture when `|τ| > τ_break` (Wilson et al. bone yield ~ 50-100 N·m for long bones, scaled to game). Filter: only apply if `|τ| > τ_min` (ignore micro-stress).

##### Algorithm
1. Each joint computes τ from Jolt. 2. Below τ_min: ignore. 3. Above τ_break: bone fractures → limb goes limp (ragdoll segment). 4. Between: stiffness reduced (sprain).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Limb damage is legible: a broken leg means the zombie/climber drags, a human limps. Makes combat about disabling, not just HP — a loop deeper than 'subtract health'.

#### [M5-EXT-37] (provisional) IK Rig Metadata Serialization Loader *(SOURCED FROM PLAN FILE)*

> **tags** · IK, SOURCED, PLAN, FILE
> **tl;dr** · Consumed by M5.2-EXT-08 (IK Rig Asset struct) + M5.2-EXT-09 (Motion-Warping target). Feeds every end-effector consumer in M5.2 (melee-hit align, vault landin...
> **ctx** · (provisional) IK Rig Metadata Serialization Loader *(SOURCED FROM PLAN FILE) -- Consumed by M5.2-EXT-08 (IK Rig Asset struct) + M5.2-EXT-09 (Motion-Warping target). Feeds every end-effector consumer in M5.2 (melee-hit align, vault landing, door interact). Distinct from M2.6 glTF/prefab loading (mesh/scene, not bone-chain+pole-vector metadata).
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Consumed by M5.2-EXT-08 (IK Rig Asset struct) + M5.2-EXT-09 (Motion-Warping target). Feeds every end-effector consumer in M5.2 (melee-hit align, vault landing, door interact). Distinct from M2.6 glTF/prefab loading (mesh/scene, not bone-chain+pole-vector metadata).

##### Math
Declarative asset: IKRigAsset{skeletonId, chains:[{boneIdx[], poleVectorHint}]}. Hashing for on-disk table reuses same FNV-1a keying idiom M2.6 uses for geometry cache (uint64_t key=FNV1a(path)).

##### Algorithm
Small JSON/flat-binary asset per skeleton, authored once, loaded at skeleton-registration. Loader parses bone-name->index against skeleton's own joint list (renamed bone fails loudly at load, not silently at runtime), stores resolved IKRigAsset in same hash-keyed table as geometry cache (key space skeletonId string). Phenotype variants reuse one asset; extra chains via override asset.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Skeletons load bone-chain defs from data, not hardcoded bone-name lookups — robust to renamed bones.

#### [M5-EXT-38] XPBD Rope/Tether Constraint *(SOURCED FROM PLAN FILE)*

> **tags** · XPBD, SOURCED, PLAN, FILE
> **tl;dr** · Long-range-attachment (tether) constraint used internally for cloth/vegetation but not exposed; promotes to reusable XPBD distance/rope constraint. Consumes ...
> **ctx** · XPBD Rope/Tether Constraint *(SOURCED FROM PLAN FILE) -- Long-range-attachment (tether) constraint used internally for cloth/vegetation but not exposed; promotes to reusable XPBD distance/rope constraint. Consumes M5.2 solver + M5.2-EXT-08 IK/joint infra. Used by zipline/grappling/winch + drag-ragdoll.
> **meta** · depends-on: M5-EXT-25 · depended-by: -

##### Systems Touched
Long-range-attachment (tether) constraint used internally for cloth/vegetation but not exposed; promotes to reusable XPBD distance/rope constraint. Consumes M5.2 solver + M5.2-EXT-08 IK/joint infra. Used by zipline/grappling/winch + drag-ragdoll.

##### Math
Extended PBD (Macklin 2016): each distance constraint has compliance a and accumulates Lagrange multiplier lambda per substep, stiffness independent of iteration count/timestep: Dx=(w1w2/(w1+w2+a~)) * C * nC, with a~=a/Dt^2. Rope = chain of N distance constraints solved with substep XPBD (e.g. 4 substeps, 1 iter each).

##### Algorithm
Build rope as small particle chain (positions+inverse masses), one XPBD distance constraint per segment. Each substep: predict, solve all constraints updating lambda, integrate. Two endpoints bind to entity handles (player hand, vehicle hitch). Persistent-corrected lambda: rope neither explodes nor sags through floors under load.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Ziplines/ropes/winches are stable and non-stretchy — no PBD explosion or floor-sag failure.

#### [M5-EXT-39] Supersonic Scent-Wake Cones (Aerodynamic Scent Compression)

> **tags** · general
> **tl;dr** · Modifies spatial distribution rules for the Atmospheric Gaussian Plume Scent Dispersion grid.
> **ctx** · Supersonic Scent-Wake Cones (Aerodynamic Scent Compression) -- Modifies spatial distribution rules for the Atmospheric Gaussian Plume Scent Dispersion grid.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Modifies spatial distribution rules for the Atmospheric Gaussian Plume Scent Dispersion grid.

##### Math
`θ_cone = arcsin(‖v_advect + v_diff‖ / ‖v_player‖)`

##### Algorithm
Extends your M5.3 Atmospheric Gaussian Plume Scent Dispersion grid. When a player is sprinting or driving a vehicle fast through an open-world environment, updating scent emission cell-by-cell on a coarse grid creates blocky, disconnected traces that entities cannot pathfind along smoothly.

When the entity's velocity vector (‖v_player‖) exceeds the local wind advection and diffusion thresholds, the engine switches from standard circular emission grids to an analytical Mach-cone geometry trailing behind the player. Scent concentration levels are compressed strictly inside the calculated boundary angle (θ_cone).

The ratio inside `arcsin` must be ≤ 1 (it's a sine): slower ambient speed over faster player speed, the same shape as the classic Mach-cone half-angle `arcsin(c/v)`.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Moving at high speed leaves a concentrated, tightly packed aerodynamic scent wake behind you. Downwind screamer hordes can pick up the trail of a speeding vehicle over immense distances long after it has passed, preventing players from completely bypassing zombie perception logic simply by driving fast.

#### [M5-EXT-40] Atmospheric Convection Acoustic Refraction (Wind-Gated Hearing Filters)

> **tags** · general
> **tl;dr** · Blends M10 (Weather/Wind vectors) with M5.3 (SNR perception filtering) and M6 (Acoustic audio events).
> **ctx** · Atmospheric Convection Acoustic Refraction (Wind-Gated Hearing Filters) -- Blends M10 (Weather/Wind vectors) with M5.3 (SNR perception filtering) and M6 (Acoustic audio events).
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Blends M10 (Weather/Wind vectors) with M5.3 (SNR perception filtering) and M6 (Acoustic audio events).

##### Math
The sound pressure level (SPL) of an audio event arriving at an AI spatial hearing sensor is attenuated based on the vector dot product of the localized wind velocity (v_wind ) and the direct sound propagation unit vector (u_sound ) relative to the speed of sound (c):

`SPL_effective = SPL_base − 20·log₁₀(1 − (v_wind · u_sound)/c)`

##### Algorithm
Your M5.3 Signal-to-Noise Ratio (SNR) detection pipeline calculates continuous detection probabilities by dividing player audio signatures by active environmental backgrounds. This system introduces physical audio warping. Sound traveling upwind (against a high-velocity wind storm vector) is refracted upward and compressed, dropping its effective volume rapidly before it reaches zombie spatial hash coordinates.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
This introduces a deep layer of tactical meteorology. When a severe 15% rain storm or a 3% gale-force atmospheric event kicks up extreme wind vectors, firing a weapon upwind drastically reduces its audible footprint. You can utilize high-velocity storm fronts to mask noisy tactical movements or weapon discharges from a screamer horde positioned downwind from your location, turning the environment into an active sensory shield.

#### [M5-EXT-41] Volatile Scent-Evaporation Thermal Grids

> **tags** · general
> **tl;dr** · Extends M5.3 Atmospheric Gaussian Scent Plume Dispersion matrices.
> **ctx** · Volatile Scent-Evaporation Thermal Grids -- Extends M5.3 Atmospheric Gaussian Scent Plume Dispersion matrices.
> **meta** · depends-on: - · depended-by: M5-EXT-44

##### Systems Touched
Extends M5.3 Atmospheric Gaussian Scent Plume Dispersion matrices.

##### Math
Your character's scent emission rate (E_scent ) is treated as a volatile organic compound evaporation field that scales over ambient atmospheric temperatures (T_Celsius ) and wind flow shears:

`E_scent = E_baseline · (1.0 + α·T_Celsius)`

The lifespan and decay rate (λ_decay) of the scent particles deposited into your spatial tracking grids expand exponentially as the ground surface heats up:

`λ_decay = λ_base · e^(β·T_Celsius)`

##### Algorithm
Your spatial tracking system distributes scent footprint intensity bytes across your spatial maps. This system scales the baseline injection levels and decay rates by sampling your global M10 calendar temperature loops. It updates the grid variables on a slow, low-overhead background cadence, avoiding frame budget spikes.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Sprinting across an asphalt avenue during a hot 35 ∘ C midday sector cause your scent trail to evaporate and dissipate into the air rapidly, making it difficult for tracking trackers to lock onto your path. However, cold, humid night cycles lock your scent down near the pavement surface, leaving a thick, long-lasting scent trail that lets background trackers track your route across long distances.

#### [M5-EXT-42] Volumetric Scent-Cloud Domain Warping (Wind Blaster)

> **tags** · general
> **tl;dr** · Modifies M5.3 Atmospheric Gaussian Scent Plume cells during explosive events.
> **ctx** · Volumetric Scent-Cloud Domain Warping (Wind Blaster) -- Modifies M5.3 Atmospheric Gaussian Scent Plume cells during explosive events.
> **meta** · depends-on: - · depended-by: M5-EXT-44

##### Systems Touched
Modifies M5.3 Atmospheric Gaussian Scent Plume cells during explosive events.

##### Math
High-pressure explosive detonations generate a divergence step that distorts your spatial scent tracking field arrays away from the blast center:

`∇⋅v_scent =Q⋅δ(x−x_blast )`

##### Algorithm
An explosive detonation injects a divergence source term at the blast's grid cell. Rather than deleting scent data, nearby scent-cell velocity vectors are pushed radially outward from the blast center each tick, scattering the cloud instead of erasing tracking data.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
If a tracking zombie pack has localized your position via scent plume advection vectors, detonating a pipe bomb or firing a high-overpressure muzzle shot creates a localized atmospheric shockwave. This violently clears and pushes your scent cloud away from your coordinates, breaking their scent tracking vectors instantly.

#### [M5-EXT-43] Aerodynamic Scent-Plume Mass Displacement

> **tags** · general
> **tl;dr** · Deepens M5.3 Atmospheric Gaussian Scent Plume cell updates using M10 wind pathways.
> **ctx** · Aerodynamic Scent-Plume Mass Displacement -- Deepens M5.3 Atmospheric Gaussian Scent Plume cell updates using M10 wind pathways.
> **meta** · depends-on: - · depended-by: M5-EXT-44

##### Systems Touched
Deepens M5.3 Atmospheric Gaussian Scent Plume cell updates using M10 wind pathways.

##### Math
To prevent players from gaming or wiping their scent trails using simple tracking updates, explosions do not delete scent footprint data arrays. Instead, high-pressure explosive detonations generate a physical fluid divergence step that shifts scent cells outward using a strict conservation of mass vector:

`∂t_∂C +v_wind ⋅∇C=D∇ 2 C+S_blast (x−x_blast )`

##### Algorithm
Rather than clearing scent data on explosion, a blast adds a source term to the advection-diffusion scent equation, shoving nearby scent mass radially outward. Tracking entities still converge on the scattered field, just without a precise origin point.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Detonating a pipe bomb or firing a high-overpressure muzzle shot does not dissolve your scent trail. The blast wave violently shoves your localized scent cloud outward, scattering it down alleys and building corridors. Tracking zombie herds lose your exact spot coordinates, but they will still converge on the broader, scattered scent field downwind, forcing you to think about wind direction before creating explosions.

#### [M5-EXT-44] Volatile Scent-Suppression Atmospheric Aerosol Diffusion Grid

> **tags** · general
> **tl;dr** · Hardens M5.3's Atmospheric Gaussian Plume Scent Dispersion against active particulate layers — dense smoke from burning structures (M6.5 fire thermodynamics)...
> **ctx** · Volatile Scent-Suppression Atmospheric Aerosol Diffusion Grid -- Hardens M5.3's Atmospheric Gaussian Plume Scent Dispersion against active particulate layers — dense smoke from burning structures (M6.5 fire thermodynamics) or vehicle exhaust — masking and decaying the player's scent signature within affected grid cells. Complements [M5-EXT-41]'s thermal scent-evaporation grid; that one scales emission/decay by temperature, this one scales the read-out by local aerosol density.
> **meta** · depends-on: M5-EXT-41, M5-EXT-42, M5-EXT-43 · depended-by: -

##### Systems Touched
Hardens M5.3's Atmospheric Gaussian Plume Scent Dispersion against active particulate layers — dense smoke from burning structures (M6.5 fire thermodynamics) or vehicle exhaust — masking and decaying the player's scent signature within affected grid cells. Complements `[M5-EXT-41]`'s thermal scent-evaporation grid; that one scales emission/decay by temperature, this one scales the read-out by local aerosol density.

##### Math
`C_effective = C_raw_scent · e^(−κ_mask · ρ_smoke)`

##### Algorithm
Samples the local aerosol/smoke cell density (M6.5) at the same grid resolution the scent-dispersion pass already queries, and applies an exponential mask directly to the raw scent value read out for that cell — no separate grid maintained, this is a read-time modifier on the existing plume data.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Setting a diversionary fire or driving through smoke genuinely masks your scent trail from tracking hordes, giving you a tactical option beyond `[M5-EXT-42]`/`[M5-EXT-43]`'s blast-displacement approach — smoke suppresses continuously rather than scattering a one-time cloud.

#### [M5-EXT-45] Confidence-Gated Stealth Strikes

> **tags** · general
> **tl;dr** · Confidence-Gated Stealth Strikes
> **ctx** · Confidence-Gated Stealth Strikes
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Stealth AI, player detection ([M1] ECS), audio ([M6]). Depends on [M5] perception.

##### Math
Strike confidence `c = σ(w·[LOS, proximity, noise, targetVuln])` (logistic). Strike only if `c > c_thresh` AND `surprise> s_min`. False strikes punished by exposure.

##### Algorithm
1. Agent estimates c from senses. 2. If c>thresh and target unaware → stealth strike (instant takedown). 3. Else: hold, reposition. 4. Noise ([M6]) lowers c.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Stealth is earned, not a button: flank a distracted zombie and it's a clean takedown; rush a aware one and you get swarmed. Rewards patience over brute force.

#### [M5-EXT-46] Visual Occlusion Sector Ray-March Pre-Filter

> **tags** · general
> **tl;dr** · M5.3 + M5.4. Ray-marches visibility sectors pre-frame to pre-cull AI that can't see the player.
> **ctx** · Visual Occlusion Sector Ray-March Pre-Filter -- M5.3 + M5.4. Ray-marches visibility sectors pre-frame to pre-cull AI that can't see the player.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.3 + M5.4. Ray-marches visibility sectors pre-frame to pre-cull AI that can't see the player.

##### Math
sector_i visible if March(origin, dir_i) < dist(player); cull AI in invisible sectors.

##### Algorithm
Before AI update, the player's visibility is ray-marched into angular sectors; AI whose sectors are occluded are pre-culled from perception (they can't see the player), saving perception/behavior cost. Recomputed at a low rate, reprojected between.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Off-screen AI doesn't magically track you through walls - perception is honest and cheaper.

#### [M5-EXT-47] Volumetric Boid Compression Stampede Crush

> **tags** · general
> **tl;dr** · Enhances M5.4 reaction-diffusion fields and M5.1 Reynolds boid flocking parameters.
> **ctx** · Volumetric Boid Compression Stampede Crush -- Enhances M5.4 reaction-diffusion fields and M5.1 Reynolds boid flocking parameters.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Enhances M5.4 reaction-diffusion fields and M5.1 Reynolds boid flocking parameters.

##### Math
When hordes are panicked, their separation thresholds drop, generating intense mechanical crowd pressure (P_crush ). Your engine evaluates this by tallying neighbor density straight from your existing SpatialHash:

`P_crush = Σ_{i∈LocalCell} ∥F_separation_i∥`

If P_crush exceeds the tissue threshold (σ_flesh), the engine fires a target DamageEvent downward to any entities caught beneath the swarm.

##### Algorithm
Each tick, the AI steering phase sums separation-force magnitudes of boids sharing a local spatial-hash cell into a crush-pressure scalar. Exceeding the tissue threshold damages any entity beneath the crowd proportional to the excess — a panicked horde's own density becomes the hazard.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
This mechanics triggers terrifying stampede scenarios. If an explosion or a screamer alarm panics a packed alleyway, the trailing zombies will physically run over and trample crawled or injured entities in their path. This creates crushed organic debris, giving you a tactical incentive to herd hordes into tight architectural gaps to let them crush their own numbers.

#### [M5-EXT-48] Over-Mantle Tumbling Torque Vectoring

> **tags** · general
> **tl;dr** · Integrates M5.4 horde steering pressure variables directly into M2.9 procedural vaulting routines.
> **ctx** · Over-Mantle Tumbling Torque Vectoring -- Integrates M5.4 horde steering pressure variables directly into M2.9 procedural vaulting routines.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Integrates M5.4 horde steering pressure variables directly into M2.9 procedural vaulting routines.

##### Math
When a zombie entity initiates a procedural vault maneuver over a low wall barrier, it experiences crowd pushing forces (F_push ) from trailing entities. If this push force exceeds the balance threshold, it injects an over-rotating angular momentum torque (τ_tumble ):

`τ_tumble =r_torso ×F_push`

`If the rotation velocity (ω_rot ) exceeds stability limits during the vault, the entity's procedural animation loop aborts, instantly switching its Jolt physics state flags into a full tumbling ragdoll pass.`

##### Algorithm
To safeguard your performance budget, the engine completely bypasses ragdoll initialization while the zombie is airborne. The entity is treated as a single Jolt rigid body box tracking rotation. The system queries your SpatialHash neighbor densities; if forward crowd force vectors cross safety limits, it forces an immediate tumble state, delaying ragdoll assembly generation until terminal contact with the terrain.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
When a dense horde chases you over a rooftop, the front zombies attempting to vault over the edge railings get physically pushed from behind by the mass of the crowd. Instead of landing cleanly on their feet, they over-rotate, tumbling head-over-heels over the edge and crashing into the street below, creating organic pile-ups beneath the structure.

#### [M5-EXT-49] Emergent Difficulty via Director Pacing & Outbreak Feedback (No Difficulty Setting)

> **tags** · general
> **tl;dr** · Emergent Difficulty via Director Pacing & Outbreak Feedback (No Difficulty Setting)
> **ctx** · Emergent Difficulty via Director Pacing & Outbreak Feedback (No Difficulty Setting)
> **meta** · depends-on: - · depended-by: M5-EXT-03

##### Systems Touched
Director/AI pacing, outbreak spread ([M8] SEIR), spawn budget. Depends on [M8].

##### Math
Outbreak feedback: `pressure = k·I(t)/N_total` (infected fraction). Spawn rate `λ = λ0·(1 + γ·pressure)` capped at `λ_max`. No explicit difficulty dial — the world's infected fraction IS the difficulty.

##### Algorithm
1. Track I(t) from [M8]. 2. Map to spawn pressure. 3. Scale horde size/budget within Tier-0 perf cap. 4. Director paces set-pieces off pressure, not a timer.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Difficulty emerges from your choices: contain outbreaks → quiet; let them spread → the world turns on you. No 'easy/normal/hard' menu — the simulation is the dial (avoids the NMS 'promised systems absent' trap by making it real).

#### [M5-EXT-50] Reaction-Diffusion Grid Sub-sampled Boundary Welder

> **tags** · general
> **tl;dr** · M5.4 fear field (RD). Welds RD grid boundaries across chunk seams so the field is continuous.
> **ctx** · Reaction-Diffusion Grid Sub-sampled Boundary Welder -- M5.4 fear field (RD). Welds RD grid boundaries across chunk seams so the field is continuous.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.4 fear field (RD). Welds RD grid boundaries across chunk seams so the field is continuous.

##### Math
ghost = avg(neighbors across seam); sub-sample boundary, copy to neighbor ghost cells each step.

##### Algorithm
The reaction-diffusion fear field is computed per chunk; at chunk boundaries, a sub-sampled ghost exchange copies edge cells into neighbor ghost cells so diffusion crosses seams without discontinuity or double-counting.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Fear/panic spreads continuously across the world - no visible grid seams in the fear field.

#### [M5-EXT-51] Hierarchical A* Macro-Cell Long-Range Road-Graph Router

> **tags** · general
> **tl;dr** · M5.4 horde + M8.5 caravan routing. Hierarchical A* over the macro road-graph for off-screen long-range routing.
> **ctx** · Hierarchical A* Macro-Cell Long-Range Road-Graph Router -- M5.4 horde + M8.5 caravan routing. Hierarchical A* over the macro road-graph for off-screen long-range routing.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.4 horde + M8.5 caravan routing. Hierarchical A* over the macro road-graph for off-screen long-range routing.

##### Math
macro = Cluster(roadNodes, cell); path = HAStar(root,goal,macro,fine); determinism = SplitMix64(seed).

##### Algorithm
A macro-graph of abstract street/highway nodes (built from M4-EXT-10 splines) is clustered into macro-cells; Hierarchical A* plans at the macro level then refines fine paths inside the goal cell. Seeded via SplitMix64 for deterministic hordes. Terrain-gen consumes it for road-exclusion masks (M4-EXT-17). Single system - M9-EXT-18 convoy router extends this graph with a lane-width field rather than duplicating it.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Hordes flow along real streets and caravans traverse believably; roads reshape spawning - one shared graph, no duplicate systems.

#### [M5-EXT-52] Holling Type II Cannibalism Feeding Satiator

> **tags** · II
> **tl;dr** · M5.4 cannibal factions. Models feeding satiation (Holling Type II) so factions stop at capacity.
> **ctx** · Holling Type II Cannibalism Feeding Satiator -- M5.4 cannibal factions. Models feeding satiation (Holling Type II) so factions stop at capacity.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.4 cannibal factions. Models feeding satiation (Holling Type II) so factions stop at capacity.

##### Math
consumption = (a*N)/(1 + a*h*N); dN/dt = r*N*(1-N/K) - consumption; satiation caps intake.

##### Algorithm
Cannibal factions 'consume' victim density with a Holling Type II functional response (intake saturates with abundance), so they don't infinitely devour - a satiation term caps per-capita consumption, producing stable predator-prey oscillation instead of wipeout.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Cannibal factions thin herds then back off - the world's ecosystem stays balanced, not extinct.

#### [M5-EXT-53] Fear-Field Diffusion via Spatial Hash

> **tags** · general
> **tl;dr** · M5.4 + M1-EXT-27. Diffuses the fear/reaction-diffusion field over a spatial hash.
> **ctx** · Fear-Field Diffusion via Spatial Hash -- M5.4 + M1-EXT-27. Diffuses the fear/reaction-diffusion field over a spatial hash.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
M5.4 + M1-EXT-27. Diffuses the fear/reaction-diffusion field over a spatial hash.

##### Math
f_new = f + D*Laplacian(f) - decay; Laplacian from neighbor cells in hash;

##### Algorithm
The reaction-diffusion fear field is stepped by sampling neighbor cells from the spatial hash (M1-EXT-27) to compute the Laplacian, diffusing panic smoothly across the world without a full grid. Cheap, sparse.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Fear/panic spreads organically - hordes react to player pressure at range.

#### [M5-EXT-54] Cannibalism / World-State Morality Flag

> **tags** · general
> **tl;dr** · Emits M13-EXT-31 broadcast and shifts M8.6-EXT-13/-24 faction standing; major moral node.
> **ctx** · Cannibalism / World-State Morality Flag -- Emits M13-EXT-31 broadcast and shifts M8.6-EXT-13/-24 faction standing; major moral node.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Emits M13-EXT-31 broadcast and shifts M8.6-EXT-13/-24 faction standing; major moral node.

##### Math
morality.world |= CANNIBAL; Δrep[faction] = −w_cannibal.

##### Algorithm
If the player consumes human meat, the world-state morality flag is set and a heavy broadcast (M13-EXT-31) fires; factions (M8.6) react with lasting hostility.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Some lines can't be uncrossed — the world brands you and remembers.

#### [M5-EXT-55] Noise-Field Encounter Trigger

> **tags** · general
> **tl;dr** · Drives M8 spawns and M5.4-EXT-16 graph edges; couples to M4-EXT-59 road network.
> **ctx** · Noise-Field Encounter Trigger -- Drives M8 spawns and M5.4-EXT-16 graph edges; couples to M4-EXT-59 road network.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Drives M8 spawns and M5.4-EXT-16 graph edges; couples to M4-EXT-59 road network.

##### Math
noise_field(x) diffuses; trigger when ∫ noise over cell > threshold.

##### Algorithm
A diffusing noise field accumulates player sound; cells above threshold seed encounter requests (M8) biased toward road-graph edges (M4-EXT-59) so threats arrive by path.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Be loud and the world sends something down the road to investigate.

#### [M5-EXT-56] Encounter Spawn Budget / Cap

> **tags** · general
> **tl;dr** · Bounds M8 spawns and M12-EXT-12 voices; shares M5.4-EXT-16 event graph budget.
> **ctx** · Encounter Spawn Budget / Cap -- Bounds M8 spawns and M12-EXT-12 voices; shares M5.4-EXT-16 event graph budget.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Bounds M8 spawns and M12-EXT-12 voices; shares M5.4-EXT-16 event graph budget.

##### Math
active = min(active+req, budget(region)); budget scales with perf headroom.

##### Algorithm
Each region has a spawn budget; encounter requests (M8) draw from it and are rejected when full, preventing death-spiral spawns; budget tracks perf headroom (M4-EXT-48).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
The horde can't spawn infinitely — the engine caps the nightmare to what your rig survives.

#### [M5-EXT-57] Event→Encounter Graph Resolver

> **tags** · general
> **tl;dr** · Central resolver: events → encounter requests (M5.4-EXT-13) placed via M4 graphs; M5.1/M13 ambiance.
> **ctx** · Event→Encounter Graph Resolver -- Central resolver: events → encounter requests (M5.4-EXT-13) placed via M4 graphs; M5.1/M13 ambiance.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Central resolver: events → encounter requests (M5.4-EXT-13) placed via M4 graphs; M5.1/M13 ambiance.

##### Math
encounter = resolve(event, nearest_graph_node(pos)).

##### Algorithm
When an event fires, the resolver finds the nearest road/POI node (M4-EXT-59/62) to the event position and emits an encounter request (M5.4-EXT-13) there, so threats arrive from the world, not nowhere.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Events become places: a raid doesn't pop in — it comes from the road.

#### [M5-EXT-58] Moral-Choice → Faction Edge Writer

> **tags** · general
> **tl;dr** · Consumes M13-EXT-31/32 morals + M13-EXT-36 broadcasts; mutates M8.6-EXT-13 reputation edges.
> **ctx** · Moral-Choice → Faction Edge Writer -- Consumes M13-EXT-31/32 morals + M13-EXT-36 broadcasts; mutates M8.6-EXT-13 reputation edges.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Consumes M13-EXT-31/32 morals + M13-EXT-36 broadcasts; mutates M8.6-EXT-13 reputation edges.

##### Math
edge(faction,player) += w_moral * sign(choice).

##### Algorithm
Each moral broadcast (M13-EXT-31) applies a signed weight to the player's edge in every faction graph (M8.6-EXT-13); repeated acts saturate standing. Broadcasts (M13-EXT-36) log it.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Your reputation is a graph you draw with your choices — every act is an edge.

#### [M5-EXT-59] RETIRED — Secondary Road Graph (do not instantiate)

> **tags** · RETIRED
> **tl;dr** · Intentionally excluded per _RETIRED.md; M4-EXT-59 is the canonical road graph. Listed for traceability only.
> **ctx** · RETIRED — Secondary Road Graph (do not instantiate) -- Intentionally excluded per _RETIRED.md; M4-EXT-59 is the canonical road graph. Listed for traceability only.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Intentionally excluded per _RETIRED.md; M4-EXT-59 is the canonical road graph. Listed for traceability only.

##### Math
n/a (retired).

##### Algorithm
The v68 merge audit notes this road graph is superseded by M4-EXT-59. Instantiating it would create a duplicate graph and orphaned edges. Left retired.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Documented so nobody reintroduces a redundant road network. (No gameplay impact — M4-EXT-59 covers it.)

#### [M5-EXT-60] Horde Director

> **tags** · ai, combat
> **tl;dr** · M5/M8. Spawns/scales zombie waves; uses M8-EXT-32 funnel pathing; co-op-aware.
> **ctx** · Horde Director -- M5/M8. Part 24 + R43/R48. Reuses M8-EXT-32 (funnel), M8-EXT-51 (scheduled horde), Jolt + M1 SpatialHash.
> **meta** · depends-on: [M8-EXT-32], [M8-EXT-51] · depended-by: -

##### Systems Touched
Horde Director -- M5/M8. Part 24 + R43/R48. Reuses M8-EXT-32 (funnel), M8-EXT-51 (scheduled horde), Jolt + M1 SpatialHash.

##### Math
-

##### Algorithm
Director queries threat (player loot, defense deficit) → spawns waves along M8-EXT-32 funnels; scales with M8-EXT-51 cadence. Co-op: synced via M12-EXT-22.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Intelligent, escalating hordes — not random spam.

#### [M5-EXT-61] Navmesh / A* Pathfinding

> **tags** · ai
> **tl;dr** · M5. Terrain-adaptive navmesh + A*; zombies fall/jump/avoid obstacles.
> **ctx** · Navmesh/A* Pathfinding -- M5. Part 24 + R43. Reuses Jolt collision + M1 SpatialHash broad-phase. Either/or E60 RESOLVED (client-prediction): co-op uses client-side prediction with server reconciliation for smooth movement; lockstep reserved for sim-critical deterministic moments.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Navmesh/A Pathfinding -- M5. Part 24 + R43. Reuses Jolt collision + M1 SpatialHash broad-phase. Either/or E60 RESOLVED (client-prediction): co-op uses.

##### Math
-

##### Algorithm
Navmesh baked per chunk (M4 streaming); A* over nodes; terrain-adaptive (slope/height → fall/jump tags). Obstacle avoidance via M1 SpatialHash.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Zombies navigate the real world — climb rubble, avoid your barricades.

#### [M5-EXT-62] Wildlife Ecosystem

> **tags** · ai
> **tl;dr** · M5. Ambient animals (scavengers, infected beasts) for life + food + dread.
> **ctx** · Wildlife Ecosystem -- M5. Part 25. Reuses M5-EXT-60 (horde director) + M5-EXT-61 (navmesh) + Jolt. Either/or E64 RESOLVED (infected-beast): fauna includes infected beasts (zombie wolves/deer) for atmospheric horror tension, alongside passive scavengers; pure-passive reserved for safe-zone biomes.
> **meta** · depends-on: [M5-EXT-60], [M5-EXT-61] · depended-by: -

##### Systems Touched
Wildlife Ecosystem -- M5. Part 25. Reuses M5-EXT-60 (horde director) + M5-EXT-61 (navmesh) + Jolt. Either/or E64 RESOLVED (infected-beast): fauna incl.

##### Math
-

##### Algorithm
Spawn points seeded by biome (M4-EXT-23); animals use M5-EXT-61 navmesh; some variants infected (reuse M8 SEIR). Flee from player/zombie noise (M6.5).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A living world — and a new thing to fear in the dark.

---

#### [M5-EXT-63] World-State Director

> **tags** · systems, ai
> **tl;dr** · M5. Director drives events based on world-state, not just player state. Famine, plague, migration, mega-horde.
> **ctx** · World-State Director -- M5. Part 32. Reuses M5-EXT-60 (director) + M8-EXT-60 (cascade engine) + M8-EXT-59 (settlement sim) + M8-EXT-03 (SEIR illness) + M8-EXT-04 (cure) + M8-EXT-51 (horde). Either/or E84 RESOLVED (full world-state weighting): the director reads the entire world-state graph (M8-EXT-60) — population, faction war, illness, food, power — to choose events; limited weighting would waste the cascade data.
> **meta** · depends-on: [M5-EXT-60], [M8-EXT-60], [M8-EXT-59], [M8-EXT-03], [M8-EXT-04], [M8-EXT-51] · depended-by: -

##### Systems Touched
World-State Director -- M5. Part 32. Reuses M5-EXT-60 (director) + M8-EXT-60 (cascade engine) + M8-EXT-59 (settlement sim) + M8-EXT-03 (SEIR illness) .

##### Math
-

##### Algorithm
Director reads world-state graph: if settlement overcrowded → plague event (SEIR, M8-EXT-03); if food shortage across region → famine + migration wave; if faction war unresolved → refugee caravan; if zombie population critical → mega-horde (M8-EXT-51). Events are chosen by what makes the most interesting emergent story (RimWorld storyteller model), not by difficulty scaling. Player actions affect the probability of each event type.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
The world reacts to its own problems, not just yours. Starve a settlement and the director sends a plague. Ignore the zombie buildup and the horde comes.

#### [M5-EXT-64] Sleeper Zombie Spawn & Wake System

> **tags** · AI, spawn, horror
> **tl;dr** · A fraction of interior zombies spawn Dormant (slumped/leaning); they wake on sound, proximity, light, or damage. Buildings are unpredictable.
> **ctx** · Sleeper Zombie Spawn & Wake System -- M5. New block. Fills the "sleeper" gap vs Project Zomboid / Dying Light. Ties to M5-EXT-13 (Dormant state) + M5-EXT-12 (emerge anim) + M4 (interior placement).
> **meta** · depends-on: [M5-EXT-13], [M5-EXT-12], [M4-EXT-32], [M5-EXT-14] · depended-by: [M5-EXT-13], [M11-EXT-62]

##### Systems Touched
M5 spawn pipeline + FSM. On worldgen, interior cells (M4-EXT-32 POI placement) roll `sleeperChance` (default 0.30) per zombie → `wakeFlags` bitmask. Director (M5-EXT-10) respawns dormant pool on a cooldown after a building is cleared.

##### Math
`wakeScore = max(soundLevel/wakeThresh, proximityPenalty, lightOnFace, damageTaken)`
- `sleeperChance = 0.30` interior, `0.0` exterior (sleepers only spawn indoors)
- `wakeThresh`: whisper 0.05, door-slam 0.4, gunshot 0.9, proximity < 2 m = 1.0, flashlight-on-face = 0.7

##### Algorithm
At spawn, set `state = Dormant` and `wakeFlags`. Each tick while Dormant: poll sound (M5-EXT-14), proximity (player within 2 m), light (M10 day/night or player flashlight hitting face), damage. If any exceeds its threshold → `Dormant → Idle` (M5-EXT-13) and play emerge animation (M5-EXT-12). Cleared building stays dormant-free for `respawnCooldown` days, then Director repopulates the dormant pool. Stealth takedown (M5-EXT-19) on a Dormant zombie = instant kill, no sound — high risk/reward for clearing.

##### Examples
- Player enters a quiet house; 3 dormant zombies lean in corners. Player moves silently → passes. Player knocks a shelf → door-slam sound → all 3 wake.
- Player shines flashlight on a dormant zombie's face → lightOnFace 0.7 > wakeThresh → wakes, grab animation.
- Player sneaks behind a dormant zombie, hold-breath, takedown → instant kill, no alert.

##### Failure Modes
- Dormant zombies block navmesh baking (M5-EXT-01) → treat Dormant as static obstacle until woken.
- Respawn cooldown too short → building never feels "cleared" → tune per POI size.
- Wake chain reaction (one wakes, screams, wakes all) → cap scream propagation to 1 ring.

##### Player-Facing Impact
A cleared room can still kill you. Search every corner. Move quiet. The dark is not safe.

#### [M5-EXT-65] Zombie Scent / Pheromone Trail

> **tags** · AI, perception, scent
> **tl;dr** · Player blood, open wounds, and sweat emit a scent value diffused downwind; zombies chemotax up the gradient. Rain and water wash it away.
> **ctx** · Zombie Scent / Pheromone Trail -- M5. New block. Fills the "smell/pheromone" gap. Ties to M2-EXT-80/81 (injury/wound) + M10 (weather/wind) + M5-EXT-09 (smellScore).
> **meta** · depends-on: [M2-EXT-80], [M2-EXT-81], [M10-EXT-23], [M5-EXT-09] · depended-by: [M5-EXT-09], [M5-EXT-13]

##### Systems Touched
M5 scent field. A coarse grid (cell = 4 m) per chunk stores `scentValue` diffused each tick by wind direction (M10 weather). Player emits scent from wound state (M2-EXT-80/81) + exertion. Zombies sample the field in M5-EXT-09.

##### Math
`scentEmit = baseEmit * woundSeverity * (1 - hygiene)`; `baseEmit`: freshBlood 1.0, oldBlood 0.4, sweat 0.3, rottingGear 0.2
- Diffusion: `scent[cell] += wind * (neighborUpwind - scent[cell]) * dt * diffuseRate`
- Decay: `scent *= exp(-decayRate * dt)`; rain (M10) multiplies decayRate ×5
- `smellScore = scentSample(pos) * windAlignmentFactor` (M5-EXT-09)

##### Algorithm
Each tick, sources (bleeding players, carcasses) inject `scentEmit` into their cell. The field diffuses downwind and decays. Zombies in M5-EXT-09 sample `scentSample(pos)`; if above `smellThreshold`, they steer toward the gradient (chemotaxis) even without sight/hearing. Player countermeasures: submerge in water (wash), cover with mud, wear scent-blocking gear (hazmat/raincoat), or move upwind.

##### Examples
- Player takes a deep bite, bleeds, stands downwind of a pack → scent ramps → pack converges up-gradient within 30 s.
- Player wades through a river → scent washed → trail breaks → pack loses track.
- Player mud-camouflages → scentEmit ×0.3 → survives closer passes.

##### Failure Modes
- Scent field O(n) per zombie per tick → sample only 1 cell (no loop) → O(1).
- Field stale after player leaves → decay handles it; verify decayRate > emitRate at rest.
- Wind direction jump → smooth wind vector (M10) before diffusion step.

##### Player-Facing Impact
You can't see the smell, but they can. A bleeding player is a beacon. Clean up, cover up, or get downwind.

#### [M5-EXT-66] Stealth Approach System

> **tags** · AI, player, stealth
> **tl;dr** · Player noise + visibility meters gate detection; crouch/prone + cover + hold-breath enable silent takedowns on unaware zombies.
> **ctx** · Stealth Approach System -- M5. New block. Fills the "stealth approach" gap. Ties to M5-EXT-09 (perception) + M5-EXT-13 (states) + M2-EXT-18 (encumbrance) + M10 (day/night).
> **meta** · depends-on: [M5-EXT-09], [M5-EXT-13], [M2-EXT-18], [M10-EXT-23] · depended-by: [M5-EXT-09], [M5-EXT-19]

##### Systems Touched
M5 player-stealth model. Player emits `noiseLevel` (movement × surface × encumbrance) + `visibility` (light × stance × cover × camo). Both feed zombie `detect` in M5-EXT-09. Takedown resolves via M2.9 damage event.

##### Math
`noiseLevel = moveSpeedNorm * surfaceNoise * (1 + encumbrancePenalty)`
- `surfaceNoise`: grass 0.4, concrete 0.8, metal 1.2, snow 0.6
- `visibility = lightLevel * stanceMult * coverMult`; `stanceMult`: stand 1.0, crouch 0.5, prone 0.25
- `takedownWindow`: behind + within 1.5 m + holdBreath > 0.8 s → instant kill, no sound

##### Algorithm
Per tick, compute player `noiseLevel` + `visibility`. Zombies within range in M5-EXT-09 compare against their thresholds. Crouch reduces both; prone minimizes; full cover (wall) blocks LOS entirely. A stealth takedown requires: zombie in Dormant/Idle/Wander (not Alert), player behind (angle > 120°), within 1.5 m, hold-breath held > 0.8 s → silent instant kill (M2.9). Failed takedown (zombie Alert) → it turns + Attack. Noise from the takedown itself (body fall) emits a small SoundEvent (M5-EXT-14) that can alert neighbors.

##### Examples
- Player crouch-walks on grass behind an Idle zombie at 1 m, holds breath → takedown → silent kill.
- Player sprints on metal past a Wander zombie → noiseLevel high → Alert → Chase.
- Player prone in tall grass at dusk (low light) → visibility 0.1 → passes 3 m from a shambler unseen.

##### Failure Modes
- Takedown through a wall (LOS not checked) → require LOS-clear in takedown check.
- Hold-breath exploit (perma-hold) → stamina drain forces breath release (M2-EXT-18).
- Noise from takedown alerts whole block → cap emitted SoundEvent amplitude low.

##### Player-Facing Impact
The quiet approach is its own weapon. Move slow, stay low, watch the wind, and the dead won't know you're there — until they're already down.

---
#### [M5-EXT-67] Aerodynamic Wake Slipstream Horde Slingshot
> **tags** · m5.1, system
> **tl;dr** · Aerodynamic Wake Slipstream Horde Slingshot — M5-EXT-67.
> **ctx** · M5.1-EXT-01 — M5.1 procedural zombie variation.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Links M9 vehicle chassis velocity tracking fields straight to M5.1 Reynolds boid steering arrays.

##### Math

A speeding vehicle chassis shears the atmosphere, creating a low-pressure wake zone behind its path. Aggressive zombie phenotypes entering this tracking cone encounter virtually zero aerodynamic resistance, dropping their drag coefficients (C_d ) exponentially:

`C_d(x) = C_d0 · (1.0 − e^(−x/λ_wake))`

##### Algorithm

Each tick, boids inside M9's tracked wake cone have their drag coefficient queried against distance into the cone via the exponential falloff, letting fast zombies inside the cone match the vehicle's closing speed.

##### Examples

```cpp
float GetHordeDraftDrag(float c_d0, float dist, float lambda) {
    return c_d0 * (1.0f - std::exp(-dist / lambda));
}
```

##### Failure Modes

Boid steering divergence. Wake cone not sampled. Drag NaN. Vehicle tracking lost.

##### Player-Facing Impact

Speeding down highways doesn't let you effortlessly outrun threats. High-tier running zombies can actively catch your vehicle's slipstream, entering the low-pressure pocket behind your rear bumper. They gain a high-velocity drafting boost that lets them latch onto your tailgate or swarm your open truck beds like drafting racing cars.

##### Depends on (not yet built at this point)

M9 — implement against a hardcoded/stub value for now; revisit once that milestone lands.
---

#### [M5-EXT-68] Player Character Generation (Uninfected-Mode Phenotype Pipeline)
> **tags** · m5.1, system
> **tl;dr** · Player Character Generation (Uninfected-Mode Phenotype Pipeline) — M5-EXT-68.
> **ctx** · M5.1-EXT-02 — M5.1 procedural zombie variation.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Runs the existing M5.1 zombie phenotype/bio-weight generator with the infection-mutation pass disabled — one canonical procedural humanoid generator for players, survivors, and zombies, not a separate hand-authored player model.

##### Math

No new math — identical `ZombieTraits` deterministic trait distribution already used for zombies, evaluated with `InfectionState = Uninfected` so the mutation terms zero out.

##### Algorithm

The same trait-distribution pipeline already generating every zombie in the world runs once at character-creation time with the mutation/decay passes gated off by an `InfectionState` flag — character creation is choosing which slice of an existing procedural space to sample from, not a second content pipeline.

##### Examples

```cpp
// Same function signature as zombie generation — InfectionState gates the mutation pass off
HumanoidPhenotype GeneratePlayerCharacter(uint64_t player_seed) {
    return GenerateZombiePhenotype(player_seed, InfectionState::Uninfected); // mutation terms no-op
}
```

##### Failure Modes

Infection flag ignored. Trait non-deterministic. Seed collision.

##### Player-Facing Impact

Player character appearance is procedurally generated with the same richness and variation as the zombies and survivors around them — genuinely unique-looking characters with zero hand-authored art, consistent with everything else in this engine.
---

#### [M5-EXT-69] Topology-Invariant Master Rig with Deformation-Transfer Binding
> **tags** · m5.1, system
> **tl;dr** · Topology-Invariant Master Rig with Deformation-Transfer Binding — M5-EXT-69.
> **ctx** · M5.1-EXT-03 — M5.1 procedural zombie variation.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Upgrades M5.1 zombie variation routines and feeds bind data into M5.2 procedural animation and M5.2's existing Dual-Quaternion compute skinning.

##### Math

Vertex skinning weights fall off with distance from a bone's influence line, but are now sampled once against the fixed master topology rather than recomputed per instance:

`Weight = max(0, 1 − Distance_to_bone / Radius_of_influence)²`

Per-vertex bone weights are NOT computed at runtime via raw line-proximity against each phenotype's bone placement — at extreme joint configurations (deep lunges, low-clearance crawling, compact ragdoll pile-ups) that produces "paper-folding" volume collapse and self-intersecting mesh, since a line-distance-only solve has no knowledge of surrounding topology. Bone placement is still generated per-phenotype/seed-driven at runtime, but weights are not recomputed from scratch each time — see the heat-diffusion approach below.

##### Algorithm

A single, structurally perfect Master Skeleton is authored once with hand-painted, high-quality base skinning weights covering every joint fold case (deep bends, compact poses, etc.). When a phenotype seed generates a specific mutant or zombie scale, the system does not re-derive skinning weights — it programmatically adjusts the *lengths and offsets* of the Master Skeleton's bone nodes to match the generated SDF volume, then transfers the resulting mesh deformation via Skeletal Morph Targets / Deformation Transfer matrices computed against the master's known-good weight painting. Because the topology and weight painting never change — only bone length/offset — extreme poses fold cleanly across every generated body variation instead of collapsing.

##### Examples

```cpp
// Normalizes bone weights so skin shaders never divide by a near-zero sum;
// weights themselves come from the Master Skeleton's baked painting, not a runtime line-proximity solve
void BalanceSkeletalBinds(float* weights, uint32_t count) {
    float sum = 0.0f;
    for (uint32_t i = 0; i < count; ++i) sum += weights[i];
    if (sum > 0.001f) for (uint32_t i = 0; i < count; ++i) weights[i] /= sum;
}
```

##### Failure Modes

Topology mismatch. Binding NaN. Skeleton scale wrong.

##### Player-Facing Impact

Hordes display rich anatomical variety — distorted mutations, heavily built behemoths, and lanky, emaciated infected all move, crawl, and ragdoll with accurate joint behavior and zero texture stretching or paper-folding, even in extreme compact poses.
---

#### [M5-EXT-70] Anatomical Hit-Volume SDF Gradients
> **tags** · m5.1, system
> **tl;dr** · Anatomical Hit-Volume SDF Gradients — M5-EXT-70.
> **ctx** · M5.1-EXT-04 — M5.1 procedural zombie variation.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Declares the base "Anatomical Hit-Volume SDF Gradients" bullet. Consumes `[M5.1-EXT-03]`'s Master Skeleton pose data (skinned vertex positions feed the SDF's zero-crossing surface) and feeds M2.9's ballistic damage resolution — hit location and penetration depth are read off the gradient rather than a raw mesh raycast.

##### Math

Each anatomical region (skull, torso, limb) is represented as a signed distance field; a hit's damage multiplier and exit-wound depth come from the gradient at the impact point: `Depth = |Φ(p)| / |∇Φ(p)|`, where `Φ` is the SDF value and `∇Φ` its normalized gradient direction.

##### Algorithm

Rather than raycasting the full skinned mesh per shot (expensive at horde density), each zombie carries a small set of primitive SDF volumes (capsules/ellipsoids) rigged to the Master Skeleton's bones, updated once per animation tick alongside `[M5.1-EXT-03]`'s pose. A hit tests against this lightweight SDF stack instead of triangle geometry: the gradient at the query point gives both the surface normal (for spatter direction) and penetration depth (for through-and-through vs. stopping-power resolution) in one evaluation.

##### Examples

```cpp
// Evaluated once per hit query against the rigged capsule/ellipsoid SDF stack, not per-triangle
float EvaluatePenetrationDepth(const glm::vec3& hitPoint, const AnatomicalSdf& sdf) {
    float phi = sdf.Evaluate(hitPoint);              // signed distance at impact point
    glm::vec3 gradPhi = sdf.EvaluateGradient(hitPoint); // normalized surface gradient
    return std::abs(phi) / glm::length(gradPhi);
}
```

##### Failure Modes

SDF resolution low. Hit-volume misaligned. Gradient not computed.

##### Player-Facing Impact

Headshots and limb hits register consistently even mid-animation (staggering, lunging, ragdolling) without expensive per-triangle raycasts, and bullet penetration depth reads correctly against actual body mass rather than a flat hitbox.
---

#### [M5-EXT-71] Heat-Diffusion Automatic Rigging & PSOLA Phenotype Vocalization
> **tags** · m5.1, system
> **tl;dr** · Heat-Diffusion Automatic Rigging & PSOLA Phenotype Vocalization — M5-EXT-71.
> **ctx** · M5.1-EXT-05 — M5.1 procedural zombie variation.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Declares the base "Heat-Diffusion Automatic Rigging. Phenotype Vocalization Shift via PSOLA" bullet. Rigging half feeds `[M5.1-EXT-03]`'s Master Skeleton binding; vocalization half feeds M6's audio propagation as the per-zombie emitter source, sharing PSOLA machinery with M6's `[M6-EXT]` voice pipeline rather than duplicating a pitch-shift implementation.

##### Math

Skin weight at each mesh vertex is the steady-state solution of the heat equation seeded at each bone's joint position, solved to convergence rather than approximated by inverse distance: `∂h/∂t = κ∇²h`, with each joint held as a fixed-temperature boundary condition (`h = 1` at its own seed point, `h = 0` at all other seeds) and the converged `h` per joint, per vertex, normalized across joints to give that vertex's bone weights. Vocalization pitch/formant shift is a PSOLA resampling of the base clip's pitch marks by a ratio derived from the phenotype's vocal-tract-length scalar `L`: `PitchRatio = L_reference / L_phenotype`.

##### Algorithm

Automatic rigging: bone influence weights for each generated phenotype are solved by heat diffusion across the mesh surface from seed points at each joint — heat "flows" from a bone's seed point outward, and the resulting steady-state temperature at each vertex becomes its skin weight, which handles irregular/mutated topology (extra mass, missing limbs) more robustly than distance-based weighting. Vocalization: each phenotype's base groan/scream audio clip has its pitch and formants shifted via Pitch-Synchronous Overlap-Add (PSOLA) according to the phenotype's mass and vocal-tract-length parameters from `[M5.1-EXT-02]`'s generation pipeline, so a heavy zombie sounds bass-shifted and a child-scaled infected sounds pitched up from the same source clips instead of needing per-phenotype voice recordings.

##### Examples

```cpp
// Solved once at phenotype-generation time, not per frame; converged weights are cached on the mesh
void SolveHeatDiffusionWeights(Mesh& mesh, std::span<const JointSeed> joints, int iterations, float kappa) {
    for (auto& joint : joints) {
        std::vector<float> h(mesh.vertexCount, 0.0f);
        h[joint.seedVertexIndex] = 1.0f; // fixed boundary condition at the joint's seed point
        for (int i = 0; i < iterations; ++i)
            h = DiffuseLaplacian(mesh, h, kappa); // one explicit ∂h/∂t = κ∇²h step
        mesh.AssignBoneWeightChannel(joint.boneId, h);
    }
    mesh.NormalizeWeightsAcrossJoints();
}
```

##### Failure Modes

Heat diffusion unstable. PSOLA pitch drift. Rigging NaN.

##### Player-Facing Impact

Every generated phenotype — however unusual its proportions — gets correctly weighted deformation and a vocalization that matches its size, without hand-rigging or recording a unique voice per body type.
---

#### [M5-EXT-72] Screamer Convergence & Holling Type II Cannibalism Feeding
> **tags** · m5.1, system
> **tl;dr** · Screamer Convergence & Holling Type II Cannibalism Feeding — M5-EXT-72.
> **ctx** · M5.1-EXT-06 — M5.1 procedural zombie variation.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Declares the base "Line-of-Sight Screamer Convergence. Holling Type II Cannibalism Feeding" bullet. Reads horde positions from M5.4's AI Director and gates on M5.3's line-of-sight perception queries; feeds back into M5.4's threat pacing since a large convergence event raises local threat.

##### Math

Cannibalism feeding rate follows a Holling Type II functional response — feeding rate saturates rather than scaling linearly with corpse density: `f(N) = (a·N) / (1 + a·h·N)`, where `N` is nearby corpse count, `a` is the attack/discovery rate, and `h` is handling time per corpse.

##### Algorithm

A "screamer" phenotype that spots the player emits a convergence call; every zombie with unobstructed line-of-sight to the screamer (checked via M5.3's perception system) paths toward its position rather than the player directly, pulling the horde together before they re-acquire the player individually. Idle zombies near corpse piles roll into cannibalism feeding using the Holling Type II curve above — at low corpse density every corpse gets fed on quickly, but as density rises, handling time bottlenecks the rate so feeding doesn't scale unrealistically with a huge pile.

##### Examples

```cpp
// Holling Type II feeding rate: saturates rather than scaling linearly with corpse density
float ComputeFeedingRate(float corpseDensity, float attackRate, float handlingTime) {
    return (attackRate * corpseDensity) / (1.0f + attackRate * handlingTime * corpseDensity);
}
```

##### Failure Modes

Screamer too aggressive. Cannibalism threshold wrong.

##### Player-Facing Impact

Screams visibly and audibly marshal scattered zombies into a converging horde rather than a diffuse trickle, and corpse piles create believable feeding lulls — a useful, readable distraction mechanic — instead of an infinite instant-consume sink.
---

#### [M5-EXT-73] Optimal Reciprocal Collision Avoidance (ORCA) Solver
> **tags** · m5.1, system
> **tl;dr** · Optimal Reciprocal Collision Avoidance (ORCA) Solver — M5-EXT-73.
> **ctx** · M5.1-EXT-07 — M5.1 procedural zombie variation.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched
M5.1/M5.3 + M5.4. Local collision-avoidance velocity for agents (companions, animals, crowds).

##### Math
v = ORCA(agent, neighbors, goal); each agent picks velocity in its free half-plane, reciprocal.

##### Algorithm
Each dynamic agent computes a velocity avoiding collision with neighbors while moving to its goal, using the ORCA reciprocal formulation (neighbors do the same, so avoidance is shared). Drives companion/animal/crowd navigation.

##### Examples
```cpp
vec3 v = ORCA.Solve(pos, vel, goal, neighbors);
```

##### Failure Modes

ORCA collision. Half-plane NaN. Reciprocity violated.

##### Player-Facing Impact
Allies/animals/crowds navigate around each other smoothly - no overlap or jitter.

##### Systems Touched

Declares the base "Line-of-Sight Screamer Convergence. Holling Type II Cannibalism Feeding" bullet. Reads horde positions from M5.4's AI Director and gates on M5.3's line-of-sight perception queries; feeds back into M5.4's threat pacing since a large convergence event raises local threat.

##### Math

Cannibalism feeding rate follows a Holling Type II functional response — feeding rate saturates rather than scaling linearly with corpse density: `f(N) = (a·N) / (1 + a·h·N)`, where `N` is nearby corpse count, `a` is the attack/discovery rate, and `h` is handling time per corpse.

##### Algorithm

A "screamer" phenotype that spots the player emits a convergence call; every zombie with unobstructed line-of-sight to the screamer (checked via M5.3's perception system) paths toward its position rather than the player directly, pulling the horde together before they re-acquire the player individually. Idle zombies near corpse piles roll into cannibalism feeding using the Holling Type II curve above — at low corpse density every corpse gets fed on quickly, but as density rises, handling time bottlenecks the rate so feeding doesn't scale unrealistically with a huge pile.

##### Examples

```cpp
// Holling Type II feeding rate: saturates rather than scaling linearly with corpse density
float ComputeFeedingRate(float corpseDensity, float attackRate, float handlingTime) {
    return (attackRate * corpseDensity) / (1.0f + attackRate * handlingTime * corpseDensity);
}
```

##### Failure Modes

ORCA collision. Half-plane NaN. Reciprocity violated.

##### Player-Facing Impact

Screams visibly and audibly marshal scattered zombies into a converging horde rather than a diffuse trickle, and corpse piles create believable feeding lulls — a useful, readable distraction mechanic — instead of an infinite instant-consume sink.

## M5.2 — Procedural animation (physics/IK-driven)

##### Implementation Steps

* IK-Driven Locomotion Solver (parametric foot trajectory).
* Proportional Active Ragdoll Dynamics via Jolt soft-body muscles and PD motors.
* Gyroscopic Balance Control. Grounded Foot-Slip Correction.
* XPBD Cloth Simulation with wind-vector coupling. Secondary motion jiggle oscillators.
* Separable Subsurface Scattering Wounds.
* Approximate-Nearest-Neighbor Motion Matching (`caps.annMotionMatching`). Projective Dynamics cloth (`caps.projectiveDynamicsCloth`).

##### Decisions to flag

Zero-clip vs minimal baked reference poses.

##### Exit Criteria

* Locomotion resolves via IK seamlessly.
* Hit reactions flow through PD motors accurately.



### Extended Systems Library — engine-side additions for M5.2

*Systems proposed in later design-iteration sessions, folded in here at their correct pipeline destination. IDs are stable — reference them (e.g. `M9-EXT-03`) when discussing this doc with the coding agent so everyone means the same system.*
---

#### [M5-EXT-74] Undergrowth Occlusion
> **tags** · m5.1, system
> **tl;dr** · Undergrowth Occlusion — M5-EXT-74.
> **ctx** · M5.1-EXT-08 — M5.1 procedural zombie variation.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

M5.1 vegetation/foliage rendering (the instanced grass/undergrowth this attenuates the view through), M4.5 primary depth + `[M4.5-EXT-13]` cluster depth-bounds (depth source for the occlusion test), M1 `SpatialHash` / M4 Voronoi cell (per-cell density of undergrowth).

##### Math

Light (and therefore visibility) through a slab of undergrowth of optical depth `τ` follows **Beer–Lambert exponential extinction** — the transmittance decays as a negative exponential of accumulated density, *not* a log-normal distribution:

`T(d) = e^{−σ·d}`,  where `d` is the path length through the undergrowth and `σ` is its per-unit-length extinction coefficient (derived from the per-cell foliage density).

The occlusion factor applied to a target behind the slab is `O = 1 − T(d)`; at `d→0`, `T→1` (fully visible), and as `d` grows, `T` decays exponentially toward 0 (fully occluded). This is a first-principles exponential-decay law, not a statistical log-normal shape.

##### Algorithm

Dense grass, ferns, and low scrub between the camera and a target attenuate the player's sightline. The system samples the undergrowth density along the camera→target ray (from the per-cell density already tracked in the M4 Voronoi partition / M1 `SpatialHash`), integrates it into an optical depth `d`, and applies the Beer–Lambert transmittance `e^{−σ·d}` to the target's visibility/rendering weight. Thin fringes (`d` small) barely dim the target; a deep wall of foliage (`d` large) exponentially kills the sightline, which is the physically correct behavior — a log-normal curve would put the bulk of the probability mass in the wrong place and mis-model how density actually accumulates along a path. The depth test reuses M4.5's existing depth buffers so no new render target is allocated, and the density lookup rides the same spatial partition the rest of the world streams on. Because transmittance is a smooth exponential, the occlusion fades naturally with distance through the foliage rather than popping at a hard threshold.

##### Examples

```cpp
// Beer-Lambert exponential extinction through undergrowth. T(d) = e^(-sigma*d); NOT log-normal.
// d = integrated foliage optical depth along the camera->target ray; sigma from per-cell density.
float UndergrowthOcclusion(float opticalDepth, float sigma) {
    float d = opticalDepth * sigma;          // accumulated extinction along the ray
    return 1.0f - std::exp(-d);              // O = 1 - T(d): occlusion factor in [0,1]
}
```

##### Failure Modes

Occlusion depth mismatch. Density wrong. View block stale.

##### Player-Facing Impact

Dense undergrowth genuinely hides zombies and breaks line-of-sight the way real foliage does — a target wades into tall grass and fades out smoothly as the density along your sightline accumulates, rather than vanishing at a hard distance or flickering. It makes cover-reading and ambush tactics around vegetation believable, and rewards using terrain instead of treating grass as decorative.


---
#### [M5-EXT-75] Procedural Joint Compliance Degradation (Dynamic Muscle Crippling)
> **tags** · m5.2, system
> **tl;dr** · Procedural Joint Compliance Degradation (Dynamic Muscle Crippling) — M5-EXT-75.
> **ctx** · M5.2-EXT-1 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Expands M5.2 (Procedural Animation / Active Ragdolls) and interfaces with M2.9 Ballistics.

##### Math

The Proportional-Derivative (PD) motor torque equations governing your active ragdoll skeleton joints are modified at runtime based on localized bone kinetic energy dissipation tensors:

`T_motor = K_p·(θ_target − θ_current) + K_d·(ω_target − ω_current)`

When a specific limb segment sustains an entry from a non-penetrating ballistic hit, instead of applying a static lurch animation, the engine scales down the joint tracking gains exponentially over accumulated tissue damage metrics:

`K_p ←K_p ⋅e^(−α⋅∑D_limb)`

`,K_d ←K_d ⋅e^(−α⋅∑D_limb)`

Explicit PD (evaluating force from the current frame's error only) is unstable on a heavy phenotype combined with a large `Δt` (frame hitch) or stiff `K_p` — it can overshoot and oscillate without bound. Reference Implementation below uses **Stable PD** (implicit) instead: it solves for next-step velocity before applying force, damping high-gain resonance regardless of frame-time — the technique from Tan, Liu & Turk's "Stable Proportional-Derivative Controllers" (2011), standard for ragdoll/character joint control.

##### Examples

```cpp
// Implicit "Stable PD" form (Tan et al. 2011) — solves for next-step torque so high joint
// stiffness or a large frame-time delta damps out instead of overshooting into a stretched/exploded joint.
void CalculateStableJointTorque(float theta_target, float theta_current, float omega_target, float omega_current,
                                 float k_p, float k_d, float dt, float& out_torque) {
    float position_error = theta_target - theta_current;
    float velocity_error = omega_target - omega_current;
    float implicit_damping = 1.0f + (dt * k_d) + (dt * dt * k_p);
    out_torque = (k_p * position_error + k_d * velocity_error) / implicit_damping;
}
```

##### Algorithm

This cleanly replaces hardcoded injury animations with genuine physics-driven impairment. Your active ragdolls continually try to match their procedural locomotion target poses. By dropping the motor stiffness (K_p ) and damping (K_d ) on a heavily damaged joint, the limb physically gives out under the zombie's own phenotype mass scale.

##### Failure Modes

Compliance multiplier NaN. Degradation not applied. Stiffness scale wrong. Rig instability.

##### Player-Facing Impact

Zombies don't just have a generic "limp" toggle. If you sweep a low-caliber weapon or a blunt melee object into a heavy zombie's right knee joint, that specific Jolt constraint goes soft. The zombie's leg will physically buck under its weight, lowering its hip center-of-mass, causing it to drag the limp leg realistically across dynamic rubble fields, or tumble forward completely if it tries to turn a sharp corner at high velocity.
---

#### [M5-EXT-76] Zombie Tissue Rigor Mortis Lifecycle Decay
> **tags** · m5.2, system
> **tl;dr** · Zombie Tissue Rigor Mortis Lifecycle Decay — M5-EXT-76.
> **ctx** · M5.2-EXT-2 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Intersects M5.1 zombie bio phenotypes with M5.2 Active Ragdoll joint tracking loops.

##### Math

The muscle flexibility index (χ) of a zombie transitions over biological timeline states (t_age ). Fresh infections display standard flexibility; recently turned corpses enter a high-stiffness phase, which eventually degrades into a flaccid, loose decay state:

`χ(t_age) = Clamp(e^(−α·t) + 2.5·(1.0−e^(−β·t))·e^(−γ·t), 0.05, 1.5)`

This flexibility coefficient scales the base motor stiffness variables (K_p) across all skeletal joint elements inside the active ragdoll controllers:

`K_p = K_p0 · χ(t_age)`

##### Algorithm

Your zombie entities hold timestamp values inside their EnTT component sheets tracking their generation time. During your active ragdoll physics loop updates, the joint tracking motor gains sample this age-dependent modifier, shifting constraint limits dynamically without introducing specialized procedural animation code branches.

##### Examples

```cpp
void AdjustRigorStiffness(float age, float alpha, float beta, float gamma, float& k_p) {
    k_p *= std::clamp(std::exp(-alpha * age) + 2.5f * (1.0f - std::exp(-beta * age)) * std::exp(-gamma * age), 0.05f, 1.5f);
}
```

##### Failure Modes

Decay rate miscalibrated. Animation pops. LOD transition broken. Timeline drift.

##### Player-Facing Impact

Zombies generated from freshly killed survivors move with fluid dexterity. Hordes that have spent hours wandering in cold sectors enter a stiff, robotic rigor mortis phase, causing their walks to appear mechanical and making them clumsy over dynamic debris. Deeply decayed zombies display loose, hyper-extended joint reactions that cause their bodies to buck wildly under bullet impacts.
---

#### [M5-EXT-77] Biomechanical Joint Luxation Limits
> **tags** · m5.2, system
> **tl;dr** · Biomechanical Joint Luxation Limits — M5-EXT-77.
> **ctx** · M5.2-EXT-3 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Deepens M5.2 (Active Ragdolls) and interfaces with Jolt constraint friction callbacks.

##### Math

Instead of skeletal constraints possessing binary breaks, joint limits experience structural luxation (dislocation) when the torque loads (τ_joint) surpass a dynamic tendon structural yield limit (T_yield):

`If ∥τ_joint∥ > T_yield ⟹ θ_limit_max = θ_limit_max + γ·(∥τ_joint∥ − T_yield)`

This structural displacement warps your target physics limits permanently, inducing a persistent mechanical structural offset in the ragdoll assembly.

##### Algorithm

Your active ragdoll limits are driven by Jolt's native joint constraint systems. This field listens to the impulse manifold data returned by the physics step. If an impact load crosses the structural boundary, the script uses SetLimits directly on the Jolt constraint handles to warp the joint bounds inline, bypassing the need to destroy or detach the entire joint assembly.

##### Examples

```cpp
void CheckJointLuxation(float torque, float yield_t, float gamma, float& limit_max) {
    if (torque > yield_t) limit_max += gamma * (torque - yield_t);
}
```

##### Failure Modes

Luxation limit too low. Joint dislocates on minor hits.

##### Player-Facing Impact

Dropping a heavy zombie off a multi-story building ledge onto concrete doesn't always break the leg off entirely. If the forces hit a specific threshold, the hip or knee joint experiences realistic dislocation. The active ragdoll constraint warps permanently, causing the limb to drag backward at an unnatural angle while the entity struggles to balance its weight during locomotion loops.
---

#### [M5-EXT-78] Centrifugal Kinetic Limb Flail
> **tags** · m5.2, system
> **tl;dr** · Centrifugal Kinetic Limb Flail — M5-EXT-78.
> **ctx** · M5.2-EXT-4 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Expands M5.2 active ragdoll constraints using Jolt contact impulse manifolds.

##### Math

When a zombie's joint compliance drops or experiences partial structural failure, the limb is not lazily deleted. It acts as an unconstrained chaotic double pendulum. The rotational asymmetry generates a dynamic centrifugal angular torque vector (τ_flail ) during locomotion swings:

`τ_flail =m_limb ⋅(ω_torso ×(ω_torso ×r_limb ))`

##### Algorithm

When a limb's joint compliance is loosened/broken, it stops following the animated/ragdoll blend and free-swings as an unconstrained double-pendulum segment. Torso rotation drives a centrifugal torque that whips the limb outward; contact impulses apply through the existing Jolt manifold like any rigid-body hit.

##### Examples

```cpp
void ComputeLimbFlail(float mass, const glm::vec3& w, const glm::vec3& r, glm::vec3& out_torque) {
    out_torque = mass * glm::cross(w, glm::cross(w, r));
}
```

##### Failure Modes

Flail physics unbounded. Velocity explosion. Limb clipping.

##### Player-Facing Impact

Damaging a heavy zombie's arm or shoulder joint causes the limb to swing wildly like a heavy canvas sack as it charges forward. This chaotic kinetic momentum turns the limb into a physical flail that accidentally smashes into and trips neighboring zombies in the crowd, initiating chain-reaction stumble cascades across the herd.
---

#### [M5-EXT-79] Control-Rig-Style Procedural Pose Override Layer
> **tags** · m5.2, system
> **tl;dr** · Control-Rig-Style Procedural Pose Override Layer — M5-EXT-79.
> **ctx** · M5.2-EXT-5 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Sits downstream of `caps.annMotionMatching` (M5.2 above) in the per-frame pose pipeline. Intended chain: Motion Matching → Upper-Body Override (`[K-EXT-01]`, Appendix K) → Procedural Rig (this layer) → Physics Post-Process → Final Pose. This layer consumes Motion Matching's output pose directly, further overridden by `[K-EXT-01]`'s spine-root blend before it reaches this rig layer.

##### Math

A per-joint override is a direct pose substitution: `pose_joint = Lerp(pose_MM, pose_procedural, weight)`, weight supplied per-joint by the Constraint-Weight Blending layer (M5.2-EXT-06).

##### Algorithm

A new post-solve stage that runs after Motion Matching produces its base pose and before the existing Active Ragdoll / PD-motor layer (M5.2 above) takes over for physically-driven joints. Bone transforms coming out of Motion Matching can be overridden per-joint by procedural solvers (aim-offsets, look-at, foot placement corrections) rather than requiring a new baked clip for every situational variant.

##### Examples

```cpp
// Applied per-joint after Motion Matching resolves the base pose, before ragdoll blending
Transform ApplyProceduralOverride(const Transform& mm_pose, const Transform& procedural_pose, float weight) {
    return Transform::Lerp(mm_pose, procedural_pose, weight); // weight from constraint-weight blending, EXT-06
}
```

##### Failure Modes

Override layer not blending. Pose pops. IK conflict.

##### Player-Facing Impact

Zombies and companions can aim, look, or reach at targets that Motion Matching's clip library was never authored for, without a visible pop back to a baked animation.
---

#### [M5-EXT-80] Constraint-Weight Blending (Animated ↔ Procedural)
> **tags** · m5.2, system
> **tl;dr** · Constraint-Weight Blending (Animated ↔ Procedural) — M5-EXT-80.
> **ctx** · M5.2-EXT-6 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Per-joint blend weight consumed by the procedural override layer (M5.2-EXT-05) above.

##### Math

A single scalar per constrained joint drives the blend between the Motion Matching pose and the procedural solve:

`pose_final = pose_animated · (1 − w) + pose_procedural · w`, `w ∈ [0, 1]`

`w = 0` is pure Motion Matching output; `w = 1` is fully procedural (e.g. a hard IK foot-lock or aim solve). Intermediate values let a solver fade in/out over several frames instead of snapping.

##### Algorithm

Every procedural solver in M5.2-EXT-05 (foot IK, aim IK, look-at, etc.) writes its result as a candidate pose plus a target blend weight rather than overwriting the joint transform outright. Each tick, the weight is stepped toward its target (a simple rate-limited lerp, not an instant snap) and `pose_final` is recomputed from the current animated pose and the procedural candidate. This keeps every procedural override going through one shared blend point instead of each solver having its own ad hoc fade logic.

##### Examples

```cpp
float w = std::clamp(blend_weight, 0.0f, 1.0f);
Transform pose_final = Transform::Lerp(pose_animated, pose_procedural, w);
```

##### Failure Modes

Threshold not detected. Override never triggers.

##### Player-Facing Impact

Procedural corrections (foot placement on stairs, reaching for a door handle) fade in smoothly rather than snapping the limb into place, avoiding the "robotic pop" look of hard IK switches.
---

#### [M5-EXT-81] Ragdoll-to-Animation Recovery Blend
> **tags** · m5.2, system
> **tl;dr** · Ragdoll-to-Animation Recovery Blend — M5-EXT-81.
> **ctx** · M5.2-EXT-7 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Bridges M5.2's Active Ragdoll Dynamics (existing bullet above) back to Motion Matching once a knocked-down entity is ready to stand.

##### Math

A 10–15 frame crossfade blends the ragdoll's final settled pose into an orientation-matched get-up clip:

`pose_blend(t) = pose_ragdoll · (1 − α(t)) + pose_getup_clip · α(t)`, `α(t) = t / N`, `t ∈ [0, N]`, `N ∈ [10, 15]` frames

The get-up clip is selected/oriented to match the ragdoll's final yaw and prone/supine state so the crossfade doesn't visibly rotate the body mid-blend.

##### Algorithm

Once a ragdolled entity's Jolt bodies settle (velocity below threshold for N consecutive frames), the system snapshots the ragdoll's final per-bone world pose and picks a get-up animation clip matching that orientation (prone-face-down, prone-face-up, or on-side, each with its own clip). The crossfade then runs for a fixed 10–15 frame window, blending bone-by-bone from the snapshotted ragdoll pose toward the clip's pose at each frame. Once `alpha` reaches 1, control hands off fully to Motion Matching and the ragdoll bodies are released from the physics world.

##### Examples

```cpp
float alpha = std::clamp(float(frame) / float(crossfadeFrames), 0.0f, 1.0f); // crossfadeFrames in [10,15]
Transform pose = Transform::Lerp(ragdoll_final_pose, getup_clip_pose, alpha);
```

##### Failure Modes

Blend weights NaN. Transition pop. Animation freeze.

##### Player-Facing Impact

A zombie or companion knocked down by an explosion or vehicle impact doesn't teleport back into a standing idle pose — it settles where physics put it, then rises through a matched get-up animation from that exact position and orientation.
---

#### [M5-EXT-82] IK Rig Asset (Per-Skeleton Bone-Chain / End-Effector Definition)
> **tags** · m5.2, system
> **tl;dr** · IK Rig Asset (Per-Skeleton Bone-Chain / End-Effector Definition) — M5-EXT-82.
> **ctx** · M5.2-EXT-8 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Consumed by the procedural override layer (M5.2-EXT-05) and motion-warping (M5.2-EXT-09) below, for any feature that needs to resolve an end-effector target (foot, hand, weapon muzzle) back through a bone chain.

##### Math

No formula — a declarative bone-chain graph per skeleton: `chain = [root_bone, ..., end_effector_bone]` plus a pole-vector hint per chain.

##### Algorithm

A one-time, per-skeleton data asset declaring bone chains (root→end-effector) and pole-vector hints, authored once per unique skeleton (base zombie, companion, phenotype variants sharing a skeleton) rather than re-declared per-feature.

##### Examples

```cpp
struct IKChain { std::vector<BoneIndex> bones; glm::vec3 poleVectorHint; };
struct IKRigAsset { std::string skeletonId; std::vector<IKChain> chains; }; // variable count per skeleton — do not hardcode a fixed chain count; phenotype variants may need more than the standard 4 human-limb chains
```

No IK Rig asset format or loader exists elsewhere in this doc — it's a genuinely new data asset type and file format, not a reuse of glTF geometry caching (M2.6) or prefab loading (M1), which load meshes/scenes, not bone-chain/end-effector metadata. The format needs to be designed before M5.2-EXT-05/09 can consume it.

##### Failure Modes

Spline not aligned. Limb stretch. IK fail.

##### Player-Facing Impact

None directly — this is the authoring/data layer other procedural-animation features above depend on to know which bones form "the left arm" or "the right foot" without hardcoding bone-name strings per skeleton.
---

#### [M5-EXT-83] Motion-Warping for Target Alignment
> **tags** · m5.2, system
> **tl;dr** · Motion-Warping for Target Alignment — M5-EXT-83.
> **ctx** · M5.2-EXT-9 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Consumes the IK Rig asset (M5.2-EXT-08, flagged above) and blends through the procedural override layer (M5.2-EXT-05) for melee-hit alignment, vault-landing alignment (M2.7's vault fan-cast), and door-interaction alignment.

##### Math

The root motion offset for a clip is warped toward a target transform over the clip's duration using the same normalized-time blend shape as the recovery crossfade above:

`offset(t) = offset_clip(t) + (target − offset_clip(1)) · β(t)`, `β(t)` a normalized ease-in-ease-out curve over `t ∈ [0, 1]` (clip-relative time), so the warp is zero at the clip start and fully resolved at the clip's designed contact frame.

##### Algorithm

During a vault, melee hit, or door interaction, the resolved contact point becomes the warp target. The root-motion offset blends from its authored path toward that target using an ease curve reaching full alignment at the clip's designed contact frame, so hands/feet land precisely regardless of approach angle.

##### Examples

```cpp
glm::vec3 WarpRootOffset(glm::vec3 clip_offset, glm::vec3 clip_end_offset, glm::vec3 target, float beta) {
    return clip_offset + (target - clip_end_offset) * beta; // beta from an authored ease curve, not linear t
}
```

##### Failure Modes

Damage scaling wrong. No visual change.

##### Player-Facing Impact

Melee hits actually connect with the zombie's hitbox instead of swinging through empty air a foot off-target, and vault/door clips land hands and feet precisely on the ledge or handle regardless of the player's exact approach angle.
---

#### [M5-EXT-84] Physics-Based Secondary Motion Post-Process (Spring-Damper Attachments)
> **tags** · m5.2, system
> **tl;dr** · Physics-Based Secondary Motion Post-Process (Spring-Damper Attachments) — M5-EXT-84.
> **ctx** · M5.2-EXT-10 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Runs as the final stage of the pose pipeline — after Motion Matching, the procedural override layer (M5.2-EXT-05), and ragdoll blending — for non-simulated attachment points (capes, straps, loose gear). Explicitly separate from the existing XPBD Cloth Simulation (M5.2 above), which owns actual cloth meshes; this is a cheap per-point spring for rigid/semi-rigid dangling attachments that don't warrant a full cloth solve.

##### Math

A critically-damped spring-damper per attachment point, driven by its parent bone's motion:

`x'' = −k(x − x_target)/m − c·x'`, critically damped at `c = 2√(km)`

where `x` is the attachment's current offset from its rest position, `x_target` is the rest position driven by the parent bone, `k` the spring stiffness, `m` the point mass, and `c` the damping coefficient. Critical damping is chosen specifically so the attachment settles to rest without oscillating past its target — appropriate for gear that should sway and stop, not bounce indefinitely.

##### Algorithm

Each tracked attachment point runs its own critically-damped spring each tick, pulled toward a rest position driven by its parent bone. Critical damping (`c = 2√(km)`) means it settles without overshoot or jitter.

##### Examples

```cpp
void UpdateSpringDamper(glm::vec3& x, glm::vec3& v, const glm::vec3& x_target, float k, float m, float dt) {
    float c = 2.0f * std::sqrt(k * m); // critical damping
    glm::vec3 accel = (-k * (x - x_target) - c * v) / m;
    v += accel * dt;
    x += v * dt;
}
```

##### Failure Modes

Splat threshold too low. Excessive gore.

##### Player-Facing Impact

Capes, ammo straps, and loose gear sway and settle naturally with a zombie's or companion's movement instead of rigidly following the parent bone or requiring a full cloth-sim pass for something that small.
---

#### [M5-EXT-85] Asynchronous Fixed-Block Humanoid Rig Matrix Pool Allocator
> **tags** · m5.2, system
> **tl;dr** · Asynchronous Fixed-Block Humanoid Rig Matrix Pool Allocator — M5-EXT-85.
> **ctx** · M5.2-EXT-11 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Backs skeletal bone-transform storage for every humanoid entity (zombies, companions, players) across M5.2's animation pipeline — eliminates heap allocation thrashing when hundreds of entities spawn/evict across chunk borders.

##### Math

`Stride_aligned = (BoneCount·64 + 63) & ~63` — cache-line-aligned stride sizing.

##### Algorithm

A flat memory arena of fixed-size, cache-aligned rig blocks is constructed once at boot, each holding up to 32 bone matrices. A simple free-list index chain hands out and reclaims blocks in O(1) with zero runtime heap traffic, matching the same alignment idiom `[M0-EXT-01]` and `[M1-EXT-07]` already use elsewhere in the doc for cache-line safety under enkiTS worker contention.

##### Examples

```cpp
struct alignas(64) HumanoidRigBlock {
    float boneMatrices[32][16]; // fixed 32-bone structural transform block
    uint32_t nextAvailableIndex;
};
struct RigFixedPoolArena {
    HumanoidRigBlock* memoryBlockBuffer{nullptr};
    uint32_t freeListCursorHead{0};
    inline HumanoidRigBlock* AllocateRigBlock(RigFixedPoolArena& pool) {
        HumanoidRigBlock* block = &pool.memoryBlockBuffer[pool.freeListCursorHead];
        pool.freeListCursorHead = block->nextAvailableIndex;
        return block;
    }
};
```

##### Failure Modes

Decay timer not synced. Animation mismatch.

##### Player-Facing Impact

Invisible directly — this is what keeps horde spawn/despawn waves at chunk borders from causing allocator-driven frame hitches.
---

#### [M5-EXT-86] Skeletal Joint Yield Torque Bone Fracturing Filter
> **tags** · m5.2, system
> **tl;dr** · Skeletal Joint Yield Torque Bone Fracturing Filter — M5-EXT-86.
> **ctx** · M5.2-EXT-12 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched

Extends M5.2's Active Ragdoll Dynamics and `[M5.2-EXT-01]`'s Stable-PD joint torque model — detects structural joint failure after severe explosive or vehicle impacts and soft-kills the affected joint's motor rather than letting it stretch into a degenerate state.

##### Math

`S_shear = ‖τ‖ / r_bone_profile³`

`If S_shear ≥ σ_bone_fracture_limit ⟹ K_p → 0, K_d → 0` (motor deactivated)

##### Algorithm

Tracks per-joint transient torque impulse against a fracture shear limit defined per bone profile. Crossing the limit flags the joint as fractured and zeroes its Stable-PD gains (`[M5.2-EXT-01]`) immediately, so the limb goes fully passive/ragdoll at that joint instead of the PD motor fighting an already-destroyed constraint — this is the structural-failure counterpart to `[M5.2-EXT-01]`'s gradual compliance degradation, triggering on a hard threshold rather than accumulated damage.

##### Examples

```cpp
struct PhysicsJointMotorData {
    float transientTorqueImpulseNewtonSeconds{0.0f};
    float fractureShearLimit{1200.0f};
    bool isJointFractured{false};
};
inline void CheckBoneJointFracture(PhysicsJointMotorData& joint, float frameImpulseForce, float dt) {
    float transientStress = joint.transientTorqueImpulseNewtonSeconds + (frameImpulseForce / (dt + 1e-5f));
    if (transientStress > joint.fractureShearLimit) {
        joint.isJointFractured = true; // strips active PD muscle torque gains this frame
    }
}
```

##### Failure Modes

Rigor force wrong. Body floats. Collapse wrong.

##### Player-Facing Impact

A zombie hit by an explosion or vehicle impact snaps limbs convincingly at the moment of overload instead of stretching into a rubber-jointed glitch — the ragdoll reads as broken, not buggy.



---

## Appendix Cross-References for M5.2

*The following items from the doc's Appendices/§5.x Addenda explicitly reference M5.2 in their "Systems Touched" or scope. Auto-matched by milestone ID — verify relevance before feeding to Antigravity.*

### [Appendix K] [K-EXT-01] Skeletal Upper-Body Action-Layering Override (fleshes §5.8 "Upper-Body Animation Layer Override")


**Systems Touched:** M5.2 procedural animation pipeline, M2.7 combat/reload state machine. Slots directly into the pose chain M5.2 already documents but left open: Motion Matching → **Upper-Body Override (this)** → Procedural Rig → Physics Post-Process → Final Pose.

**How It Works:** A spine-root bone index splits the skeleton into a lower-body half (owned by Motion Matching's locomotion output) and an upper-body half (owned by whatever action state — reload, aim, melee swing — is currently active). Every tick, bones at and above the spine root are lerped from the base locomotion pose toward the action pose by a per-state blend weight; bones below stay untouched, so sprinting legs never freeze when the player reloads.

**Reference Implementation**

```cpp
// Blends upper-body action transforms over lower-body Motion-Matching output via an explicit spine-root bone index.
void ApplyUpperBodyOverride(std::span<Transform> basePose, std::span<const Transform> actionPose,
                             float weight, uint32_t spineRootIdx) {
    for (size_t i = spineRootIdx; i < basePose.size(); ++i)
        basePose[i] = Transform::Lerp(basePose[i], actionPose[i], weight);
}
```

`weight` ramps 0→1 over ~0.15s on action entry/exit (critically-damped spring, reusing M5.2's existing spring-damper convention) rather than snapping, so transitions don't pop.

**Player-Facing Impact:** Entities reload, aim, and melee while sprinting/strafing without needing a pre-baked clip for every locomotion × action combination.

---



### [Appendix M] [M5.2-EXT-13] Kinematic Character Ledge-Locking Coordinate Re-Basing Matrix


**Systems Touched:** `JPH::CharacterVirtual`, M2.7 player controller, M3 structural graphs, M2.6 open-world foundations.

**How It Works:** While anchored to a moving platform (vehicle bed, collapsing floor section), the character's position is tracked as a local double-precision offset from the platform's root transform rather than in absolute world space, then converted to camera-relative single precision only at the final draw step. This is the standard fix for jitter/sinking on moving platforms in large open worlds using floating-origin techniques — directly useful since you already have double-precision world space and moving structural pieces.

**Player-Facing Impact:** No sliding/jitter while standing on a moving vehicle or a partially-collapsed floor.

---





---

## Milestone Sequencing

**Current milestone:** M5.2

Do not proceed past this file's Exit Criteria until every item passes verification by actually running the build — per the doc's Engineering Audit, this is not a read-the-code-and-assume-it-works check.

**Once Exit Criteria pass:** load the next milestone file in sequence: `14_M5_3.md` (M5.3).

---
---

#### [M5-EXT-87] (provisional) IK Rig Metadata Serialization Loader *(SOURCED FROM PLAN FILE)*
> **tags** · m5.2, system
> **tl;dr** · (provisional) IK Rig Metadata Serialization Loader *(SOURCED FROM PLAN FILE)* — M5-EXT-87.
> **ctx** · M5.2-EXT-13 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched
Consumed by M5.2-EXT-08 (IK Rig Asset struct) + M5.2-EXT-09 (Motion-Warping target). Feeds every end-effector consumer in M5.2 (melee-hit align, vault landing, door interact). Distinct from M2.6 glTF/prefab loading (mesh/scene, not bone-chain+pole-vector metadata).

##### Math
Declarative asset: IKRigAsset{skeletonId, chains:[{boneIdx[], poleVectorHint}]}. Hashing for on-disk table reuses same FNV-1a keying idiom M2.6 uses for geometry cache (uint64_t key=FNV1a(path)).

##### Algorithm
Small JSON/flat-binary asset per skeleton, authored once, loaded at skeleton-registration. Loader parses bone-name->index against skeleton's own joint list (renamed bone fails loudly at load, not silently at runtime), stores resolved IKRigAsset in same hash-keyed table as geometry cache (key space skeletonId string). Phenotype variants reuse one asset; extra chains via override asset.

##### Examples
```cpp
struct IKChainDef { std::vector<uint16_t> boneIdx; glm::vec3 poleVectorHint; }; auto asset=LoadIKRig(skeletonId);
```

##### Failure Modes

Limb mass zero. Physics broken.

##### Player-Facing Impact
Skeletons load bone-chain defs from data, not hardcoded bone-name lookups — robust to renamed bones.

---
---

#### [M5-EXT-88] XPBD Rope/Tether Constraint *(SOURCED FROM PLAN FILE)*
> **tags** · m5.2, system
> **tl;dr** · XPBD Rope/Tether Constraint *(SOURCED FROM PLAN FILE)* — M5-EXT-88.
> **ctx** · M5.2-EXT-14 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched
Long-range-attachment (tether) constraint used internally for cloth/vegetation but not exposed; promotes to reusable XPBD distance/rope constraint. Consumes M5.2 solver + M5.2-EXT-08 IK/joint infra. Used by zipline/grappling/winch + drag-ragdoll.

##### Math
Extended PBD (Macklin 2016): each distance constraint has compliance a and accumulates Lagrange multiplier lambda per substep, stiffness independent of iteration count/timestep: Dx=(w1w2/(w1+w2+a~)) * C * nC, with a~=a/Dt^2. Rope = chain of N distance constraints solved with substep XPBD (e.g. 4 substeps, 1 iter each).

##### Algorithm
Build rope as small particle chain (positions+inverse masses), one XPBD distance constraint per segment. Each substep: predict, solve all constraints updating lambda, integrate. Two endpoints bind to entity handles (player hand, vehicle hitch). Persistent-corrected lambda: rope neither explodes nor sags through floors under load.

##### Examples
```cpp
void SolveDistanceXPBD(Particle& a, Particle& b, float rest, float alpha, float dt, float& lambda){ vec3 d=b.x-a.x; float C=length(d)-rest; vec3 n=d/max(length(d),1e-5f); float k=1.0f/(a.w+b.w+alpha/(dt*dt)); vec3 corr=-k*C*n; a.x-=a.w*corr; b.x+=b.w*corr; lambda+=k*C; }
```

##### Failure Modes

Wound depth wrong. Clipping through body.

##### Player-Facing Impact
Ziplines/ropes/winches are stable and non-stretchy — no PBD explosion or floor-sag failure.

---
---

#### [M5-EXT-89] Kinematic Character Ledge-Locking Coordinate Re-Baser
> **tags** · m5.2, system
> **tl;dr** · Kinematic Character Ledge-Locking Coordinate Re-Baser — M5-EXT-89.
> **ctx** · M5.2-EXT-15 — M5.2 injury & degradation systems.
> **meta** · depends-on: — · depended-by: -

##### Systems Touched
M5.2 + M2.7. Re-bases character coords when ledge-locking so mantle/vault stays stable at world scale.

##### Math
local = worldPos - anchor; on lock store local; on unlock worldPos = anchor + local.

##### Algorithm
When a character ledge-locks (mantle/vault), its coordinates are re-based to the ledge anchor so the small-scale animation integration doesn't accumulate float error at km world scale. On release, coords re-base back to world. Prevents drift/snap at extreme coordinates.

##### Examples
```cpp
vec3 local = worldPos - anchor; // on lock; worldPos = anchor + local; on unlock
```

##### Failure Modes

Injury severity random. No consistency.

##### Player-Facing Impact
Vaults/mantles are stable everywhere on the map - no float jitter far from origin.
---


## Appendix Cross-References for M5.1

*The following items from the doc's Appendices/§5.x Addenda explicitly reference M5.1 in their "Systems Touched" or scope. Auto-matched by milestone ID — verify relevance before feeding to Antigravity.*

### [§5.x Addendum] 5.4 AI, Director, Propagation (M5.1 / M5.3 / M5.4)


**[M5.1-EXT-07] / [M5.3-EXT-XX] Optimal Reciprocal Collision Avoidance (ORCA) Solver**

Purpose: standard ORCA local avoidance so hordes don't clip/overlap at close range — pairs with your existing Reynolds flocking (subsystem #15 in Section 2) rather than replacing it; ORCA governs hard collision avoidance, boids govern soft group behavior.

**[M5.3-EXT-08] Visual Occlusion Sector Ray-March Pre-Filter**

Purpose: coarse macro-voxel LOS approximation to cheaply reject most raycasts before falling back to full-res traces for AI perception.

**[M5.4-EXT-04] Thompson-Sampling Actor Weights Snapshot Persistence Register** — canonical copy is under §5.1/M5.4 in the main body; see there. *(v75: was a byte-identical duplicate, no divergence found — collapsed to a stub for the same reason as `[M1-EXT-09]` above.)*

**[M5.4-EXT-05] Reaction-Diffusion Grid Sub-sampled Boundary Welder**

Purpose: blends the panic/fear field (subsystem #26-equivalent reaction-diffusion grid) across sector boundaries so AI panic propagation doesn't stall or discontinuity-snap at chunk edges.

*[M5.4-EXT-06] Hierarchical A Macro-Cell Long-Range Router**

Purpose: routes off-screen horde movement across unstreamed sectors on an abstract street/highway node graph, avoiding full pathfinding cost for entities the player can't see.

**[M5.4-EXT-07] Holling Type II Cannibalism Feeding Satiator**

Purpose: saturating consumption-rate curve binding idle zombies to corpse piles as a tactical distraction mechanic.





---

## Milestone Sequencing

**Current milestone:** M5.1

Do not proceed past this file's Exit Criteria until every item passes verification by actually running the build — per the doc's Engineering Audit, this is not a read-the-code-and-assume-it-works check.

**Once Exit Criteria pass:** load the next milestone file in sequence: `13_M5_2.md` (M5.2).
---


