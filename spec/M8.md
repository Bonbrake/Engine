---
title: M8 milestone spec
milestone: M8
name: settlement   # M8 — settlement
ext_blocks: 60
clusters: 5
cross_file_deps: 46
self_contained: true
index: see llms.txt or the <details> block index below
---

## M8 — Data-driven itemization


> **M8 quick-index (18 EXT blocks).** Read ONLY the block(s) you need — each is self-contained.

<details><summary>M8 block index (click to expand)</summary>

- `M8-EXT-01` Brand-Gated Thermal Ammo Cook-Off
- `M8-EXT-02` Property-Matching Crafting & Derived Item Stats
- `M8-EXT-03` Secondary Illness via Reparameterized SEIR (Sepsis, Dysentery, Tetanus)
- `M8-EXT-04` Per-Save Pathogen Characterization & Cure Synthesis (Deliberately Near-Impossible)
- `M8-EXT-05` Data-Table Modding/Workshop Exposure
- `M8-EXT-06` Two-Tier Deterministic Archetype Synthesis (Identity vs. Session Variance)
- `M8-EXT-07` Memory-Mapped FlatBinary Archetype Schema Serializer
- `M8-EXT-08` Log-Normal Camouflage Masking
- `M8-EXT-10` Procedural Loot Icon Generation
- `M8-EXT-09` Memory-Mapped FlatBinary Inventory Cache Dictionary
- `M8-EXT-11` Data-Driven Itemization
- `M8-EXT-12` Stacking & Inventory Slots
- `M8-EXT-13` Ammo Types & Crafting
- `M8-EXT-14` Barter & Trade Economy
- `M8-EXT-15` Currency & Scarcity Model
- `M8-EXT-16` Settlement NPC Residents
- `M8-EXT-17` Perk Tree & Prerequisites
- `M8-EXT-18` Skill Progression & XP

</details>

##### Implementation Steps

* JSON Layout Schema Validation for all items.
* **Brand/quality-tier schema** — read by M8-EXT-01 (cook-off) and M2.7-EXT-12 (jam) but never declared until now. Added to every weapon-family entry in the item JSON tables:

```json
{
  "id": "tommy_gun_m1928",
  "brandTier": "military_surplus",       // enum, see below
  "brandTierMultiplier": 0.4,            // float, [0.0, 2.0], default 1.0
  "brandQualityMultiplier": 0.6          // float, [0.0, 2.0], default 1.0
}
```

* `brandTier`: enum string, one of `military_surplus | commercial_licensed | improvised_zip_gun | black_market_reload`. Authored per-weapon-family (not per-instance) in `assets/items/weapons.json`.
* `brandTierMultiplier`: scales cook-off probability (M8-EXT-01) and jam probability (M2.7-EXT-12); lower is better-made. Military surplus/commercial licensed default to 0.4-0.7; improvised/black-market default to 1.2-2.0.
* `brandQualityMultiplier`: a second, independent scalar for per-instance wear-and-provenance variance (a specific looted rifle can be a rough or pristine example of its tier), rolled once at item-spawn time within a tier-specific range and then fixed for that instance's lifetime; stored on the item instance, not the family definition. Both multipliers compose multiplicatively wherever cook-off/jam math already reads `BrandTierMultiplier` (M8-EXT-01's formula becomes `... · GrimeFactor · brandTierMultiplier · brandQualityMultiplier`).
* Authoring location: `assets/items/weapons.json`, validated by the existing JSON Layout Schema Validation step above (add both fields to the schema).
* Volumetric Grid Inventory.
* Kinetic Lock-Busting Solver (`KE` vs Jolt material shear).
* Personal Compartmental SEIR Infection.
* Log-Normal Camouflage Masking.

##### Decisions to flag

Scarcity queue constants. Whether `brandQualityMultiplier`'s per-instance roll range should itself be exposed in JSON per tier (recommended: yes, e.g. `"qualityRollRange": [0.5, 0.8]` for `military_surplus`) rather than hardcoded in C++.

##### Exit Criteria

* JSON manipulation adds modular items seamlessly without C++ modification.
* Improvised tools map mathematically to mass and geometry bounds.


### Extended Systems Library — engine-side additions for M8

*Systems proposed in later design-iteration sessions, folded in here at their correct pipeline destination. IDs are stable — reference them (e.g. `M9-EXT-03`) when discussing this doc with the coding agent so everyone means the same system.*



#### [M8-EXT-01] Brand-Gated Thermal Ammo Cook-Off

> **tags** · general
> **tl;dr** · Reads the canonical BarrelHeat struct (locked in M2.7's base Implementation Steps) — specifically kelvin and grimeFactor — and intersects with your M8 JSON item
> **ctx** · Brand-Gated Thermal Ammo Cook-Off -- Reads the canonical BarrelHeat struct (locked in M2.7's base Implementation Steps) — specifically kelvin and grimeFactor — and intersects with your M8 JSON item tables for BrandTierMultiplier.
> **meta** · depends-on: [M8-EXT-01] · depended-by: -

##### Systems Touched
Reads the canonical `BarrelHeat` struct (locked in M2.7's base Implementation Steps) — specifically `kelvin` and `grimeFactor` — and intersects with your M8 JSON item tables for `BrandTierMultiplier`.

##### Math
Chamber ammo cooking off is a rare, extreme mechanical failure gate. The calculation evaluates to zero unless your barrel temperature (`BarrelHeat.kelvin`) passes maximum physical operational limits (**T_fail = 473.15K / 200°C**, **T_melt = 533.15K / 260°C**), scaling up strictly if using low-grade weapon brands or ignoring weapon cleaning loops. *(Corrected from the doc's earlier ≈450°C figure, which overshot the real physical process by more than 2x: published cook-off literature — chambered 7.62mm cartridge testing, nitrocellulose's ~160-170°C autoignition point, and the commonly-cited 149-204°C/300-400°F "cooks off" range for chambered ammo — puts real-world onset around 150-200°C, not 450°C. T_fail=200°C sits at the top of that real range (erring toward "less trigger-happy," appropriate for a rare failure gate); T_melt=260°C is the upper bound cited for guaranteed cook-off, so P_cook saturates to 1.0 across a plausible 60°C band instead of an arbitrary one.)*

`P_cook =Clamp( (T_barrel −T_fail) / (T_melt −T_fail) ,0.0,1.0)⋅GrimeFactor⋅BrandTierMultiplier⋅BrandQualityMultiplier`

##### Algorithm
Every fire cycle checks `BarrelHeat.kelvin` against the fixed failure temperature; below it, probability is always zero. Above it, the probability formula scales with grime and brand-tier/quality multipliers, and a failed roll empties the magazine uncontrolled.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
High-tier tactical weapons or military-grade components are heavily insulated and will never cook off ammunition under normal conditions. However, if you neglect weapon care or push a cheap makeshift zip-gun into sustained automatic fire, the receiver heat reaches critical boundaries. The weapon enters a brief, terrifying runaway loop that automatically empties the rest of the magazine, forcing you to ride out the recoil until the weapon clicks empty.

#### [M8-EXT-02] Property-Matching Crafting & Derived Item Stats

> **tags** · general
> **tl;dr** · Reads the same material/shape/mass tags already required on every scavenged item for physics. Declares the canonical CraftingStationComponent — the workbench-ro
> **ctx** · Property-Matching Crafting & Derived Item Stats -- Reads the same material/shape/mass tags already required on every scavenged item for physics. Declares the canonical CraftingStationComponent — the workbench-role entity later shared with [M9-EXT-16]'s garages (repair-role).
> **meta** · depends-on: [M8-EXT-02], [M9-EXT-16] · depended-by: -

##### Systems Touched
Reads the same material/shape/mass tags already required on every scavenged item for physics. Declares the canonical `CraftingStationComponent` — the workbench-role entity later shared with [M9-EXT-16]'s garages (repair-role).

##### Math
No recipe table. A combination is valid if tag compatibility passes, and derived stats are computed, not looked up:

`Damage_derived = k_edge·Sharpness + k_mass·Mass·HandleLeverage`

`Durability_derived = N_e·(S_e/S_material)^m` — the same Miner's-rule exponent/constants M3-EXT-01 uses, reused via a dedicated small helper (`GetFatigueLifeCycles`) that wraps those constants rather than duplicating them.

##### Algorithm
Every item already carries physics tags (mass, material, is-it-bladed) because your bullet penetration and ragdoll mass-scaling need them anyway. Crafting queries two items' tag sets against a small compatibility ruleset (rigid-handle + bladed-edge = melee weapon, container + absorbent = filter, etc.) and computes output stats from the input tags directly — adding a new craftable combination later means adding a compatibility rule, not authoring a new item.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
There's no recipe book to memorize — if it physically makes sense (a kitchen knife duct-taped to a mop handle), the system will let you build it and compute believable stats, consistent with the world having zero hand-authored content anywhere else.

#### [M8-EXT-03] Secondary Illness via Reparameterized SEIR (Sepsis, Dysentery, Tetanus)

> **tags** · SEIR
> **tl;dr** · Reuses M8's existing Personal Compartmental SEIR Infection engine (the same one modeling zombie-bite infection) with different R0/incubation/mortality constants
> **ctx** · Secondary Illness via Reparameterized SEIR (Sepsis, Dysentery, Tetanus) -- Reuses M8's existing Personal Compartmental SEIR Infection engine (the same one modeling zombie-bite infection) with different R0/incubation/mortality constants — not a new illness system. Fed by untreated wounds (M2.9 damage events), contaminated food ([M7-EXT-05]), and contaminated water ([M8-EXT-20]).
> **meta** · depends-on: [M8-EXT-03], [M7-EXT-05], [M8-EXT-20] · depended-by: -

##### Systems Touched
Reuses M8's existing Personal Compartmental SEIR Infection engine (the same one modeling zombie-bite infection) with different R0/incubation/mortality constants — not a new illness system. Fed by untreated wounds (M2.9 damage events), contaminated food ([M7-EXT-05]), and contaminated water ([M8-EXT-20]).

##### Math
Identical SEIR compartmental equations already governing zombie infection, re-parameterized per illness type:

`dS/dt = −βSI/N, dE/dt = βSI/N − σE, dI/dt = σE − γI, dR/dt = γI` — same shape, different `(β, σ, γ)` triplet per illness (sepsis, dysentery, tetanus each get their own triplet, seeded at population N=1 for a personal infection).

##### Algorithm
An untreated open wound, a bad meal, or contaminated fish doesn't get bespoke sickness code — it seeds the existing SEIR state machine at N=1 with a different constants triplet than the zombie-infection variant. The same UI/status-effect hooks already built for showing infection progression drive these too.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Ignoring a wound or eating spoiled food doesn't just cost a flat HP tick — it runs the same believable exposed-infected-recovered progression already governing whether a bite turns you, just tuned to a survivable illness instead of a fatal one (usually).

#### [M8-EXT-04] Per-Save Pathogen Characterization & Cure Synthesis (Deliberately Near-Impossible)

> **tags** · general
> **tl;dr** · Rolls its own R0/mutation-drift/antigen-signature parameters per save file (independent of the M8-EXT-03 illness constants) at world-gen time. Sample integrity
> **ctx** · Per-Save Pathogen Characterization & Cure Synthesis (Deliberately Near-Impossible) -- Rolls its own R0/mutation-drift/antigen-signature parameters per save file (independent of the M8-EXT-03 illness constants) at world-gen time. Sample integrity uses [M7-EXT-05]'s Arrhenius spoilage curve. Requires uninterrupted M8.6 grid power (same CableSpanComponent/CheckCableSnap() system as every other electrical feature). Lab equipment durability uses [M3-EXT-06]'s shared StructuralFatigue/TickFatigue() system directly. Scientist companion bonus reads [M8-EXT-21]'s trust/skill state. Escalation is driven by the existing M5.4 AI Director. Completion feeds Appendix A's procedural narrative engine.
> **meta** · depends-on: [M8-EXT-04], [M7-EXT-05], [M3-EXT-06], [M8-EXT-21], [M8-EXT-29], [M8-EXT-03] · depended-by: -

##### Systems Touched
Rolls its own R0/mutation-drift/antigen-signature parameters per save file (independent of the M8-EXT-03 illness constants) at world-gen time. Sample integrity uses [M7-EXT-05]'s Arrhenius spoilage curve. Requires uninterrupted M8.6 grid power (same `CableSpanComponent`/`CheckCableSnap()` system as every other electrical feature). Lab equipment durability uses [M3-EXT-06]'s shared `StructuralFatigue`/`TickFatigue()` system directly. Scientist companion bonus reads [M8-EXT-21]'s trust/skill state. Escalation is driven by the existing M5.4 AI Director. Completion feeds Appendix A's procedural narrative engine.

##### Math
Pathogen parameters are rolled once per save via SplitMix64, not fixed:

`(β_strain, σ_strain, R0_strain) = Hash(save_seed, "pathogen") → deterministic-but-unknown-to-player triplet`

Sample cold-chain integrity is the existing spoilage formula, applied to lab samples:

`SampleIntegrity = Clamp(1.0 − K_decay·Δt_hours, 0.0, 1.0)` — reusing [M7-EXT-05]'s `K_decay` function directly, just with a tighter `T_fail` threshold than food.

Synthesis success per attempt is a low-probability Bernoulli draw, gated multiplicatively (not additively) by equipment tier and scientist bonus, so no combination of bonuses pushes probability anywhere near certain:

`P_success = Clamp(P_base·EquipTierMult·(1.0 + ScientistBonus), 0.0, P_ceiling)`, with `P_ceiling` deliberately low (single-digit percent) regardless of stacked bonuses.

##### Algorithm
At world-gen, a hidden pathogen-parameter triplet is hashed from the save seed — there is no fixed "correct answer," so no wiki entry can shortcut it; the player must empirically characterize the strain via captured samples before synthesis unlocks at all. Samples decay under the reused spoilage function, gated by uninterrupted power to the lab's refrigeration — a snapped `CableSpanComponent` power line (weather, vehicle impact, or simple neglect, all of which already exist) kills the cold chain and the batch. Lab equipment (centrifuges, analyzers) are subject to the same [M3-EXT-06] fatigue wear as everything else — they fail and need replacement. The M5.4 AI Director is explicitly made aware of active research-site power/activity signatures and escalates spawn density/horde pressure against that location the longer it stays active, using pacing knobs that already exist rather than a scripted final encounter. Losing the settlement (walls per [M8-EXT-29], outbreak per [M8-EXT-03]) can permanently wipe accumulated sample/equipment progress — persistence (M7) records this loss like any other world-state change, there is no special-cased "safe" savepoint for research progress.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
This is meant to be the hardest achievable thing in the game, and most playthroughs shouldn't finish it. You have to actually characterize your specific save's strain through captured samples before synthesis is even possible; keeping a cold chain alive through a sustained AI Director escalation against your own research site is a genuine campaign, not a checklist; losing the settlement can erase real progress with no safety net beyond normal persistence. A completed cure is a narrative capstone, not an off-switch — the world doesn't go quiet just because you won.

#### [M8-EXT-05] Data-Table Modding/Workshop Exposure

> **tags** · general
> **tl;dr** · Exposes the JSON item/brand-tier tables (M8), socio-economic tags (Appendix C), crafting compatibility rules ([M8-EXT-02]), and pathogen-parameter roll function
> **ctx** · Data-Table Modding/Workshop Exposure -- Exposes the JSON item/brand-tier tables (M8), socio-economic tags (Appendix C), crafting compatibility rules ([M8-EXT-02]), and pathogen-parameter roll function ([M8-EXT-04], exposed as an overridable seed only — not the roll itself, to preserve the intended difficulty of that feature) as editable/loadable external data. Not a new system — a loader-path addition for tables that already exist.
> **meta** · depends-on: [M8-EXT-05], [M8-EXT-02], [M8-EXT-04] · depended-by: -

##### Systems Touched
Exposes the JSON item/brand-tier tables (M8), socio-economic tags (Appendix C), crafting compatibility rules ([M8-EXT-02]), and pathogen-parameter roll function ([M8-EXT-04], exposed as an overridable seed only — not the roll itself, to preserve the intended difficulty of that feature) as editable/loadable external data. Not a new system — a loader-path addition for tables that already exist.

##### Math
None — this is I/O and validation, not simulation.

##### Algorithm
Every table this engine already runs on (weapon JSON records, brand-tier multipliers, socio-economic tag weights, crafting compatibility rules) gets a secondary load path that checks a user-data/mods directory before falling back to the built-in table, with schema validation against the existing struct definitions so a malformed mod table fails to load loudly rather than corrupting a run silently.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Because the entire game already runs off data tables rather than hardcoded content, community modding support is mostly "let people point at their own version of a table that already exists" — a natural, low-effort payoff of the zero-hand-authored-assets approach held since M0, rather than a bolted-on modding framework fighting the engine's own content pipeline.

### Cluster A

#### [M8-EXT-06] Two-Tier Deterministic Archetype Synthesis (Identity vs. Session Variance)

> **tags** · general
> **tl;dr** · Governs how every weapon, structural item, and core prop generated by M8's itemization system is synthesized; the item-instance identifier this feature defines
> **ctx** · Two-Tier Deterministic Archetype Synthesis (Identity vs. Session Variance) -- Governs how every weapon, structural item, and core prop generated by M8's itemization system is synthesized; the item-instance identifier this feature defines is what M7-EXT-07's delta-state persistence actually serializes for items.
> **meta** · depends-on: [M8-EXT-06] · depended-by: -

##### Systems Touched
Governs how every weapon, structural item, and core prop generated by M8's itemization system is synthesized; the item-instance identifier this feature defines is what M7-EXT-07's delta-state persistence actually serializes for items.

##### Math
Generation splits into two independent SplitMix64 streams keyed off different seeds — a fixed archetype hash for identity, a session/instance seed for variety:

`Tier1: gen₁ = SplitMix64(Hash64("Weapon_TommyGun"))` — constant across every playthrough
`Tier2: gen₂ = SplitMix64(SessionRunSeed ⊕ InstanceUUID)` — varies per instance/session

##### Algorithm
Generating 100% of assets procedurally while keeping specific items ("a Tommy Gun") instantly recognizable and identical across playthroughs requires separating *identity* from *variety* into two data classes. **Tier 1 — Global Archetype Manifest:** every recognizable item gets a permanent 64-bit hash (e.g. `Hash64("Weapon_TommyGun")`) that seeds an isolated SplitMix64 generator producing the baseline identity: geometric proportions (component assembly graph, receiver ratios, socket offsets) and functional physics baselines (inertia tensors, slide travel, recoil spring constant `k`, thermal bloom rate). Because this seed never changes, the Tommy Gun's core shape and feel are identical on every machine and save file. **Tier 2 — Session Mutation Vector:** once the Tier 1 baseline exists, the current `SessionRunSeed` combined with the item instance's UUID drives a second, bounded pass — weathering noise masks (scratches, rust, grime), attachment kitbashing rolls (weighted by the local zone's item tier), and a small handling-tolerance multiplier (clamped `0.95`–`1.05`) on the recoil spring constant. This pass never touches structural topology.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A Tommy Gun always looks and handles like a Tommy Gun, on any save or machine — but the specific one in your hands has its own scratches, rust pattern, and a barely-perceptible mechanical "personality" from wear tolerance, without ever risking a broken or nonsensical weapon configuration.

#### [M8-EXT-07] Memory-Mapped FlatBinary Archetype Schema Serializer

> **tags** · FlatBinary
> **tl;dr** · Backs the JSON Layout Schema Validation step in M8's base implementation — a boot-time compile pass that removes runtime string-parsing overhead when reading it
> **ctx** · Memory-Mapped FlatBinary Archetype Schema Serializer -- Backs the JSON Layout Schema Validation step in M8's base implementation — a boot-time compile pass that removes runtime string-parsing overhead when reading item/weapon records, complementing [M8-EXT-06]'s Two-Tier Archetype Synthesis (that feature generates the archetype identity; this one is how the *authored* JSON-declared fields, like brandTier/brandTierMultiplier, get resolved cheaply at runtime once loaded).
> **meta** · depends-on: [M8-EXT-07], [M8-EXT-06] · depended-by: -

##### Systems Touched
Backs the JSON Layout Schema Validation step in M8's base implementation — a boot-time compile pass that removes runtime string-parsing overhead when reading item/weapon records, complementing `[M8-EXT-06]`'s Two-Tier Archetype Synthesis (that feature generates the archetype identity; this one is how the *authored* JSON-declared fields, like `brandTier`/`brandTierMultiplier`, get resolved cheaply at runtime once loaded).

##### Math
`RecordAddress = BufferBaseAddress + (RecordIndex · FixedStrideBytes)`

##### Algorithm
At boot, the JSON item/weapon tables (already schema-validated per M8's base step) are pre-compiled into a dense, fixed-stride binary buffer. Runtime queries resolve a record's address by direct index arithmetic against a memory-mapped buffer instead of walking a string-keyed JSON structure, removing string parsing and heap allocation from the itemization hot path entirely.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Invisible directly — this is what keeps item/weapon-stat lookups cheap enough to run every frame (brand-tier cook-off checks, jam-probability rolls) without the string-parsing cost item JSON would otherwise carry into the hot path.

#### [M8-EXT-08] Log-Normal Camouflage Masking

> **tags** · general
> **tl;dr** · Stealth/perception ([M5]), survivor AI. Depends on [M5].
> **ctx** · Log-Normal Camouflage Masking -- Stealth/perception ([M5]), survivor AI. Depends on [M5].
> **meta** · depends-on: [M8-EXT-08] · depended-by: -

##### Systems Touched
Stealth/perception ([M5]), survivor AI. Depends on [M5].

##### Math
Camo `C~LogNormal(μ,σ)` per biome. Detection `P=1-exp(-k·(1-C)·exposure)`. Lognormal: few excellent, most mediocre.

##### Algorithm
1. Draw C from biome lognormal. 2. Perception computes exposure. 3. P=detection. 4. Player raises C via terrain/movement.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Hiding probabilistic+ biome-dependent: match terrain→vanish, open→seen. Emergent, not binary flag.

#### [M8-EXT-09] Memory-Mapped FlatBinary Inventory Cache Dictionary

> **tags** · FlatBinary
> **tl;dr** · M8. Memory-maps a flat-binary item-definition dictionary for instant lookup.
> **ctx** · Memory-Mapped FlatBinary Inventory Cache Dictionary -- M8. Memory-maps a flat-binary item-definition dictionary for instant lookup.
> **meta** · depends-on: [M8-EXT-09] · depended-by: -

##### Systems Touched
M8. Memory-maps a flat-binary item-definition dictionary for instant lookup.

##### Math
ItemDef* def = mmap(dict)[id]; // no parse on access;

##### Algorithm
Item definitions live in a flat binary mmap'd at load; lookup is a direct offset (no per-item parse), so inventory/loot resolution is O(1) and zero-alloc. Pairs with M8-EXT-10 icon gen.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Item lookups are instant - no parse stall on big inventories.

#### [M8-EXT-10] Procedural Loot Icon Generation

> **tags** · general
> **tl;dr** · M8 + M8-EXT-09. Renders item icons procedurally from defs (not hand-authored art).
> **ctx** · Procedural Loot Icon Generation -- M8 + M8-EXT-09. Renders item icons procedurally from defs (not hand-authored art).
> **meta** · depends-on: [M8-EXT-10] · depended-by: -

##### Systems Touched
M8 + M8-EXT-09. Renders item icons procedurally from defs (not hand-authored art).

##### Math
icon = RenderIcon(def, rarity); cache by def hash;

##### Algorithm
Loot icons are drawn from the item definition (shape/rarity tint) rather than shipped sprites, so modded/new items get icons automatically. Cached by def hash. Feeds M11 UI.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Every item (incl. modded) gets an icon - no missing-sprite holes.


### Cluster B

#### [M8-EXT-11] Data-Driven Itemization

> **tags** · general
> **tl;dr** · Items/weapons as data defs (fire-rate/reload/recoil); SLM generates balanced stats (M13).
> **ctx** · Data-Driven Itemization -- M8. No item/weapon def system; only BulletComponent data fields.
> **meta** · depends-on: [M8-EXT-11] · depended-by: -

##### Systems Touched
M8. No item/weapon def system; only `BulletComponent` data fields.

##### Math
-

##### Algorithm
Items/weapons as data defs (fire-rate/reload/recoil); SLM generates balanced stats (M13).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Weapons/items are real + tunable.

#### [M8-EXT-12] Stacking & Inventory Slots

> **tags** · general
> **tl;dr** · Items stack by type up to max; fixed slot grid or weight-based
> **ctx** · Stacking & Inventory Slots -- M8. No stacking/slot model.
> **meta** · depends-on: [M8-EXT-12] · depended-by: -

##### Systems Touched
M8. No stacking/slot model.

##### Math
-

##### Algorithm
Items stack by type up to max; fixed slot grid or weight-based. `slots.Add(item, qty);`

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Looting is organized.

#### [M8-EXT-13] Ammo Types & Crafting

> **tags** · general
> **tl;dr** · Ammo types (FMJ/HP/AP) with tradeoffs; craft from components
> **ctx** · Ammo Types & Crafting -- M8. No ammo variants/crafting.
> **meta** · depends-on: [M8-EXT-13] · depended-by: -

##### Systems Touched
M8. No ammo variants/crafting.

##### Math
-

##### Algorithm
Ammo types (FMJ/HP/AP) with tradeoffs; craft from components. `if (Has(comp)) MakeAmmo(type);`

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Crafting loops close.


### Cluster B — input / determinism / save / platform

#### [M8-EXT-14] Barter & Trade Economy

> **tags** · general, economy, systems
> **tl;dr** · M8.5. NPC vendors buy/sell with price modifiers driven by faction standing (M8-EXT-53), social tier (M8-EXT-62), and regional supply (M8.5 elastic model + M8-EXT-61 trade routes).
> **ctx** · Barter & Trade Economy -- M8.5. Replaces the original "No trade system" stub; the trade layer is now realized by standing-gated prices (M8-EXT-53), tier-gated access (M8-EXT-62), and route-disrupted supply (M8-EXT-61). The cascade engine (M8-EXT-60) perturbs prices when a trade route is blocked.
> **meta** · depends-on: [M8-EXT-53], [M8-EXT-62], [M8-EXT-61], [M8-EXT-15] · depended-by: [M8-EXT-53], [M8-EXT-60], [M8-EXT-61]

##### Systems Touched
Vendor price model: `price = base * factionMod(standing) * tierMod(socialTier) * supplyMod(regionStock, routeHealth)`.

##### Math
`factionMod = clamp(1 + k*(standingNorm), 0.5, 2.0); supplyMod = 1 / (1 + elasticity * (regionStock / demand));`

##### Algorithm
Each vendor (resident from M2-EXT-77, tiered via M8-EXT-62) exposes buy/sell prices. Your faction standing (M8-EXT-53) shifts prices — idolized buys cheap, hostile buys dear or refused. Regional stock is seeded from M8.5's elastic supply-demand and modulated by trade-route health (M8-EXT-61): a blocked route spikes prices (cascade M8-EXT-60). Currency (M8-EXT-15) is the settlement medium; barter is the fallback when currency is scarce.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Trade is a relationship, not a menu. Who you are and what's happening on the roads changes what you pay.
Economy is interactive.

#### [M8-EXT-15] Currency & Scarcity Model

> **tags** · general, economy, systems
> **tl;dr** · M8.5. Scarce currency (pre-war bills + barter-weight); regional inflation driven by supply/demand (M8.5) and disrupted by blocked trade routes (M8-EXT-61 / cascade M8-EXT-60).
> **ctx** · Currency & Scarcity Model -- M8.5. Replaces the original "No currency design" stub; the scarcity layer now drives M8-EXT-14 prices and is perturbed by the world-state cascade (M8-EXT-60) + trade routes (M8-EXT-61).
> **meta** · depends-on: [M8-EXT-61], [M8-EXT-60], [M8-EXT-14] · depended-by: [M8-EXT-14], [M8-EXT-53], [M8-EXT-62], [M8-EXT-61]

##### Systems Touched
Currency stock per region: `regionMoney = f(population, production, routeHealth)`; wallet carries physical weight.

##### Math
`inflationRegion = baseCPI * (demand / (supply + routeHealth*bonus)); walletWeight = billCount * unitWeight;`

##### Algorithm
Currency is pre-war cash + barter-weight (ammo, meds, food as alternate money). Each region has a money supply; inflation rises when supply drops or a trade route (M8-EXT-61) is cut (cascade M8-EXT-60 propagates the shock). Wallet has physical weight — you can't carry infinite cash, reinforcing M8-EXT-29 durability/scarcity feel. Prices in M8-EXT-14 read this inflation.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Money is heavy and unstable. A blocked road makes everything cost more. Hoarding has a real cost.

#### [M8-EXT-16] Settlement NPC Residents

> **tags** · NPC, systems
> **tl;dr** · M8.6. Settlements spawn persistent + procedural resident NPCs with roles, daily loops, and personal plots — the world feels alive.
> **ctx** · Settlement NPC Residents -- M8.6. Replaces the original "no resident NPCs" stub (pre-merge); the resident-NPC system is now realized by the autonomous settlement sim (M8-EXT-59), social stratification (M8-EXT-62), and daily-life sim (M8-EXT-65), populated by the persistent cross-world registry (M2-EXT-76) and procedural detail generator (M2-EXT-77).
> **meta** · depends-on: [M8-EXT-59], [M8-EXT-62], [M8-EXT-65], [M2-EXT-76], [M2-EXT-77] · depended-by: -

##### Systems Touched
M8.6 resident population: spawn, role assignment, daily routine, personal-plot progression.

##### Math
`SpawnNPC(role, home, seed=hash(worldSeed, StableId))` — deterministic per M2-EXT-77; stratification tier (M8-EXT-62) gates role + housing.

##### Algorithm
Settlements spawn NPCs with roles (guard/trader/cook/scavenger) drawn from the procedural generator (M2-EXT-77); each runs a daily loop (M8-EXT-65) and belongs to a wealth/power tier (M8-EXT-62). Persistent NPCs (companions, mayors) are registered cross-world via M2-EXT-76 and survive permadeath. The autonomous sim (M8-EXT-59) assigns plots and grows the population over time.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Settlements feel alive — named people with jobs, schedules, and stories, not cardboard decorations.

#### [M8-EXT-17] Perk Tree & Prerequisites

> **tags** · general, progression, systems
> **tl;dr** · M8.7. Perks form a DAG with prerequisites; survivors (M2-EXT-18) and the player specialize by spending points unlocked via M8-EXT-18 XP.
> **ctx** · Perk Tree & Prerequisites -- M8.7. Replaces the original "No perk tree" stub; the perk DAG now gates survivor/player specialization and is fed by M8-EXT-18 XP + M8-EXT-56 psyche state.
> **meta** · depends-on: [M8-EXT-18], [M2-EXT-18], [M8-EXT-56] · depended-by: [M8-EXT-18]

##### Systems Touched
Perk DAG: `unlock(p) requires all(prereq(p)) AND points>0`; tiers gate gear (M8-EXT-01 brand-tier).

##### Math
`points = floor(xp / xpPerPoint); canUnlock(p) = HasPrereq(p) && points>=cost(p);`

##### Algorithm
Perks are nodes in a directed acyclic graph (combat / crafting / social / survival branches). Each survivor (M2-EXT-77 generated, or companion) accrues XP (M8-EXT-18) and spends points to unlock perks; prerequisites enforce a build path so you can't grab the end-tier without the foundations. Psyche state (M8-EXT-56) can temporarily block perk use under panic. Top-tier perks are the only door to top-tier gear (M8-EXT-01), so progression is earned, not bought.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Builds matter. A medic, a mechanic, a warlord — each is a real specialization your survivors grow into.

#### [M8-EXT-18] Skill Progression & XP

> **tags** · XP, progression, systems
> **tl;dr** · M8.7. Activities grant XP along skill lines (combat/craft/social/survival); levels gate M8-EXT-17 perk points; skills decay if unused (M2-EXT-18 upkeep).
> **ctx** · Skill Progression & XP -- M8.7. Replaces the original "No XP curve" stub; the skill/XP layer now feeds M8-EXT-17 perk unlocks and is coupled to M2-EXT-18 needs upkeep (skills atrophy without practice).
> **meta** · depends-on: [M8-EXT-17], [M2-EXT-18] · depended-by: [M8-EXT-17]

##### Systems Touched
Per-skill XP tracks: `xp[skill] += gain(activity, skill); level = floor(xp/xpPerLevel); perkPoints += onLevelUp;`

##### Math
`decay: xp[skill] *= (1 - decayRate*dt) if lastUsed[skill] > idleThreshold;`

##### Algorithm
Each survivor has skill lines (combat, craft, social, survival). Doing the activity grants XP to that line; crossing a level threshold awards a perk point spent in M8-EXT-17. Skills decay slowly if unused (coupled to M2-EXT-18 upkeep), so a medic who stops healing gets rusty — reinforcing the "manage your people, don't grind them" psyche design (M8-EXT-56). Player and companions share the same system.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
People grow by doing. A survivor who fights becomes a fighter; one left idle loses it. Progression is lived, not farmed.

#### [M8-EXT-19] Poisson-Disk Farm Plot Growth & Scent-Attractor Coupling

> **tags** · general
> **tl;dr** · Reuses the Poisson-disk clustering already driving foliage placement (Appendix F). Reads live M10 weather/season state for growth gating. Writes a positive sour
> **ctx** · Poisson-Disk Farm Plot Growth & Scent-Attractor Coupling -- Reuses the Poisson-disk clustering already driving foliage placement (Appendix F). Reads live M10 weather/season state for growth gating. Writes a positive source term into M5.3's Atmospheric Gaussian Plume Scent Dispersion grid.
> **meta** · depends-on: [M8-EXT-19] · depended-by: -

##### Systems Touched
Reuses the Poisson-disk clustering already driving foliage placement (Appendix F). Reads live M10 weather/season state for growth gating. Writes a positive source term into M5.3's Atmospheric Gaussian Plume Scent Dispersion grid.

##### Math
Growth stage (`g`) follows a logistic curve gated by a weather-suitability scalar (`W_suit`, 0–1, computed from your existing M10 temperature/precipitation state — not new weather code):

`dg/dt = r·g·(1 − g)·W_suit`

Crop scent-source strength feeds directly into the existing plume equation as an additional emission term:

`Q_farm = β_crop · g · A_plot`

##### Algorithm
Plot centers are seeded via the same Poisson-disk sampler used for foliage clustering, constrained to socio-economic-tagged residential/rural cells (Appendix C) — no new placement logic, just a new consumer of tags/sampler that already exist. Each active plot writes `Q_farm` as an additional point-source into the scent-dispersion grid M5.3 already solves every tick; the plume system doesn't know or care that this source is "a farm" versus a corpse or a gunshot.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Planting near your base is efficient but not free — a productive plot is a standing scent beacon that measurably increases horde traffic toward your walls, using the exact same plume math a fresh gunshot or blood pool already generates. A cold snap right after planting can wipe out a harvest for real, because it's reading the same weather state driving everything else.

#### [M8-EXT-20] Boid-Flock Fish Population & Hazard-Cell Contamination

> **tags** · general
> **tl;dr** · Lightweight Reynolds boid flocking (already used for zombie crowd movement and drafting in M5.1) applied to fish inside existing Jolt water volumes. Contaminati
> **ctx** · Boid-Flock Fish Population & Hazard-Cell Contamination -- Lightweight Reynolds boid flocking (already used for zombie crowd movement and drafting in M5.1) applied to fish inside existing Jolt water volumes. Contamination risk is derived from two things that already exist — the M10 Saint-Venant h_water flood heightmap and the Appendix C socio-economic zone tag — rather than a new hazard-cell struct.
> **meta** · depends-on: [M8-EXT-20], [M8-EXT-03] · depended-by: -

##### Systems Touched
Lightweight Reynolds boid flocking (already used for zombie crowd movement and drafting in M5.1) applied to fish inside existing Jolt water volumes. Contamination risk is derived from two things that already exist — the M10 Saint-Venant `h_water` flood heightmap and the Appendix C socio-economic zone tag — rather than a new hazard-cell struct.

##### Math
Catch events are a Poisson arrival process gated by local school density (`ρ_fish`):

`P(catch in Δt) = 1 − e^(−ρ_fish·κ_gear·Δt)`

Contamination probability is derived at catch-time from existing per-cell data, no new persistent struct:

`P_contaminated = Clamp(w_zone·IsIndustrialTag(cell) + w_flood·Saturate(h_water/h_ref), 0.0, 1.0)`

where `IsIndustrialTag` reads the same Appendix C socio-economic tag already used for loot weighting, and `h_water` is sampled directly from M10's existing flood heightmap — no new hazard scalar is written or stored anywhere.

##### Algorithm
Fish schools are boid-flocked exactly like zombie crowds (same separation/alignment/cohesion weights, different steering target — avoid player/predator instead of pursue), just running at a coarser update cadence since they don't need combat-grade fidelity. Catching a fish rolls the Poisson arrival, then computes contamination risk on the fly from the zone tag and current flood depth at that cell, feeding into the shared illness roll from [M8-EXT-03].

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Fishing in a clean rural creek is slow but safe; fishing downstream of a flooded industrial sector is faster (denser schools near runoff-fed nutrients) but rolls real illness risk, computed live from data the world already tracks. No separate "is this water safe" UI or new hazard bookkeeping — just the existing zone tag and flood depth doing double duty.

#### [M8-EXT-21] Recruitable Survivor AI (Companion Command Layer over Existing Zombie AI)

> **tags** · AI
> **tl;dr** · Runs the existing M5.1 Weighted Consideration Utility AI stack with the faction flag flipped, plus a command-override layer. Extends M8.5's existing Bayesian Co
> **ctx** · Recruitable Survivor AI (Companion Command Layer over Existing Zombie AI) -- Runs the existing M5.1 Weighted Consideration Utility AI stack with the faction flag flipped, plus a command-override layer. Extends M8.5's existing Bayesian Companion Trust Updating rather than introducing a second trust system. Cross-session identity uses the M7 StableId system.
> **meta** · depends-on: [M8-EXT-21] · depended-by: -

##### Systems Touched
Runs the existing M5.1 Weighted Consideration Utility AI stack with the faction flag flipped, plus a command-override layer. Extends M8.5's existing Bayesian Companion Trust Updating rather than introducing a second trust system. Cross-session identity uses the M7 `StableId` system.

##### Math
Trust doesn't just Bayesian-update on discrete events — it drifts continuously toward a floor absent reinforcement, using a bounded Ornstein-Uhlenbeck process layered on top of the existing Bayesian updater:

`dTrust = θ·(μ_floor − Trust)·dt + σ·dW + ΔTrust_bayesian_event`

##### Algorithm
A recruited survivor entity keeps its existing zombie-stack components (utility-AI consideration set) but with `Faction = Player` and a `CommandOverride` component that can inject a player-issued goal above the AI's own utility-scored choices — a similar overriding pattern to how screamer convergence already overrides normal zombie utility scoring in M5.1. Trust events (shared kills, gifted items, completed requests) fire the existing Bayesian updater from M8.5; between events, the OU term above handles ambient drift so trust isn't static outside of scripted moments. `StableId` (M7) is what lets a companion's trust record survive a save/reload without needing a new persistence path.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Companions feel alive between story beats — trust doesn't just move when a scripted event fires, it quietly drifts if you ignore them, using the same random-walk math already giving your zombie phenotypes organic variation rather than a hand-tuned relationship-meter script.

#### [M8-EXT-22] Live Economy-Sim Trade Pricing (Barter)

> **tags** · general
> **tl;dr** · Reads directly from M8.5's existing Elastic Supply-Demand Trade Model and Macro Regional Price Inflation (MV=PQ) — this feature is a UI/interaction layer over d
> **ctx** · Live Economy-Sim Trade Pricing (Barter) -- Reads directly from M8.5's existing Elastic Supply-Demand Trade Model and Macro Regional Price Inflation (MV=PQ) — this feature is a UI/interaction layer over data that already exists, not a new pricing system.
> **meta** · depends-on: [M8-EXT-22] · depended-by: -

##### Systems Touched
Reads directly from M8.5's existing Elastic Supply-Demand Trade Model and Macro Regional Price Inflation (`MV=PQ`) — this feature is a UI/interaction layer over data that already exists, not a new pricing system.

##### Math
No new formula — trade offer value is the existing regional price term evaluated at the transaction's item category and quantity:

`Price_offer = P_region(item_category)·Q_traded`, where `P_region` is already being computed by the existing `MV=PQ` regional model.

##### Algorithm
A trade UI reads the same live `P_region` values M8.5 is already maintaining per-region per-category — a besieged settlement's prices are already spiking in the economy sim before a player ever opens a trade menu; this feature just exposes that number instead of computing a separate shop price.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Prices genuinely reflect what's happening in the world — a settlement that just lost its ammo cache to a raid will actually pay more for ammo, because the same economy simulation already tracking that shortage is what the trade window is reading from.

#### [M8-EXT-23] Snare / Trap Detailing for Foraging

> **tags** · general
> **tl;dr** · Supports M8 food spawns and M4-EXT-59 regrowth loops; trap nodes placed in valid cells.
> **ctx** · Snare / Trap Detailing for Foraging -- Supports M8 food spawns and M4-EXT-59 regrowth loops; trap nodes placed in valid cells.
> **meta** · depends-on: [M8-EXT-23] · depended-by: -

##### Systems Touched
Supports M8 food spawns and M4-EXT-59 regrowth loops; trap nodes placed in valid cells.

##### Math
place if cell_valid && rng<snare_density.

##### Algorithm
Cells flagged for foraging receive snare/trap props with a loot hook into M8 food and a regrowth timer (M4-EXT-59) so the spot replenishes.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Set a snare, come back later — the land keeps producing if you let it.

#### [M8-EXT-24] Kirchhoff Power Grid Overload Arcing

> **tags** · general
> **tl;dr** · Connects your M8.6 electrical network arrays directly into your M6.5 cellular-automata fire grids.
> **ctx** · Kirchhoff Power Grid Overload Arcing -- Connects your M8.6 electrical network arrays directly into your M6.5 cellular-automata fire grids.
> **meta** · depends-on: [M8-EXT-24] · depended-by: -

##### Systems Touched
Connects your M8.6 electrical network arrays directly into your M6.5 cellular-automata fire grids.

##### Math
When line configurations experience high loads, the thermal power dissipation (P_loss ) is tracked across line resistance values (R):

`P_loss = I²·R`

If `P_loss` crosses your safety threshold (`P_fuse`), the line fails, firing an immediate event that passes the 2D matrix coordinates straight into your cellular fire simulation to ignite an electrical hazard cell.

##### Algorithm
Each power-loop update checks every line's dissipated power against its fuse threshold. A line crossing threshold fails immediately, passing its grid coordinates directly into the M6.5 cellular fire sim to ignite a hazard cell at that exact location.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Overloading your safehouse grid by daisy-chaining too many traps and searchlights onto an un-relayed line creates an immediate fire hazard. The lines will physically snap, throw bright electrical arcs across the dark viewport, and ignite nearby wooden components, forcing you to design balanced electrical layout structures using dedicated relays.

#### [M8-EXT-25] Surface-Saturation Capacitive Arc Tracking

> **tags** · general
> **tl;dr** · Extends M8.6 (Kirchhoff Power Networks) across M10 shallow-water flooding maps.
> **ctx** · Surface-Saturation Capacitive Arc Tracking -- Extends M8.6 (Kirchhoff Power Networks) across M10 shallow-water flooding maps.
> **meta** · depends-on: [M8-EXT-25], [M8-EXT-27] · depended-by: -

##### Systems Touched
Extends M8.6 (Kirchhoff Power Networks) across M10 shallow-water flooding maps.

##### Math
When high-voltage power components or electrical grids snap, current paths route along the shortest path over saturated ground terrain coordinates. The local cell resistance (R_cell ) drops non-linearly based on the shallow-water height variables (h_water ):

`G_cell = (1.0 + α·h_water) / R_baseline`

This is a conductance (`G_cell` = 1/R, computed by `GetCellConductance` below), not a resistance — it rises with water depth rather than falling. Note [M8-EXT-27] below computes the same quantity via a separate function, also named `G_cell` — see the consolidation note there.

##### Algorithm
Your power grid updates run on a slow cadence. When an open wire or circuit failure event fires, the system maps out conductance lines using your existing 2D SpatialHash layout. It checks the water layer states across neighboring tiles, routing lethal electrical damage values directly through flooded cells containing players or entities without running complex finite-element field models.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Snapping an electrical wire onto dry concrete does little more than spark locally. However, if you drop that same live wire into an intersection flooded by a severe storm, the electrical current arcs across the wet asphalt surface. It transforms the puddle into a highly lethal electric trap that shocks everything wading through the water, letting you use the weather to control choke points.

#### [M8-EXT-26] Catenary Cable Tension Snapping Solver

> **tags** · general
> **tl;dr** · Complements M8.6 electrical wiring layouts and handles Jolt distance constraints. Declares the canonical CableSpanComponent — the single shared component backin
> **ctx** · Catenary Cable Tension Snapping Solver -- Complements M8.6 electrical wiring layouts and handles Jolt distance constraints. Declares the canonical CableSpanComponent — the single shared component backing both this feature and [M8-EXT-28] Catenary Wire Clothesline Decapitation Loop. Per standard ECS practice, this is one system distinguished by a role tag, not two independent implementations of near-identical tension-snap math (the same gap pattern as the ammo cook-off duplicate):
> **meta** · depends-on: [M8-EXT-26], [M8-EXT-28], [M8-EXT-24], [M8-EXT-25] · depended-by: -

##### Systems Touched
Complements M8.6 electrical wiring layouts and handles Jolt distance constraints. Declares the canonical `CableSpanComponent` — the single shared component backing both this feature and [M8-EXT-28] Catenary Wire Clothesline Decapitation Loop. Per standard ECS practice, this is one system distinguished by a role tag, not two independent implementations of near-identical tension-snap math (the same gap pattern as the ammo cook-off duplicate):

```cpp
enum class CableRole { PowerLine, DecapTrap };

struct CableSpanComponent {
    JPH::BodyID anchorA;
    JPH::BodyID anchorB;
    float ropeDensity   = 1.0f;   // ρ_cable
    float breakTension  = 0.0f;   // T_break
    CableRole role       = CableRole::PowerLine;
};
```

##### Math
Power lines hanging between anchor cells map out a catenary mathematical curve shape. The internal tension force (T) at the support boundaries reacts to the suspended cable mass (m) and structural sag distances (a):

`y = a·cosh(x/a), T_max = ρ_cable · g · y_max`

If an external kinetic impact load (like a vehicle hit) injects a sudden force impulse (J), the tension spike triggers an immediate line snap if safety limits are crossed:

`T_total = T_max + (‖J‖/Δt) > T_break`

##### Algorithm
Your power grids map connection coordinates across structural anchor blocks. This system attaches a `CableSpanComponent` (with a Jolt distance or spring constraint) between the anchor nodes to represent the wire span. If an object intersects the bounding volume of the wire path, the collision impulses are passed directly into the shared `CheckCableSnap()` tension check below; crossing the breaking threshold deletes the constraint component instantly. What happens on snap is gated by `CableSpanComponent::role`: `PowerLine` cuts off circuit routing (feeds [M8-EXT-24]/[M8-EXT-25]); `DecapTrap` fires the kinematic joint-severing impulse described in [M8-EXT-28] instead. The tension math and break check are identical either way — only the on-snap consequence differs.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Smashing a speeding off-road vehicle straight into an overhead utility pole or tearing through low-hanging power line rigs will physically catch the cables. The lines will stretch taut with high tension before snapping violently with a sharp electrical crack, causing live, sparking wire ends to whip down across the street surface and shock nearby entities.

#### [M8-EXT-27] Electro-Capacitive Water-Wading Chain Stun

> **tags** · general
> **tl;dr** · Merges your M8.6 Kirchhoff power networks directly into M10 Saint-Venant flooding tiles.
> **ctx** · Electro-Capacitive Water-Wading Chain Stun -- Merges your M8.6 Kirchhoff power networks directly into M10 Saint-Venant flooding tiles.
> **meta** · depends-on: [M8-EXT-27], [M8-EXT-25] · depended-by: -

##### Systems Touched
Merges your M8.6 Kirchhoff power networks directly into M10 Saint-Venant flooding tiles.

##### Math
When a live electrical wire snaps and contacts a flooded street, current routes across adjacent cells along the highest conductance paths based on water thickness (h_water ):

`G_cell = (1.0 + α·h_water) / R_baseline`

Same conductance formula and computation as [M8-EXT-25] Surface-Saturation Capacitive Arc Tracking (`GetCellConductance` there, `StepChainConductance` here — same inputs/math, plus an added `1e-5f` epsilon). [M8-EXT-25] is the canonical declaration; this feature calls that shared function rather than maintaining a second implementation.

##### Algorithm
When a snapped live wire contacts a flooded tile, current conducts outward across adjacent cells scaling with water depth via the shared conductance function ([M8-EXT-25]). Any entity in a cell above the conduction threshold is stunned simultaneously — a large flooded area chain-stuns everyone in it at once.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Dropping a live electrical line or shooting a power transformer above a flooded avenue turns the puddle into a massive chain-reaction trap. The current routes across the contiguous water layer, shocking and paralyzing every zombie wading through the water simultaneously, letting you use active weather storms to secure entire avenues.

#### [M8-EXT-28] Catenary Wire Clothesline Decapitation Loop

> **tags** · general
> **tl;dr** · Extends M8.6 wiring anchor blocks, setting clean Jolt distance constraints. Reuses the canonical CableSpanComponent and CheckCableSnap() declared in [M8-EXT-26]
> **ctx** · Catenary Wire Clothesline Decapitation Loop -- Extends M8.6 wiring anchor blocks, setting clean Jolt distance constraints. Reuses the canonical CableSpanComponent and CheckCableSnap() declared in [M8-EXT-26] with role = CableRole::DecapTrap — this is the same tension-snap system as the power-line cable, not a second implementation of it. Build M8.6-EXT-03 first; this feature only adds the placement flow (player rigs a wire between two anchor blocks) and the on-snap decapitation impulse.
> **meta** · depends-on: [M8-EXT-28], [M8-EXT-26] · depended-by: -

##### Systems Touched
Extends M8.6 wiring anchor blocks, setting clean Jolt distance constraints. Reuses the canonical `CableSpanComponent` and `CheckCableSnap()` declared in [M8-EXT-26] with `role = CableRole::DecapTrap` — this is the same tension-snap system as the power-line cable, not a second implementation of it. Build M8.6-EXT-03 first; this feature only adds the placement flow (player rigs a wire between two anchor blocks) and the on-snap decapitation impulse.

##### Math
A steel cable rigged between two fixed anchor blocks maps out a structural catenary curve — identical math to [M8-EXT-26]. When an asset crosses the coordinate at high velocity, the mechanical tension spike (T_total ) is processed through the same `CheckCableSnap()` check:

`T_total = T_static + (‖J_impact‖/Δt) > T_break`

##### Algorithm
Once a player rigs a cable between two anchor blocks (reusing M8.6-EXT-03's placement flow, `role = CableRole::DecapTrap`), the span tracks tension through the shared `CheckCableSnap()`. Crossing break tension triggers the decapitation impulse instead of the power-line's circuit-cut — same detector, different on-snap consequence via `role`.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
You can rig a thin steel wire between structural metal poles or ruined cars to form an active defensive line. Speeding vehicles pass safely beneath the boundary, but pursuing common zombies running at neck height hit the wire constraint directly. If the tension limits hold, the cable performs an instant kinetic joint severing loop, cleanly decapitating targets.

#### [M8-EXT-29] Player-Placed Barricade Snap Constraints (Fortification)

> **tags** · general
> **tl;dr** · Reuses the exact CableSpanComponent/CheckCableSnap() pattern declared in [M8-EXT-26], adding a third CableRole::Barricade value rather than new constraint code.
> **ctx** · Player-Placed Barricade Snap Constraints (Fortification) -- Reuses the exact CableSpanComponent/CheckCableSnap() pattern declared in [M8-EXT-26], adding a third CableRole::Barricade value rather than new constraint code. Horde load reuses M9-EXT-06's continuum-fluid horde-pressure force and M5.4-EXT-01's existing P_crush formula.
> **meta** · depends-on: [M8-EXT-29], [M8-EXT-26] · depended-by: -

##### Systems Touched
Reuses the exact `CableSpanComponent`/`CheckCableSnap()` pattern declared in [M8-EXT-26], adding a third `CableRole::Barricade` value rather than new constraint code. Horde load reuses M9-EXT-06's continuum-fluid horde-pressure force and M5.4-EXT-01's existing `P_crush` formula.

##### Math
No new snap math — identical to the existing shared check:

`T_total = T_static + (‖J_impact‖/Δt) > T_break`

Horde load against a placed barricade is the same crowd-pressure sum already computed for crush damage:

`P_wall = Σ_{i∈LocalCell} ‖F_separation_i‖` (identical to the existing `P_crush` term from M5.4-EXT-01, evaluated against the barricade's bounding volume instead of a player/entity)

##### Algorithm
Placing a barricade spawns a `CableSpanComponent`/Jolt constraint pair with `role = CableRole::Barricade` between two anchor points, exactly like a power line or a decap-trap wire — same struct, same break-check function, third enum value. A leaning horde applies force through the existing `SpatialHash`-driven crowd-density query already computing `P_crush` elsewhere; crossing `T_break` deletes the constraint the same way a snapped power line does.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A barricade groaning and finally snapping under horde weight uses the identical physical logic as watching a power line snap under a vehicle impact — one consistent "things break under load" language across the whole game instead of a separate abstracted "wall HP" bar.

#### [M8-EXT-30] Kirchhoff Circuit Ohmic Dissipation Water Vaporization Solver

> **tags** · general
> **tl;dr** · Links M8.6's Kirchhoff power grid to M10's Saint-Venant shallow-water flooding grid — when a snapped live line ([M8-EXT-26]/[M8-EXT-28], CableSpanComponent) dis
> **ctx** · Kirchhoff Circuit Ohmic Dissipation Water Vaporization Solver -- Links M8.6's Kirchhoff power grid to M10's Saint-Venant shallow-water flooding grid — when a snapped live line ([M8-EXT-26]/[M8-EXT-28], CableSpanComponent) discharges into a flooded cell, this converts the dissipated electrical energy into a vaporization volume that feeds back into the flood heightmap, rather than the electrical and flood systems staying one-directionally coupled (M8.6-EXT-02/04 currently only read water depth to boost conductance; this is the reverse write).
> **meta** · depends-on: [M8-EXT-30], [M8-EXT-26], [M8-EXT-28], [M8-EXT-24], [M8-EXT-27] · depended-by: -

##### Systems Touched
Links M8.6's Kirchhoff power grid to M10's Saint-Venant shallow-water flooding grid — when a snapped live line (`[M8-EXT-26]`/`[M8-EXT-28]`, `CableSpanComponent`) discharges into a flooded cell, this converts the dissipated electrical energy into a vaporization volume that feeds back into the flood heightmap, rather than the electrical and flood systems staying one-directionally coupled (M8.6-EXT-02/04 currently only read water depth to boost conductance; this is the reverse write).

##### Math
`Δh_water = −(I²·R·Δt) / (ρ_water · A_cell · L_vap)`

##### Algorithm
When `CheckCableSnap()` (`[M8-EXT-26]`) fires into a flooded cell, the resulting ohmic power loss (`I²R`, already computed for the overload-arcing check in `[M8-EXT-24]`) is converted via the latent heat of vaporization into a mass-loss term, which is subtracted directly from that cell's `h_water` value in the M10 Saint-Venant integration step.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A live wire dropped into a shallow flooded intersection doesn't just electrify the puddle (`[M8-EXT-27]`) — sustained high-amperage discharge visibly boils it down over time, giving the electrified-water trap a natural expiration instead of staying lethal indefinitely.

#### [M8-EXT-31] M/M/c Queueing Labor Model for Workstation Assignment

> **tags** · general
> **tl;dr** · Declares the base "M/M/c Queueing Labor Models for workstation assignment" bullet. Reads survivor population and assigned-labor counts from M8.5's faction econo
> **ctx** · M/M/c Queueing Labor Model for Workstation Assignment -- Declares the base "M/M/c Queueing Labor Models for workstation assignment" bullet. Reads survivor population and assigned-labor counts from M8.5's faction economy; feeds task-completion timing back into settlement production rates.
> **meta** · depends-on: [M8-EXT-31] · depended-by: -

##### Systems Touched
Declares the base "M/M/c Queueing Labor Models for workstation assignment" bullet. Reads survivor population and assigned-labor counts from M8.5's faction economy; feeds task-completion timing back into settlement production rates.

##### Math
Standard M/M/c queue: `c` identical workstations (servers), Poisson task arrivals at rate `λ`, exponential service time at rate `μ` per busy station. Expected wait time before a survivor gets a free station: `Wq = C(c,λ/μ) / (cμ − λ)`, where `C(c,ρ)` is the Erlang-C probability of all `c` stations being occupied.

##### Algorithm
Each settlement task type (cooking, crafting, medical) is modeled as a queue with a fixed station count `c` (how many survivors can work it simultaneously). Idle survivors arrive at the queue when assigned; if all stations are busy they wait rather than instantly working, and expected wait time is computed live from the Erlang-C formula above as population and station count change — so adding a second workbench measurably cuts the backlog instead of needing a hand-tuned throughput number.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Overcrowded settlements visibly bottleneck at popular stations (survivors queued and idle), giving the player a clear, mathematically grounded signal that it's time to build another workstation rather than an opaque productivity stat.

#### [M8-EXT-32] Genetic-Algorithm Settlement Macro-Layout Optimizer

> **tags** · general
> **tl;dr** · Settlement layout ([M8]), worldgen ([M4]). Depends on [M4].
> **ctx** · Genetic-Algorithm Settlement Macro-Layout Optimizer -- Settlement layout ([M8]), worldgen ([M4]). Depends on [M4].
> **meta** · depends-on: [M8-EXT-32] · depended-by: -

##### Systems Touched
Settlement layout ([M8]), worldgen ([M4]). Depends on [M4].

##### Math
Fitness `F=w1·defense+w2·resourceProx+w3·connectivity`. GA: tournament select, crossover (split at road), mutate (nudge). Converges ~50 gens. Deterministic per seed.

##### Algorithm
1. Seed random layouts. 2. Score F. 3. Evolve. 4. Best=macro-layout. Fixed RNG→repeatable.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Each town laid out with reason (defensible, near water, connected) — not random blocks. Avoids sameness.

#### [M8-EXT-33] Settlement NavMesh from GA Layout *(SOURCED FROM PLAN FILE)*

> **tags** · NavMesh, GA, SOURCED, PLAN, FILE
> **tl;dr** · Consumes M8.6-EXT-09 GA building-footprint layout, produces walkable NavMesh the AI director M5.4 + horde pathing need inside a settlement. Distinct from M4-EXT
> **ctx** · Settlement NavMesh from GA Layout *(SOURCED FROM PLAN FILE) -- Consumes M8.6-EXT-09 GA building-footprint layout, produces walkable NavMesh the AI director M5.4 + horde pathing need inside a settlement. Distinct from M4-EXT-08 WFC reachability (interiors vs settlement exterior/plaza).
> **meta** · depends-on: [M8-EXT-33], [M4-EXT-09], [M5-EXT-51] · depended-by: -

##### Systems Touched
Consumes M8.6-EXT-09 GA building-footprint layout, produces walkable NavMesh the AI director M5.4 + horde pathing need inside a settlement. Distinct from M4-EXT-08 WFC reachability (interiors vs settlement exterior/plaza).

##### Math
From GA footprint polygon set, compute free-space polygon (building set subtracted from zoning polygon), then standard NavMesh triangulation (improved funnel / Delaunay over walkable region) with portal edges between adjacent polygons for string-pulling path queries.

##### Algorithm
Once M8.6-EXT-09 bakes footprints + gate positions, derives settlement walkable mesh in same one-time bake. Doorways become portals; perimeter chokepoints the GA optimized for defense become natural funnel points horde pathing uses. Cached with layout — never recomputed per-frame.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Settlements are navigable for AI + hordes — defense chokepoints double as pathing funnels.

#### [M8-EXT-34] Encounter+Loot+Story Bundle

> **tags** · general
> **tl;dr** · Assembles an event from M5.4 resolver, M8 loot, M13 lore; resolved under M8.6 faction context.
> **ctx** · Encounter+Loot+Story Bundle -- Assembles an event from M5.4 resolver, M8 loot, M13 lore; resolved under M8.6 faction context.
> **meta** · depends-on: [M8-EXT-34] · depended-by: -

##### Systems Touched
Assembles an event from M5.4 resolver, M8 loot, M13 lore; resolved under M8.6 faction context.

##### Math
bundle = {encounter, loot_table, story_ref}; instantiated at a graph node.

##### Algorithm
A world event is one struct bundling the encounter request, the loot table, and the SLM story ref; instantiated at a road/POI node (M4) under the local faction (M8.6-EXT-13).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A single ruined convoy can be a fight, a haul, and a story at once — coherent, not bolted-on.

#### [M8-EXT-35] Faction Reputation / Vendor Edge Graph

> **tags** · general
> **tl;dr** · Reputation edges consumed by vendors (price) and hub ownership (M8.6-EXT-28); fed by M5.4-EXT-17, M13-EXT-41.
> **ctx** · Faction Reputation / Vendor Edge Graph -- Reputation edges consumed by vendors (price) and hub ownership (M8.6-EXT-28); fed by M5.4-EXT-17, M13-EXT-41.
> **meta** · depends-on: [M8-EXT-35] · depended-by: -

##### Systems Touched
Reputation edges consumed by vendors (price) and hub ownership (M8.6-EXT-28); fed by M5.4-EXT-17, M13-EXT-41.

##### Math
price_mult = 1 + k_rep * tanh(edge(faction,player)).

##### Algorithm
Each faction holds a signed edge to the player; vendors read it to scale prices, and standing gates hub claims (M8.6-EXT-28). Moral acts (M5.4-EXT-17, M13-EXT-41) shift the edge.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Help a faction and they sell cheap and trust you; cross them and the price — and the door — climbs.

#### [M8-EXT-36] Civil-War Faction Event

> **tags** · general
> **tl;dr** · Driven by M8.6-EXT-13 reputation splits; emits M5.4 encounter events; M13-EXT-31 narrates.
> **ctx** · Civil-War Faction Event -- Driven by M8.6-EXT-13 reputation splits; emits M5.4 encounter events; M13-EXT-31 narrates.
> **meta** · depends-on: [M8-EXT-36] · depended-by: -

##### Systems Touched
Driven by M8.6-EXT-13 reputation splits; emits M5.4 encounter events; M13-EXT-31 narrates.

##### Math
if |intra_faction_split| > τ: civil_war(faction) = true.

##### Algorithm
When a faction's internal standing splits (some members helped player, some hurt), a civil-war flag triggers intra-faction encounter events and story broadcasts (M13-EXT-31).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Your favors can split a gang against itself — and you can walk into the crossfire.

#### [M8-EXT-37] Smuggling ↔ Encounter Hook

> **tags** · general
> **tl;dr** · Smuggling routes raise M5.4 encounter pressure; reputation (M8.6-EXT-13) modulates heat.
> **ctx** · Smuggling ↔ Encounter Hook -- Smuggling routes raise M5.4 encounter pressure; reputation (M8.6-EXT-13) modulates heat.
> **meta** · depends-on: [M8-EXT-37] · depended-by: -

##### Systems Touched
Smuggling routes raise M5.4 encounter pressure; reputation (M8.6-EXT-13) modulates heat.

##### Math
heat += smuggle_volume * (1 − rep_faction(f, player)).

##### Algorithm
Running contraband increments a heat value; high heat seeds M5.4 encounters (inspectors, rivals). Friendly factions (M8.6-EXT-13) lower heat growth.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Smuggle boldly and the world sends people to check your cargo.

#### [M8-EXT-38] Forced-Labor / Exploitation Event

> **tags** · general
> **tl;dr** · An encounter archetype consuming M5.4-EXT-11 moral framing; shifts M8.6-EXT-13 standing by response.
> **ctx** · Forced-Labor / Exploitation Event -- An encounter archetype consuming M5.4-EXT-11 moral framing; shifts M8.6-EXT-13 standing by response.
> **meta** · depends-on: [M8-EXT-38] · depended-by: -

##### Systems Touched
An encounter archetype consuming M5.4-EXT-11 moral framing; shifts M8.6-EXT-13 standing by response.

##### Math
outcome = player_response ∈ {ignore, free, exploit}; Δrep per branch.

##### Algorithm
A forced-labor scene presents a response choice; ignoring or exploiting lowers standing (M8.6-EXT-13) and feeds M13-EXT-31 broadcasts; freeing raises it. Mirrors M5.4-EXT-11 morality.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Walking past slavery has a price — the world judges the choice.

#### [M8-EXT-39] Player-Owned Hub

> **tags** · general
> **tl;dr** · Hub ownership consumed by M11-EXT-59 travel, M2.9-EXT-24 rest, M7-EXT-16 stash; gates M8.6-EXT-13 perks.
> **ctx** · Player-Owned Hub -- Hub ownership consumed by M11-EXT-59 travel, M2.9-EXT-24 rest, M7-EXT-16 stash; gates M8.6-EXT-13 perks.
> **meta** · depends-on: [M8-EXT-39] · depended-by: -

##### Systems Touched
Hub ownership consumed by M11-EXT-59 travel, M2.9-EXT-24 rest, M7-EXT-16 stash; gates M8.6-EXT-13 perks.

##### Math
hub = {owner:player, tier}; tier rises with investment.

##### Algorithm
When the player claims/sleeps at a structure it becomes a hub: registers fast-travel (M11-EXT-59), enables rest (M2.9-EXT-24) and stash (M7-EXT-16), and anchors faction perks (M8.6-EXT-13). Other factions can contest it.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Build a home and the map bends around it — it's yours, and worth defending.

#### [M8-EXT-40] Crafting Recipe Graph (Tier 2)

> **tags** · general
> **tl;dr** · Recipe set feeding M8.7-EXT-03 + M2.7-EXT-19 shell; depends on M0-EXT-40 parts and M2-EXT-21 perk ceilings.
> **ctx** · Crafting Recipe Graph (Tier 2) -- Recipe set feeding M8.7-EXT-03 + M2.7-EXT-19 shell; depends on M0-EXT-40 parts and M2-EXT-21 perk ceilings.
> **meta** · depends-on: [M8-EXT-40] · depended-by: -

##### Systems Touched
Recipe set feeding M8.7-EXT-03 + M2.7-EXT-19 shell; depends on M0-EXT-40 parts and M2-EXT-21 perk ceilings.

##### Math
avail = perk_tier >= recipe.req_tier && have(parts).

##### Algorithm
Tier-2 recipes require a perk ceiling (M2-EXT-21) and parts from the build system (M0-EXT-40); the menu shell (M2.7-EXT-19) mounts this panel. Higher tiers unlock via perk investment only.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Better gear is earned: perks raise the ceiling, build supplies the parts.

### Cluster B

#### [M8-EXT-41] Perk-Ceiling Gated Crafting

> **tags** · general
> **tl;dr** · Enforces M2-EXT-21 ceiling on M8.7-EXT-02 recipes; M0-EXT-40 parts still required.
> **ctx** · Perk-Ceiling Gated Crafting -- Enforces M2-EXT-21 ceiling on M8.7-EXT-02 recipes; M0-EXT-40 parts still required.
> **meta** · depends-on: [M8-EXT-41] · depended-by: -

##### Systems Touched
Enforces M2-EXT-21 ceiling on M8.7-EXT-02 recipes; M0-EXT-40 parts still required.

##### Math
max_tier = perk_node(M2-EXT-21).tier; reject recipe.tier > max_tier.

##### Algorithm
The crafting resolver queries the perk node (M2-EXT-21) for the player's tier ceiling and rejects any recipe above it; perks raise ceilings only, parts (M0-EXT-40) are separate.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
You can't cheese top-tier gear — perks are the only door, and they're earned.

#### [M8-EXT-50] Quest-Choice Settlement Ripple

> **tags** · narrative
> **tl;dr** · M8/M12. Quest/play choices mutate settlement/brand state + Appendix A narrative.
> **ctx** · Quest-Choice Settlement Ripple -- M8/M12. Extends G-EMERGE + M11:277 Appendix A. A choice (help vs scavenge, warn vs silence) writes to M8 settlement state + brand-tier + narrative log.
> **meta** · depends-on: - · depended-by: -

##### Systems Touched
Quest-Choice Settlement Ripple -- M8/M12. Extends G-EMERGE + M11:277 Appendix A. A choice (help vs scavenge, warn vs silence) writes to M8 settlement .

##### Math
-

##### Algorithm
On quest resolve, `ApplyChoice(questId, branch)` → adjusts `SettlementDefense` / `brandTierMultiplier` / Appendix A journal entry.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
The world remembers what you did. Settlements thrive or rot from your decisions.

#### [M8-EXT-51] Scheduled Horde-Night

> **tags** · combat
> **tl;dr** · M8/M5. Every N in-game days a horde assaults the settlement/outpost; intensity scales with defense deficit + hoarded loot.
> **ctx** · Scheduled Horde-Night -- M8/M5. Uses M8-EXT-32 funnel pathing. 7DTD blood-moon model. Not arcade checkpoint-respawn.
> **meta** · depends-on: [M8-EXT-32] · depended-by: -

##### Systems Touched
Scheduled Horde-Night -- M8/M5. Uses M8-EXT-32 funnel pathing. 7DTD blood-moon model. Not arcade checkpoint-respawn.

##### Math
`hordeSize = base * (1 + lootHoarded*k) * (1 + defenseDeficit*m)`; fires every `N` days (default 7).

##### Algorithm
Timer in M7 epoch clock; spawns I-phase Director wave along M8-EXT-32 funnels; player defenses (G-BASE) engage.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Regular, escalating, winnable-with-prep defense nights — the core tension beat.

#### [M8-EXT-52] Emergent Character Arc

> **tags** · narrative
> **tl;dr** · M8/M12. Accumulated quest/play choices form the player's transformation (grounded, not power-fantasy).
> **ctx** · Emergent Character Arc -- M8/M12. Extends M8-EXT-50 + G-EMERGE. Arc state = sum of ripples; surfaces via M11 status UI + Appendix A.
> **meta** · depends-on: [M8-EXT-50] · depended-by: -

##### Systems Touched
Emergent Character Arc -- M8/M12. Extends M8-EXT-50 + G-EMERGE. Arc state = sum of ripples; surfaces via M11 status UI + Appendix A.

##### Math
-

##### Algorithm
An `arcState` vector accumulates every quest/play ripple emitted by M8-EXT-50 (help/harm/abandon/defend events). Weighted by M8-EXT-53 standing deltas and M12-EXT-28 tragedy beats, the vector trends toward archetypes (victim / protector / scavenger / monster). Surfaces via the M11 status UI (no numbers — tone/iconography) and feeds Appendix A's procedural narrative engine for endgame framing (M12-EXT-35). Persisted with the character (M12-EXT-34), so the arc carries across sessions until permadeath.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
You become who you chose to be — victim, protector, scavenger, monster. No main story needed.

---

#### [M8-EXT-53] Faction Standing System (NV-style vector + player-founded faction + leader-of-all)

> **tags** · social, systems
> **tl;dr** · Per-faction {fame, infamy} vector (NV model); player can become leader of ANY built-in faction but NOT the zombie horde; player can found a 6th dynamic faction with its own vector.
> **ctx** · Faction Standing -- M8. Part 26 + R84 (NV). Reuses M8-EXT-14 (barter) + M8-EXT-15 (currency) + M11-EXT-64 (branching quests) + M5-EXT-60 (war spawns). Ties to M8-EXT-60 (world-state graph) + M8-EXT-59 (settlement leadership) + M12-EXT-39 (player HQ → faction capital) + M12-EXT-40 (choices write vector).
> **meta** · depends-on: [M8-EXT-14], [M8-EXT-15], [M11-EXT-64], [M5-EXT-60], [M8-EXT-60], [M8-EXT-59], [M12-EXT-39] · depended-by: [M8-EXT-14]

##### Systems Touched
Faction Standing -- extends M8-EXT-14 (barter) + M8-EXT-15 (currency) + M11-EXT-64 (quests) + M5-EXT-60 (war spawns). Each faction owns a {fame, infamy} vector; NPCs read it to decide price/aggro/dialogue. Player leadership (rank ≥ threshold) unlocks perks: quest access, settlement control, faction perks (medic, trader, horde intel). The *player-founded* faction is a dynamic 6th entry — generated via M12-EXT-39 (build HQ) + M12-EXT-40 (choices). All vectors persist via M7 and feed the world-state graph (M8-EXT-60).

##### Math
`standing[faction] = {fame ∈ [0,100], infamy ∈ [0,100]}`
- `net_standing = fame − infamy`; thresholds: friendly, neutral, hostile
- `leadership_rank = max(fame, infamy) ≥ 80 && settlement_force ≥ threshold` → NPC address changes to "Boss," faction perks unlock
- `player_faction_id = M8-EXT-60.next_dynamic_faction_slot` (6th slot, capped), owner = `player.StableId`
- `leader_of(faction) → quest_unlock(faction_quests), price_discount/nerf, spawn_intel(horde_intel)`

##### Algorithm
On faction-interaction (kill, quest, trade, gift), write Δ to the vector. NPCs sample `standing[faction]` to decide behavior: neutral → standard prices, friendly → discounts + intel, hostile → markups + aggro. Leadership (rank ≥ 80 + settlement force) unlocks: "Boss" address, unique quests (M12-EXT-37), trader discounts, horde intel feed (M5-EXT-10). Player-foundable faction: build HQ (M12-EXT-39) + reach standing threshold → dynamic 6th entry appears; its standing vectors follow same rules; can war with other factions. The zombie horde is excluded from leadership (per user mandate) — you can ally with them (M5-EXT-15 cannibal), but never lead.

##### Examples
- Player kills 10 raiders → raider.infamy = 50 → raider traders 20% markup + aggro.
- Player defends settlement 3 times → settlement.fame = 85 → "Boss," quest unlocks, trader joins.
- Player builds HQ in ruins → player-founded faction slot opens → they can recruit + war with others.

##### Failure Modes
- Vector explosion (too many factions) → hard cap at 6, player faction replaces empty slot.
- Leadership abuse (become leader of all) → each faction's leader perks are exclusive (only one active); switching costs standing.
- Hostile loop (lead everyone hostile) → some quests remain open, world darkens (G-DREAD), no game-over.

##### Player-Facing Impact
Every faction feels you. Help them → they help. Kill them → they hunt you. Lead them → they follow. Found your own → they're yours. Even evil has a path to safety in this world.

#### [M8-EXT-54] Settlement Growth Sim

> **tags** · systems
> **tl;dr** · M8. Player grows settlement: attract NPCs, upgrade, economy, services.
> **ctx** · Settlement Growth -- M8. Part 26. Reuses M8-EXT-16 (residents) + M12-EXT-30 (build) + M8-EXT-14/15 (economy) + M8-EXT-29 (walls) + M8-EXT-51 (horde) + M7 (persist). Either/or E68 RESOLVED (quest-gated): settlements grow when the player completes development quests (defend, supply, recruit) — gives agency while the sim (M8-EXT-59) handles day-to-day autonomously.
> **meta** · depends-on: [M8-EXT-16], [M12-EXT-30], [M8-EXT-14], [M8-EXT-29], [M8-EXT-51] · depended-by: -

##### Systems Touched
Settlement Growth -- M8. Part 26. Reuses M8-EXT-16 (residents) + M12-EXT-30 (build) + M8-EXT-14/15 (economy) + M8-EXT-29 (walls) + M8-EXT-51 (horde) +.

##### Math
-

##### Algorithm
Settlement level rises with residents (M8-EXT-16) + built structures (M12-EXT-30) + economy (M8-EXT-14/15); unlocks medic/trader/quest-giver; horde (M8-EXT-51) tests walls (M8-EXT-29). Persisted (M7).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Build a real community. It lives or dies by your defense.

---

#### [M8-EXT-55] Corporate Decay Layer

> **tags** · world, narrative
> **tl;dr** · M8/M4. Parody-brands + decaying-corp world layer (Borderlands/WALL-E); loot logic.
> **ctx** · Corporate Decay -- M8/M4. Part 28 (your ask). Reuses M4-EXT-39 (palette) + M4 macro-graph + M8-EXT-02 (craft mats) + M8-EXT-15 (currency) + M6.5 (diegetic jingles). Parody brands (BUY-N-MORE, HYPERION-EX, KOLO-A) — no real trademark.
> **meta** · depends-on: [M4-EXT-39], [M8-EXT-02], [M8-EXT-15] · depended-by: -

##### Systems Touched
Corporate Decay -- M8/M4. Part 28 (your ask). Reuses M4-EXT-39 (palette) + M4 macro-graph + M8-EXT-02 (craft mats) + M8-EXT-15 (currency) + M6.5 (dieg.

##### Math
-

##### Algorithm
World-gen scatters corp remnants by biome (M4-EXT-39): billboards, warehouses, armored trucks, peeling ads. Warehouses = best loot; trucks = rare. Decay = story subtext (over-extraction collapse).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A dead consumer world to pick clean. The corps that ended it are still watching, rusted.

#### [M8-EXT-56] Survivor Psyche (needs + break)

> **tags** · systems
> **tl;dr** · M8/M2. Mood/needs; push too far → break (panic/desert/lash-out). RimWorld-inspired.
> **ctx** · Survivor Psyche -- M8/M2. Part 29 + R86. Reuses M2-EXT-18 (stamina/hunger) + M8-EXT-03 (illness) + M11:162 (stress). Either/or E74 RESOLVED (moderate severity): breaks are meaningful but recoverable — panic flees, ally deserts temporarily, reckless lash-out has consequences, but the survivor returns after a cooldown; permanent loss only on death.
> **meta** · depends-on: [M2-EXT-18], [M8-EXT-03] · depended-by: -

##### Systems Touched
Survivor Psyche -- M8/M2. Part 29 + R86. Reuses M2-EXT-18 (stamina/hunger) + M8-EXT-03 (illness) + M11:162 (stress). Either/or E74 RESOLVED (moderate .

##### Math
`mood = w1*rest + w2*food + w3*safety + w4*social − w5*boredom − w6*stress`
- `boredom += dt * indoorMult` (indoorMult 1.0; outdoor 0.2; panicking → boredom resets to 0)
- `boredom` clamps [0,1]; `boredom>0.6 → stress += dt*0.3, mood -= dt*0.2`
- `boredom>0.85 → riskyImpulse` (peek windows, ignore danger) — character takes avoidable risks

##### Algorithm
Needs (rest/food/safety/social from M2-EXT-18 + M8-EXT-03 illness) feed a mood scalar. Low mood → break event (panic flee, ally desert, reckless lash-out). Reuses M2-EXT-18 decay + M11 stress controller. **Boredom** is the new axis: it accrues when the survivor is indoors and inactive (PZ moodle model), dissipates outdoors or under panic/combat. High boredom raises stress and lowers mood; very high boredom triggers risky impulse behavior (the character peeks out windows, ignores a known threat). Boredom also blocks restful sleep — a bored character cannot sleep until they go outside, read, listen to radio, or socialize (M8-EXT-16 NPCs). This is a *soft* pressure (per design decision): it pushes the player to keep moving and exploring, not a hard debuff wall.

##### Examples
- Player barricades in a shack for 3 days → boredom climbs → stress rises → character peeks outside into a wanderer → avoidable danger.
- Player clears a building, steps outside, gets in a firefight → panic → boredom resets to 0.
- Player reads a book (item) indoors → boredom decays → can finally sleep.

##### Failure Modes
- Boredom accrues during forced indoor combat → shouldn't (combat = active) → gate boredom on `activityState != combat`.
- Boredom blocks sleep permanently if no books/radio → ensure at least one countersource always craftable (M8-EXT-02).
- Mood scalar swings too hard → clamp mood delta per tick.

##### Player-Facing Impact
Your people are human. Push them and they break. Bore them and they do something stupid. The apocalypse isn't just survival — it's staying sane inside it.

#### [M8-EXT-57] AI Faction War

> **tags** · systems, ai
> **tl;dr** · M8/M5. Factions fight each other independent of player; fronts shift; exploit/avoid.
> **ctx** · AI Faction War -- M8/M5. Part 29 + R86. Reuses M8-EXT-53 (standing) + M5-EXT-60 (director). NOT a difficulty tier (M1-EXT-36 is the one dynamic diff).
> **meta** · depends-on: [M8-EXT-53], [M5-EXT-60], [M1-EXT-36] · depended-by: -

##### Systems Touched
AI Faction War -- M8/M5. Part 29 + R86. Reuses M8-EXT-53 (standing) + M5-EXT-60 (director). NOT a difficulty tier (M1-EXT-36 is the one dynamic diff).

##### Math
-

##### Algorithm
Factions (M8-EXT-53) run war state machine; director (M5-EXT-60) spawns fronts/missions; player can arms-deal, lure, or hide. Info broker (M12-EXT-32) sells live fronts.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A world at war with itself. You're one survivor in someone else's conflict.

---

#### [M8-EXT-58] Weapon Modding System

> **tags** · systems, items
> **tl;dr** · M8/M2.7. Player fills mod slots (receiver/barrel/sight/underbarrel/mag); crafted mods, tradeoffs.
> **ctx** · Weapon Modding -- M8/M2.7. Part 30. Reuses M8:317 Tier-2 kitbashing + M2.7 receiver/barrel/sight structs + M8-EXT-02 (crafting) + M8-EXT-01 (brand-tier). Either/or E75 RESOLVED (slot-limited, 3-5): weapons expose 3-5 mod slots (receiver/barrel/sight/underbarrel/mag) for clarity + build identity; free-attach reserved for endgame workshop.
> **meta** · depends-on: [M8-EXT-02], [M8-EXT-01] · depended-by: -

##### Systems Touched
Weapon Modding -- M8/M2.7. Part 30. Reuses M8:317 Tier-2 kitbashing + M2.7 receiver/barrel/sight structs + M8-EXT-02 (crafting) + M8-EXT-01 (brand-tie.

##### Math
-

##### Algorithm
Weapon has slot groups; player attaches crafted mods (M8-EXT-02) → mutates Tier-2 instance (M8:317) within clamped bounds. Tradeoffs: damage↑/handling↓/weight↑. Brand-tier (M8-EXT-01) gates reliability.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Every gun is yours. Build the zombie-mobile's companion piece.

---

#### [M8-EXT-59] Autonomous Settlement Sim

> **tags** · systems, ai
> **tl;dr** · M8. Settlements run their own AI: NPCs auto-build, trade, recruit, defend. Sim Settlements-style.
> **ctx** · Autonomous Settlement Sim -- M8. Part 32. Reuses M8-EXT-16 (settlement NPCs) + M8-EXT-54 (growth) + M8-EXT-53 (standing) + M8-EXT-56 (psyche) + M8-EXT-02 (crafting) + M8-EXT-29 (walls). Either/or E81 RESOLVED (player-directable): the sim runs autonomously but the player issues high-level directives (build X, prioritize Y, recruit Z); the sim executes — Sim Settlements model, not blind AI.
> **meta** · depends-on: [M8-EXT-16], [M8-EXT-54], [M8-EXT-53], [M8-EXT-56], [M8-EXT-02], [M8-EXT-29] · depended-by: -

##### Systems Touched
Autonomous Settlement Sim -- M8. Part 32. Reuses M8-EXT-16 (settlement NPCs) + M8-EXT-54 (growth) + M8-EXT-53 (standing) + M8-EXT-56 (psyche) + M8-EXT.

##### Math
-

##### Algorithm
Each settlement runs a simulation tick: assess needs (food/safety/population) → auto-assign NPCs to plots (residential/agricultural/industrial/defensive) → build structures from prefab kit → trade excess with connected settlements. Player can influence by gifting resources, assigning priorities, or building specific plots. Plots level up over time (shack → house → fort) based on prosperity. NPCs have individual schedules (M8-EXT-56) that contribute to the settlement output.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Settlements live without you. You are the visitor, fixer, and ally — not the janitor. Every settlement grows differently.

#### [M8-EXT-60] World-State Cascade Engine

> **tags** · systems, world
> **tl;dr** · M8. Every action has ripple effects: blocked trade route → starved town → raid → reputation collapse → faction war.
> **ctx** · World-State Cascade Engine -- M8. Part 32. Reuses M8-EXT-53 (standing) + M8-EXT-57 (faction war) + M8-EXT-54 (growth) + M8-EXT-59 (settlement sim) + M8.5 (economy). Either/or E82 RESOLVED (simulated): cascades are emergent simulations over the world-state graph, not scripted event chains — compounding consequences arise from node interactions, delivering the "every choice matters" payoff.
> **meta** · depends-on: [M8-EXT-53], [M8-EXT-57], [M8-EXT-54], [M8-EXT-59] · depended-by: -

##### Systems Touched
World-State Cascade Engine -- M8. Part 32. Reuses M8-EXT-53 (standing) + M8-EXT-57 (faction war) + M8-EXT-54 (growth) + M8-EXT-59 (settlement sim) + M.

##### Math
-

##### Algorithm
World-state graph tracks nodes (settlements/factions) and edges (trade routes, alliances, wars). Each player action — blocking a road, killing a leader, gifting supplies, destroying a zombie nest — propagates through the graph. Starvation → raid → casualties → reputation loss → abandoned settlement → power vacuum → new faction. Cascades are tracked over days/weeks, not instantly. The director (M5) can inject events that accelerate cascades.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Block a road and a town starves. They attack you. You kill them. Their allies hate you. Trade stops. War starts. Every decision propagates through the world-state graph with compounding consequences.

#### [M8-EXT-61] Trade Route Network

> **tags** · systems, world
> **tl;dr** · M8. Visible roads connect settlements; goods flow; player can protect, block, or establish routes.
> **ctx** · Trade Route Network -- M8. Part 32. Reuses M8-EXT-14 (barter) + M8-EXT-15 (currency) + M8.5 (economy) + M8-EXT-59 (settlement sim) + M8-EXT-60 (cascade engine) + M8-EXT-57 (faction war carries risk). Either/or E83 RESOLVED (visible): routes are drawn on the map (M12-EXT-33 war map reuses) so the player sees supply lines and can target them — the core "block a road, starve a town" loop.
> **meta** · depends-on: [M8-EXT-14], [M8-EXT-15], [M8-EXT-59], [M8-EXT-60], [M8-EXT-57] · depended-by: [M8-EXT-14], [M8-EXT-15], [M8-EXT-60]

##### Systems Touched
Trade Route Network -- M8. Part 32. Reuses M8-EXT-14 (barter) + M8-EXT-15 (currency) + M8.5 (economy) + M8-EXT-59 (settlement sim) + M8-EXT-60 (cascad.

##### Math
-

##### Algorithm
Roads (scavenged from M4 world-gen + player-built) connect settlements. Goods flow along routes: food, ammo, meds, materials. Each route has a throughput cap, risk level (zombie ambush, bandit raid), and owner. Player can patrol (lower risk), block (cut supply), establish (connect two allied settlements), or raid (short-term loot, long-term reputation hit). Blocked routes cascade (M8-EXT-60): starved settlement → desperate raids → instability.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
You see the supply lines. Cut one and watch a town starve. Protect one and build allies. The economy is visible and touchable.

---

#### [M8-EXT-62] NPC Social Stratification

> **tags** · systems, npc, society
> **tl;dr** · M8. Settlements have class tiers (elite/mid/shanty) driven by wealth + power; determines plots, housing, quests.
> **ctx** · NPC Social Stratification -- M8. Part 33. Reuses M2-EXT-77 (NPC gen) + M8-EXT-16 (residents) + M8-EXT-59 (auto-sim) + M8-EXT-64 (land) + M8-EXT-53 (standing). CK3 courtier rank/opinion model. Either/or E89 RESOLVED (fluid): stratification is a dynamic layer — merit, wealth, and standing shifts an NPC's tier over time (a trusted scavenger can rise to foreman; a bankrupt trader falls to the slums), reinforcing the emergent-world design.
> **meta** · depends-on: [M2-EXT-77], [M8-EXT-16], [M8-EXT-59], [M8-EXT-64], [M8-EXT-53] · depended-by: -

##### Systems Touched
NPC Social Stratification -- M8. Part 33. Reuses M2-EXT-77 (NPC gen) + M8-EXT-16 (residents) + M8-EXT-59 (auto-sim) + M8-EXT-64 (land) + M8-EXT-53 (st.

##### Math
-

##### Algorithm
Each NPC has Wealth (resources owned) + Power (position/influence) scores. Tiers: **Elite** (mayor, warlord, top trader — owns prime land M8-EXT-64, lives in fortified core), **Middle** (foreman, medic, skilled trader — decent housing), **Bottom/Shanty** (scavenger, refugee, slave — stacked housing M8-EXT-63). Stratification drives: plot allocation, quest giver importance, prices (M8-EXT-14 barter gates on tier), and standing (M8-EXT-53) weights. Player actions can shift an NPC's tier (save them → rise; rob them → fall).

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
A real society with haves and have-nots. Where you stand in it changes what you can do, buy, and who talks to you.

#### [M8-EXT-63] Shanty Town / Vertical Slums

> **tags** · world, settlement, procgen
> **tl;dr** · M8/M4. Bottom-tier NPCs build dense vertical shanty stacks — depth, not flat sprawl.
> **ctx** · Shanty Town / Vertical Slums -- M8/M4. Part 33. Reuses M8-EXT-62 (stratification) + M8-EXT-64 (land) + M4-EXT-25 (material compiler) + M3 (structures) + M4-EXT-39 (palette). Watabou-style vertical city stacking. Either/or E90 RESOLVED (procedural-stacked): shanty clusters are generated procedurally from the bottom-tier population + land cap, using M3 prefab stacks + M4-EXT-25 materials; players may reinforce/extend existing stacks but do not author from scratch.
> **meta** · depends-on: [M8-EXT-62], [M8-EXT-64], [M4-EXT-25], [M4-EXT-39] · depended-by: -

##### Systems Touched
Shanty Town / Vertical Slums -- M8/M4. Part 33. Reuses M8-EXT-62 (stratification) + M8-EXT-64 (land) + M4-EXT-25 (material compiler) + M3 (structures).

##### Math
-

##### Algorithm
Bottom-tier zone generates 3-level vertical slum clusters: scrap huts stacked on top of each other (reuse M3 structure prefabs + M4-EXT-25 materials), connected by ladders/walkways, crammed into a small footprint. Density driven by settlement population vs land cap (M8-EXT-64). Each floor = a household (M2-EXT-77 NPC family). Verticality creates navigation depth (rooftop routes, ground alleys), ambush layers, and a distinct visual identity vs the elite core.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
You climb through stacked shacks to reach the fixer on the third floor. The slums have more life per square meter than anywhere else — and more danger.

#### [M8-EXT-64] Land & Plot Allocation by Status

> **tags** · systems, settlement
> **tl;dr** · M8. Land area + plot count per NPC/faction scaled by wealth/power tier.
> **ctx** · Land & Plot Allocation -- M8. Part 33. Reuses M8-EXT-62 (stratification) + M8-EXT-59 (auto-sim) + M12-EXT-30 (build) + M4-EXT-32 (POI). Either/or E91 RESOLVED (fixed-tier allotment): land budget is divided by stratification tier at generation (Elite/Middle/Bottom), with player promotion/demotion via standing (M8-EXT-53) and wealth; no open land market, keeping the economy legible and the cascade legible.
> **meta** · depends-on: [M8-EXT-62], [M8-EXT-59], [M12-EXT-30], [M4-EXT-32] · depended-by: -

##### Systems Touched
Land & Plot Allocation -- M8. Part 33. Reuses M8-EXT-62 (stratification) + M8-EXT-59 (auto-sim) + M12-EXT-30 (build) + M4-EXT-32 (POI). Either/or E91 .

##### Math
-

##### Algorithm
Settlement land budget (from M8-EXT-59 growth + M4-EXT-32 space) divided by tier: Elite gets large walled plots (forecourt, multiple buildings), Middle gets standard lots, Bottom gets stacked micro-plots (M8-EXT-63). Factions (M8-EXT-53) claim territory by power. Player can buy/earn land (M8-EXT-15 currency) to move up. Allocation re-evaluated on cascade events (M8-EXT-60) — war loser loses land.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
Wealth is visible in the ground plan. The mayor's compound versus the stacked slum tells you everything about who holds power.

#### [M8-EXT-65] NPC Daily Life Simulation

> **tags** · systems, npc, sim
> **tl;dr** · M8. NPCs run autonomous daily routines (work/social/sleep) that build their personal plot over time.
> **ctx** · NPC Daily Life Simulation -- M8. Part 33. Reuses M2-EXT-77 (NPC gen) + M8-EXT-16 (residents) + M8-EXT-56 (psyche) + M8-EXT-59 (auto-sim) + M8-EXT-62 (stratification). Either/or E92 RESOLVED (full routine sim): NPCs execute a continuous daily schedule (wake → job → eat → socialize → personal-plot → sleep) that advances personal plots and feeds the cascade engine; keyframe-only would sacrifice the "world breathes" payoff.
> **meta** · depends-on: [M2-EXT-77], [M8-EXT-16], [M8-EXT-56], [M8-EXT-59], [M8-EXT-62] · depended-by: -

##### Systems Touched
NPC Daily Life Simulation -- M8. Part 33. Reuses M2-EXT-77 (NPC gen) + M8-EXT-16 (residents) + M8-EXT-56 (psyche) + M8-EXT-59 (auto-sim) + M8-EXT-62 (.

##### Math
-

##### Algorithm
Each NPC ticks a daily schedule: wake → job (mapped to their role/plot) → eat → socialize (gossip spreads standing M8-EXT-53 changes between NPCs — a rumor of a blocked trade route M8-EXT-61 can flip a whole tier's mood) → personal-plot action (debt payment, ambition step) → sleep. Routine contributes to settlement output (M8-EXT-59) and advances their personal plot (M2-EXT-77); when a plot hits a threshold it injects an event into the cascade engine (M8-EXT-60). Psyche (M8-EXT-56) can break a routine (panic, desertion). Player can observe or interrupt — talking to an NPC mid-routine can divert it.

##### Examples
-

##### Failure Modes
-

##### Player-Facing Impact
The settlement operates on a continuous schedule. A merchant opens shop, a guard rotates patrol, a resident crosses the slum — the simulation advances independently of player presence, so the world state is never static.

---

#### [M8-EXT-66] Interaction Tool Economy

> **tags** · economy, crafting, interaction
> **tl;dr** · M8. The immersive interactions consume/require crafted tools (welding torch, lockpick set, hotwire kit, datapad, screwdriver) — made via M8-EXT-02, bought via M8-EXT-14/15, lost on break.
> **ctx** · Interaction Tool Economy -- M8. Gives the five interaction minigames (M9-EXT-30..34) a material cost and a crafting sink, tying them into the living economy (M8-EXT-14/15) and survival pressure. Reuses M8-EXT-02 (crafting) + M8-EXT-14 (barter) + M8-EXT-15 (currency) + M9-EXT-30..34 + M2-EXT-79 (proficiency).
> **meta** · depends-on: [M8-EXT-02], [M8-EXT-09], [M8-EXT-14], [M8-EXT-15], [M9-EXT-30], [M9-EXT-31], [M9-EXT-32], [M9-EXT-33], [M9-EXT-34] · depended-by: -

##### Systems Touched
All five minigames draw tools from the player's inventory (M8-EXT-09). Torch consumes fuel per weld (M9-EXT-30); lockpick set bends/breaks on over-lift (M9-EXT-32); hotwire kit provides a bypass fob (M9-EXT-31); datapad auto-solves one hack node (M9-EXT-33); screwdriver is the basic pry tool. Tools are crafted (M8-EXT-02) from scrap + traded (M8-EXT-14/15).

##### Math
Tool `durability d` decrements per use; `break` when `d≤0`. Torch fuel `f` drains at rate `r_weld·(1/bandWidth)`. Replacement cost `c = base·scarcityMultiplier(M8-EXT-15)`.

##### Algorithm
Before a minigame opens, the shell (M2-EXT-78) checks for the required tool; missing tool → action unavailable (must craft/buy). Using the tool drains durability/fuel; a broken tool must be re-crafted (M8-EXT-02), creating a resource loop that keeps interactions meaningful. Rich tiers (M8-EXT-62) sell better tools (wider windows); bottom-tier scrap tools are fragile. This makes the immersive layer part of the economy, not free verb-spam.

##### Examples
- Player wants to weld a plow (M9-EXT-30) but the torch is empty → must find fuel (M9-EXT-09) or buy a full torch at a trader (M8-EXT-14).
- Lockpick set bends on a gun-safe over-lift (M9-EXT-32) → must craft a new one from scrap (M8-EXT-02) before the next safe.

##### Failure Modes
-

##### Player-Facing Impact
Skills need tools, tools need scrap, scrap needs the world. The immersive layer is woven into survival, not bolted on.

---

#### [M8-EXT-67] G-LOOTRISK: Threat-Scaled Loot Economy

> **tags** · loot, risk, economy
> **tl;dr** · The loot economy is framed as risk/reward: dangerous zones yield better gear; safe zones yield scraps. The design-layer framing over M8-EXT-01 + M12-EXT-38.
> **ctx** · G-LOOTRISK: Threat-Scaled Loot Economy -- M8. New block. Implements the G-LOOTRISK gap from the SIX-DECISIONS plan. The mechanical loot-tier math lives in M12-EXT-38; this block is the design pillar + economy integration. Ties to M8-EXT-01 (brand-tier) + M4-EXT-33 (loot tables) + M12-EXT-37 (encounter zones).
> **meta** · depends-on: [M8-EXT-01], [M4-EXT-33], [M12-EXT-38], [M12-EXT-37] · depended-by: -

##### Systems Touched
M8 loot/economy + M12 threat zones. The brand-tier loot system (M8-EXT-01) is the supply side; M12-EXT-38 is the zone-risk multiplier. This block ensures the *player understands* loot as a risk calculus: the best gear is behind the worst threats, and scarcity (M8-EXT-15) makes it precious.

##### Math
`perceivedValue(item) = baseValue(item) * (1 + threatOfSourceZone)`
- Player risk-calc: `expectedLoot(zone) vs expectedThreat(zone)` → rational players raid dangerous zones for tier-1 gear
- Scarcity (M8-EXT-15): post-loot, item leaves world (M7-EXT-21) → permanent scarcity, not respawn-farm

##### Algorithm
The economy surfaces risk/reward through world signals: dangerous zones *look* dangerous (M4-EXT-96 density, M6.5 audio, M11-EXT-55 encounter HUD). The loot tables (M12-EXT-38) deliver the payoff. Scarcity (M8-EXT-15) means once looted, an item is gone — the player carries the risk/reward with them. Traders (M8-EXT-14) price by brand-tier + scarcity, so risky-looted military gear sells high. The loop: risk → rare loot → economy leverage → better survival.

##### Examples
- Player raids a hospital (high threat, M12-EXT-37) → rolls antibiotics + trauma kit (M12-EXT-38) → saves a life or sells for premium.
- Player farms quiet suburbs → canned food + tools → survives but never upgrades.
- Player loots a military convoy → body armor → trades at a hub → buys a welder (M8-EXT-66) → builds better base.

##### Failure Modes
- Risk/reward unclear to player → world signals (audio/visual) must telegraph threat before loot.
- Scarcity too harsh → economy stalls → traders (M8-EXT-14) inject measured supply.
- Loot tier caps break (god drops in safe zones) → M12-EXT-38 hard cap enforces.

##### Player-Facing Impact
Loot is a decision, not a chore. The best gear costs the most to get. The apocalypse pays risk in gear.

#### [M8-EXT-68] Evil / Good Alignment (meter-less, action-based)

> **tags** · social, alignment, morality
> **tl;dr** · Subtle alignment track (no visible meter) — actions tilt you toward altruist, warlord, cannibal, or ghost. Both paths reach all mechanical endpoints; alignment shapes NPC tone and event triggers, not locks.
> **ctx** · Evil / Good Alignment -- M8. New block. Implements the "player can be evil or good" mandate. Reuses M5-EXT-11 (moral framing), M8-EXT-16 (psyche), M12-EXT-34 (permadeath stakes). Inspired by Skyrim's subtle tone (no meter) + Witcher's consequence model (every choice echoes). Ties to M8-EXT-53 (standing vector) + M8-EXT-50 (events) + M11-EXT-65 (dread).
> **meta** · depends-on: [M5-EXT-11], [M8-EXT-16], [M12-EXT-34], [M8-EXT-53] · depended-by: -

##### Systems Touched
M8 alignment + standing + psyche. An `alignmentScore` (internally −1..+1, never shown) tracks actions: aid +1, kill +0.3, betray +0.7, consume_ally −0.5, cannibal +0.9. No meter — alignment surfaces via NPC tone ("savior" vs "monster"), world events (M8-EXT-50), and dread (M11-EXT-65). Both extreme paths reach all mechanical endpoints; alignment is flavor + friction, not content gating.

##### Math
`alignment ∈ {altruist (+0.6..+1), protector (+0.2..+0.6), neutral (−0.2..+0.2), selfish (−0.6..−0.2), monster (−1..−0.6)}`
- `dread_contribution = 0.1 * (1 − |alignment|)` → feeds G-DREAD
- `NPC_address_scale = alignment * tone_factor` → "savior" to "monster"
- `event_branch_factor` → some events tilt toward altruist or monster

##### Algorithm
Each moral action (quest choice, combat on civilian/surrender, sharing/hoarding, cannibal) writes Δalignment. NPCs read alignment to choose tone: altruist → "savior," monster → "monster." Events (M8-EXT-50) branch: a monster path may offer easier but cruel choices, altruist path harder but generous. Dread (M11-EXT-65) feeds on distance from neutral, not absolute evil — being *pure* anything makes the world wary. No lock ensures both paths stay viable.

##### Examples
- Player heals civilians, shares food → alignment → +0.7 → NPCs call "savior," settlement recruits flock.
- Player hoards, kills surrender → alignment → −0.8 → traders fear, guards hunt, but warlord quests open.
- Player cannibal (M5-EXT-15) → alignment → −0.9 → feared, but can still trade with raider faction.

##### Failure Modes
- Alignment becomes a lock (good path only) → ensure all gear/bases reachable on both paths.
- Monster path too easy → raise price, raise dread, fewer recruits.
- Tone feels fake → tie tone to real standing deltas (M8-EXT-53), not cosmetic.

##### Player-Facing Impact
The world sees you. Savior or monster, the living remember. But no path is shut — survive your way, live with the judgment.


